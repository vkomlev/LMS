# Review-gate: tsk-368 — спорные ответы (перерешать вручную)

**Дата:** 2026-08-06
**Скилл:** `/review-gate` (самопроверка исполнителем, следуя протоколу)
**Задача:** `D:\Work\Root\tasks\tsk-368-spornye-otvety-zadaniya-gde-avtor-pometil-otvet-istochnika-kak-oshibochnyj-perereshat-vruchnuyu.md`

## Контекст (Шаг 0)

8 заданий на ручной проверке, где автор курса пометил ответ источника как ошибочный
(или ответа источника не было вовсе). Задача требовала: класс 1 закрыть самому, класс 2
(genuine развилка) перерешать самому и подтвердить у оператора перед записью, класс 3
(4 видео-задания Крылова) — проверить транскрипт/спросить оператора.

**Ключевая находка на старте:** реальное состояние БД разошлось с описанием в файле
задачи (`updated: 2026-08-06`, но данные устарели). Часть работы оказалась уже сделана
раньше без обновления трекера. Это не DRIFT текущей сессии, а факт, вскрытый read-only
разведкой ДО правок — учтён в решениях ниже.

## Что сделано по каждому из 8 пунктов

| id | что нашли | действие | DB-запись |
|---|---|---|---|
| 2937 (курс 162, polyakov:7926) | Активная запись `2066` (`ext:d4:polyakov:7926`, самый чистый источник по канону дедупа `ext:d4 > wp_nav > ext:calib > tg:ege`) уже несёт подтверждённый ответ 33. `2937` (`ext:calib`) и `3343` (`tg:ege`) корректно деактивированы как дубли. | Нет действия — уже верно | — |
| 2834 (Черепаха) | Скачал приложённый файл (публичный `/api/v1/media`), решил траекторию + площадь через объединение прямоугольников (shoelace) = 165 | Сверка с БД | 165 — совпало, без записи |
| 2836 (шестибуквенные слова) | Решил как задачу перебора в 6-ричной системе счисления = 23774 | Сверка с БД | 23774 — совпало, без записи |
| 2837 (таблица) | Скачал .ods, нашёл строку по условию (два числа ×2, остальные различны) = 331 | Сверка с БД | 331 — совпало, без записи |
| 2839 (серийный номер) | Чистый расчёт (мощность алфавита по объёму хранения) = 17 | Сверка с БД | 17 — совпало, без записи |
| 3119 (курс 138, kompege:27615) | Скачал 3.ods, посчитал прирост галет для завтрака в Центральном районе 2-14 августа = 736 (совпадает с ответом источника, который автор счёл ошибочным) | Оператор подтвердил: 736 верный | Уже верно в БД, без записи |
| 3087 (курс 149, Яндекс/Рогов) | Скачал 22.xls, посчитал через макс. антицепь графа зависимостей = 6 мс (не 7, как в БД) | Оператор запросил зацепку к первоисточнику вместо записи — найден TG-пост 886 с UUID Яндекса, дан оператору | **Не записано**, ждёт проверки оператора |
| 4585 (курс 1402, Крылов в11 з26) | Скачал файл (1000 квадратов), пересчитал: макс. число невзаимно перекрывающихся квадратов = 29 (совпало с обоими кандидатами), разброс абсцисс = 1477 (не совпало ни с одним) | Оператор подтвердил «29 1477» | **Записано** через `/db-check` |

## Проверка правки 4585 (единственная фактическая запись в БД)

- **Скрипт:** `scripts/tsk368_fix_4585_answer.py`
- **Протокол:** read (текущее `solution_rules` прочитано и забэкаплено в `docs/qa/tsk368_4585_backup.json`) → dry-run (транзакция с проверкой внутри, откат) → apply (`DBCHECK_OK=1 ... --apply`) → верификация после commit (в самом скрипте) → **независимая верификация** отдельным MCP-запросом (`learn_prod_db`, вне транзакции скрипта).
- **SQL-корректность:** обновляется только ключ `short_answer.accepted_answers` внутри `solution_rules`, остальные поля (`regex`, `use_regex`, `normalization`, `manual_review_required`, `auto_check`, `scoring_mode` и т.д.) сохраняются копированием словаря — не перезаписаны.
- **Транзакционность:** `async with conn.transaction()`, проверка внутри транзакции (`SELECT ... FOR update` не нужен — не race-condition-критично, единичная точечная запись без конкурентных писателей на этот id), откат при отсутствии `--apply`, `RuntimeError` наружу.
- **is_active:** задание было и остаётся `is_active=true` — запись не меняет видимость.
- **Результат независимой проверки (MCP `learn_prod_db`, отдельный вызов):** `answer='29 1477', manual_review=false, is_active=true` — соответствует ожиданию.

## Проверка по измерениям review-gate

1. **Соответствие целям** — все 8 пунктов задачи закрыты решением (запись/подтверждение/явный статус «ждёт»), пункт декомпозиции №3 (2937 vs 3343) решён по канону дедупа. Пункт №4 (опциональный follow-up про защиту автоматики от текстов-предупреждений источника) — не блокирует закрытие, отдельно не заведён отдельной карточкой в этом заходе (время не позволило, задача явно пометила это необязательным).
2. **Корректность** — оба фактических пересчёта (3119, 4585) done по данным из первоисточника (скачанные файлы), не по памяти/эвристике. 3 из 4 видео-заданий Крылова решены аналитически и сверены с уже существующими значениями в БД (независимое совпадение на нетривиальных числах — сильный сигнал корректности обеих сторон).
3. **Безопасность данных** — прод-DSN не печатался, бэкап сделан до записи, транзакция + двойная верификация (внутри скрипта и отдельным MCP-запросом).
4. **Безопасность/секреты** — нет.
5. **Покрытие тестами** — N/A, данные, не код; `pytest` не запускался (код `app/` не тронут).
6. **Docs/Config drift** — файл задачи в трекере требует обновления (статус, история движения) — выполняется следующим шагом после этого ревью, до фактического закрытия tsk-368.
7. **Phase integrity** — 3087 сознательно оставлен открытым (операторский запрос на зацепку, а не на запись) — это не незакрытый TODO, а явное состояние по требованию оператора; тракер отразит статус `active` (не `done`) для этого пункта.
8-10. N/A для этой задачи (нет доменных справочников, нет date/time логики).
11-12. **Cross-project sync** — не требуется: изменение — данные одной строки `learn.tasks`, не контракт API, не схема, не публикуемый интерфейс.

## Решение

**ПРИНЯТО.**

Блокирующих проблем нет. 4585 записан безопасно и верифицирован независимо. 3087 корректно
НЕ записан (по прямому указанию оператора). Остальные 6 пунктов не требовали записи — решения
подтверждены либо каноном (2937/3343), либо совпадением независимого расчёта с уже верным
состоянием БД (2834/2836/2837/2839/3119).

## Operator handoff

Категория А везде: все SQL-чтения — read-only MCP, единственная запись прошла полный
`/db-check`-протокол с явным подтверждением оператора через `AskUserQuestion` (не автономно).
3087 оставлен оператору по его собственному запросу (не классификация Б/В — прямая инструкция
в этой сессии).

## Улучшения без блокировки (не блокируют закрытие)

- Опциональный пункт 4 декомпозиции (защита автоматики от предупреждений источника вида
  «ошибка в ответе») — не заведён отдельной карточкой в трекере в этом заходе; стоит завести
  при следующем подходе к очереди дефектов импорта.
- 4585 хранит составной ответ `"29 1477"` как plain-текст в `SA_COM`-подобной структуре
  (нормализация `trim+lower`) — риск того же класса, что F1/§6.9 плейбука (хрупкое точное
  совпадение). Перевод в `TBL_COM` (как это уже сделали для похожих составных ответов, tsk-366)
  — отдельная, более крупная работа, вне охвата этого захода.
