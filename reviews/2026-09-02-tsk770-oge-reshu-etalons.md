# tsk-770 — Эталоны синтетических партий РешуОГЭ расходятся с условием

Дата: 2026-09-02 · Курсы 1128 (задание 4), 1152 (задание 7), 1112 (задание 2) · Боевая БД

## Что случилось

Ученик 4533 решил задание 6551 («кратчайший путь А→Е»), дважды ответил **3** и дважды
получил незачёт. Он прав: перебор всех простых путей даёт 3 (A-D-E), а в базе стоял 6.

## Диагноз: испорчен эталон, а не условие

Это не самоочевидно, и проверять пришлось отдельно. У девяти заданий эталон в точности
равен длине **второго по краткости** пути, и почти всегда снятие одного ребра (чаще
последнего в списке дорог) превращает пересчёт в эталон — то есть гипотеза «условие
потеряло ребро, а эталон верен» выглядела не хуже.

Решили три задания, где эталон **недостижим ни одной длиной пути и ни при каком снятии
ребра** (6564: эталон 7 при длинах 6, 8, 9, …). Такое число не может быть «ответом на
другой вопрос» — оно выдумано. Значит источник истины — условие, и его надо пересчитать.

Подтверждение независимое, не только расчётом: ученик 4533 сам получил 3, 3 и 6 на трёх
заданиях, а ученик 4552 на 6408 дважды ответил `АДЖТДТАТ` — ровно единственный верный
разбор радиограммы. Оба получали незачёт.

## Откуда взялись неверные числа

Партия собрана в июле (tsk-155) не парсингом РешуОГЭ, а **порождением языковой моделью**:

* у 10 из 13 партий `oge:reshu:*` в ключе стоит настоящий 5-значный ID задачи sdamgia
  (`oge:reshu:t3:10632`), а у **t2, t4, t7** — порядковый номер с шагом 20
  (3, 23, 43 … 624). Подпись «Источник: РешуОГЭ, задача 143» указывает в пустоту;
* условие — не таблица расстояний, как на РешуОГЭ, а причёсанный список рёбер строкой;
* ответ модель считала **тем же неполным перебором**, что и ученик-новичок: теряла одно
  ребро и выдавала второй по краткости путь.

Артефакты генератора (`problems_t4.json`, `oge_bank_load.py`) не сохранились — они жили в
скратчпаде сессии. Воспроизводимого конвейера у этих партий нет.

**Признак класса на будущее:** `external_uid` синтетической партии не содержит ID
источника. Это дешёвая механическая проверка, в отличие от приёма ниже.

## Приём «число в заголовке против эталона» не универсален

Он сработал на t4 случайно: там заголовки были вида «Кратчайший путь длиной N км», то есть
несли сам ответ. На других партиях число в заголовке — это входные данные, и приём даёт
лавину ложных срабатываний: 23 в t5, 19 в t3, 28 в t10. Опираться на него нельзя;
надёжен только пересчёт задачи решателем.

## Что исправлено (15 заданий)

Правился только `solution_rules.short_answer.accepted_answers[0].value`, в транзакции, с
поштучной верификацией и снимком прежних правил рядом.

**t4, курс 1128 — 12 из 30** (было → стало): 6551 6→3 · 6552 6→4 · 6553 7→6 · 6554 8→5 ·
6555 5→3 · 6563 6→5 · 6564 7→6 · 6565 6→5 · 6566 7→6 · 6569 5→2 · 6570 6→2 · 6573 4→3.

**t7, курс 1152 — 2 из 30:** 6711 `ДБГЕАВ`→`ДБАГЕВ` (собирает
`Plotnikov_11klass@school.pochta.ru`) · 6724 `4213576`→`4215376` (`http://htm.ru/edu.txt`).

**t2, курс 1112 — 1 из 30:** 6408 `АДЖЛДЛАЛ`→`АДЖТДТАТ` (прежний эталон требовал 29
символов Морзе при строке в 17).

Скрипты: `scripts/tsk770_fix_oge_t4_etalons.py`, `..._t7_...`, `..._t2_...` — каждый решает
задание заново из стема в базе, а не по переписанным данным.

## Живая проверка

На боевом сайте под сессией оператора: 6551 ответ `3` → зачёт, 6553 ответ `6` → зачёт
(`task_results` 21553 и следующий, `is_correct=true`). Именно эти ответы раньше не
засчитывались.

## Переделка условий (второй заход, по решению оператора)

Семь заданий правкой эталона не чинились — у них было сломано само условие. Переделаны
скриптом `scripts/tsk770_rebuild_broken_tasks.py`: точечная замена в стеме, затем
**обязательная проверка решателем** — ответ обязан быть единственным и равным
заявленному, иначе транзакция откатывается целиком. Проверка идёт не по подготовленному
тексту, а по перечитанному из базы.

**Условие не читалось ни одним разбором** — строка восстановлена из эталона:

* **6404**: строка `????€?€` → `???€??€` (склейка кодов Б+Ы+К). Дополнительно разведены
  коды Ы и А — генератор выдал обоим `?€?`, и ответ был бы неоднозначен: А=`€€`.
* **6409**: строка `!!???!??` → `!!??!???` (склейка К+О+Л).

**Условие допускало несколько верных ответов** — двойник разведён:

* **6410**: `Р=!!!?` → `Р=!!!!`. Прежний код Р давал второй верный разбор `СР` наравне
  с `СЕД` — именно за него ученик 4552 получил незачёт 30 августа.
* **6419**: цепочка `20335` (→ТВВД) заменена на `11213`, которая читается многими
  способами. Теперь однозначна ровно одна цепочка, как и обещает условие. Заголовок
  «Однозначная расшифровка шифровки 20335» называл не ту цепочку → заменён на
  «Шифровка с единственной расшифровкой».

**Вырожденное условие** — вес одного ребра возвращён к значению, при котором сходятся
и заголовок, и исходный эталон партии (то есть условие восстановлено ПО эталону):

* **6568**: `A-F=5` → `A-F=7`. Задача решалась прямой дорогой в обход маршрута из
  заголовка; теперь ответ 6 через E, как заголовок и заявлял.
* **6569**: `D-F=1` → `D-F=4`. Ответ 5 (был вырожденный 2), совпал с исходным эталоном.
* **6570**: `B-F=1` → `B-F=5`. Ответ 6 «через B напрямую» (был вырожденный 2), совпал
  с исходным эталоном и заголовком.

Живая проверка на проде: 6404 ответ `БЫК` → зачёт, 6570 ответ `6` → зачёт.

## Снято ложное срабатывание

6396–6399 в первом заходе попали в хвост как «неоднозначные» ошибочно. В их условии есть
оговорка **«Буквы в сообщении не повторяются»**, и с её учётом вторые разборы (ОИОИМП,
ЛИМНМН, ЛЕСАСА, ПЛПЛЕТ) отпадают — там буквы повторяются. Задания исправны и не
трогались; оговорку теперь учитывает и проверочный скрипт
(`decode_all(..., no_repeat=...)`), чтобы он не поднимал ложную тревогу впредь.

## Итоговое состояние партий

Все три синтетические партии проверены решателем целиком, по каждому заданию:

| Партия | Курс | Заданий | Расхождений |
|---|---|---|---|
| t2 | 1112 | 30 | 0 |
| t4 | 1128 | 30 | 0 |
| t7 | 1152 | 30 | 0 |

Незакрытых хвостов по этим партиям нет.

## Возврат баллов и попыток (третий заход, по решению оператора)

**Баллы.** Вердикты семи сдач исправлены скриптом
`scripts/tsk770_restore_student_verdicts.py`. Правился вердикт САМОЙ сдачи, а не
выставлялся ручной зачёт отдельной строкой: штатный `grant_task` создаёт новую строку
`task_results` и тем самым тратит ещё одну попытку — именно так 6551 стало 3/3. Здесь же
ошиблась проверка, а не ученик, и его строка должна стать зачётной.

Отбор вычисляемый, без списка id: берутся незачётные сдачи, где ответ совпадает
с текущим (уже исправленным) эталоном. Ответы, неверные и после исправления, не
трогались.

| Ученик | Задание | Ответ | Сдач зачтено |
|---|---|---|---|
| 4533 | 6551 | 3 | 2 |
| 4533 | 6553 | 6 | 2 |
| 4533 | 6555 | 3 | 1 |
| 4552 | 6408 | АДЖТДТАТ | 2 |

**Попытки.** Возвращены через штатный эндпоинт
`POST /api/v1/teacher/task-limits/override` (а не записью в таблицу мимо него) — чтобы
сработали аудит и уведомление ученику. Лимит поднят на число попыток, съеденных
дефектом: 6551 → 5, 6553 → 5, 6555 → 4 (ученик 4533); 6408 → 5, 6410 → 4, 6404 → 4
(ученик 4552). Шесть уведомлений «Учитель разблокировал задание» созданы.

В набор включены два случая, которых буквальная формулировка «за верные ответы» не
покрывает, — оба про попытку, потраченную не по вине ученика:
* **6410** — ответ `СР` был верен по тогдашнему условию (оно допускало два разбора);
  после переделки условия верен только `СЕД`, поэтому балл не возвращался, а попытка —
  да;
* **6404** — задание было нерешаемо вовсе, ученик физически не мог ответить верно.
  Ответ `ББА` неверен и по восстановленному условию, поэтому балла нет; условие теперь
  исправно, и у ученика 3 свободные попытки.

Проверено движком на проде (`GET /api/v1/learning/tasks/{id}/state`): 6551 — PASSED 2/5,
6553 — PASSED 2/5, 6555 — PASSED 1/4, 6408 — PASSED 2/5, 6404 — FAILED 1/4 (решаемо).

Контрольный запрос по всем 13 партиям `oge:reshu:*`: незачётов за ответ, совпадающий
с эталоном, больше нет — ноль строк.

## Кого коснулись незачёты (исходная картина)

| Ученик | Задание | Ответ | Незачётов | Статус |
|---|---|---|---|---|
| 4533 | 6551 | 3 | 2 | верно; ручной зачёт уже стоял (14:29) |
| 4533 | 6553 | 6 | 2 | верно; зачёта нет, попытки остались |
| 4533 | 6555 | 3 | 1 | верно; зачёта нет, попытки остались |
| 4552 | 6408 | АДЖТДТАТ | 2 | верно; зачёта нет |
| 4552 | 6410 | СР | 1 | было верно вторым разбором; условие переделано, теперь верен только СЕД |
| 4552 | 6404 | ББА | 1 | задание было нерешаемо; условие восстановлено, ответ БЫК |

Итого **9 незаслуженных незачётов у двух учеников**. Все закрыты: семь сдач зачтены,
по двум оставшимся (6410, 6404) возвращены попытки — см. раздел выше.

Отдельно: незачёты ученика 4533 на 6546 («12»), 6554/6569/6573 («6», уже после правки) —
честные ошибки, он выбирал не кратчайший путь. К дефектам отношения не имеют.
