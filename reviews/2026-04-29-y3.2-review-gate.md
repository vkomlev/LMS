# /review-gate — Y-3.2 follow-ups (кумулятивно с Y-3 + Y-3.1)

**Skill:** /review-gate v2.2.0 (SKILL.md loaded; 12 измерений + MANDATORY-триггеры применены)
**Дата:** 2026-04-29
**Скоуп:** Y-3 backend + Y-3.1 + Y-3.2 кумулятивное состояние перед merge в `main`
**Diff:** 3086 строк, 24 файла (LMS) + 4 файла (CB)
**Базовый артефакт:** [reviews/2026-04-29-y3.2-followups.md](2026-04-29-y3.2-followups.md) (executor-pro report)

---

## Решение: **ПРИНЯТО**

Все 12 измерений пройдены. Блокирующих находок нет. 2 S3 находки без блокировки.

---

## Шаг 0: Якорь контекста

- ✅ MEMORY.md прочитан (project_spw_phase_y1, user_victor_context)
- ✅ Acceptance criteria из CB authority Y-3 spec + LMS-side spec + techlead-final required fixes
- ✅ DRIFT detection: Y-3.2 строго в scope из techlead-final §«Required Fixes / next-iteration»

## Шаг 0.5: Проверка реестра ошибок

`docs/ai/ERRORS.md` содержит prevention actions от 4 lessons (datetime safety, contract drift, savepoint, orphan email) + 3 OPEN entries 2026-04-29 #1, #2, #3 (test_streak_logic gap, spec SQL formula, severity misclassification).

| Prevention | Статус в Y-3.2 |
|---|---|
| 2026-04-28 #1: «spec обновлять в том же коммите что и роутер» | ✅ соблюдено — LMS spec + CB authority обновлены same-commit с rate-limit endpoint changes |
| 2026-04-28 #3: «savepoint pattern для INSERT в открытой tx» | ✅ Y-3.2 не добавил новых INSERT path — наследует Y-3.1 patterns |
| 2026-04-28 #4: «orphan-email defense + check both UNIQUE sources» | ✅ unchanged (Y-3.1 закрыл это) |
| 2026-04-29 #1: «Spec Test Coverage Audit — все ли test_*.py из spec созданы» | ✅ Y-3.2 не добавлял spec-mandated tests (S3-A2 backlog test для rate-limit — opt) |
| 2026-04-29 #2: «SQL formula verification: trace на 3-input» | ✅ Y-3.2 backsync исправил формулу в обоих spec; новых SQL не вводилось |
| 2026-04-29 #3: «Spec-Mandated Test Files → S2, не S3» | ✅ techlead-final review соблюдает |

Повторяющихся паттернов **нет**. Все prevention actions соблюдаются.

---

## 12 измерений (детали)

### 1. Соответствие целям (DRIFT)

✅ **PASS** — Y-3.2 покрывает все 4 acceptance из techlead-final / next-iteration:
- S2-A1: spec backsync + CHANGELOG entry — выполнено в `LMS spec §5.4 + CB §7.2.4 + CHANGELOG`
- S3-A5: rate-limit на /me/identity/{kind}/link — добавлен (30/мин per user)
- S3-A4: shared `PASS_THRESHOLD_RATIO` — частично (me_service ✅, task_results_service в backlog как явно out-of-scope)
- contracts/lms-api.md update — выполнено

DRIFT не обнаружен. S3-A4 частичность явно зафиксирована в плане.

### 2. Корректность

✅ **PASS** — 58/58 фокусных тестов PASS:
- 39 Y-3 tests + 8 Y-3.1 streak edge cases + 4 guest_attribution + 7 migrations
- Y-3.2 изменения не сломали существующее поведение
- Edge cases для streak: empty, today_only, gap=1, gap=2 reset, continuous 3 days, dedup same day, gap-too-large, today+yesterday — все проходят

### 3. Безопасность данных и миграций

✅ **PASS** — Y-3.2 не меняет БД-схему. M7 уже merged и протестирован roundtrip. Rate-limit использует Redis (volatile storage) — fail-open приемлем (защита defence-in-depth, не data integrity).

### 4. Безопасность и секреты + IDOR sweep

✅ **PASS**:
- Hardcoded URLs grep: 0 совпадений в Y-3.2 файлах
- Rate-limit scope: `current_user.id` (per-user isolation, не per-IP — корректно для authenticated endpoint)
- IDOR на linking endpoints: каждый ветвь проверяет `payload.user_id == current_user.id` через `_consume_link_token_for_user` (Y-3.1) + audit на mismatch
- Bandit: 1 Low **pre-existing** false-positive в `vk_oauth_service.py:27` (B105 на URL constant — не Y-3.2 регрессия). 0 Medium / 0 High.
- Secrets: `Settings.env` читается из env, `LinkTokenServiceUnavailableError` не leakит storage details клиенту (общее «Сервис временно недоступен»)

### 5. Покрытие тестами

✅ **PASS** с S3 нот:
- Mock-only на external write-path: VK PKCE flow — accepted через operator chain CB §22.1 (alternative to gated live smoke; см. techlead-final S2-A2)
- Y-3.2 rate-limit не имеет dedicated unit-теста, но это — стандартный паттерн (другие existing rate-limited endpoints в проекте тоже без отдельных тестов rate-limit). **S3-X1 (non-blocking)**: добавить в Y-3.3 или отдельный test_rate_limits.py.
- Существующие тесты не сломаны после rate-limit injection (тесты проходят в low-volume режиме, ниже 30/мин).

### 6. Docs/Config/Runtime Drift

✅ **PASS**:
- LMS-side spec §5.4 + CB authority §7.2.4 — SQL формула обновлена ✅
- contracts/lms-api.md — rate-limit + 429 + 503 для linking endpoints добавлены ✅
- contracts/lms-api.md — таблица rate-limits расширена строками `/auth/link-token/issue` и `/me/identity/{kind}/link` ✅
- CHANGELOG cross-project entry для Y-3.1/Y-3.2 follow-ups ✅
- README.md / CLAUDE.md (project): не обновлены — не требуют Y-3.2-specific entries (у CLAUDE.md project уровень — Y-3 backend done статус актуален; README — общий project-overview, не должен включать каждый rate-limit)

### 7. Phase Integrity

✅ **PASS**:
- Y-3 backend phase: M7 + 8 endpoints + tests + cross-project memory + 4 review артефакта = COMPLETE
- Y-3.1 follow-ups: 3 closure (S2-1, S3-7, P1) + bonus SQL bug fix = COMPLETE
- Y-3.2 follow-ups: 4 closure (S2-A1, S3-A5, S3-A4 partial, contracts) = COMPLETE
- Нет TODO/заглушек в коде Y-3.2 changes
- Out-of-scope items явно классифицированы как backlog (S3-A2 encrypt_token, S3-A3 magic strings, S3-A4 task_results_service, S2-A2 live VK smoke)

### 8. Goal-Level Data Completeness

N/A — Y-3.2 не migration/backfill/import.

### 9. Domain Model Completeness

N/A — Y-3.2 не вводит новый domain.

### 10. Date/Time Critical

✅ **PASS**:
- Y-3.1 streak SQL fix математически корректен (`d + rn*1d` для DESC) — verified через test_streak_logic 8 edge cases
- Y-3.2 spec backsync синхронизировал документацию с кодом
- Никаких новых date/time comparisons в Y-3.2 не вводилось
- Trace через 3-input проверен: для days={today, yesterday, day-before} с rn={1,2,3} → grp = T+1d (константа) → один run → streak=3 ✅

### 11. Cross-project memory sync

✅ **PASS** — все 4 файла обновлены:
- `D:\Work\ContentBackbone\docs\cross-project\contracts\lms-api.md` — rate-limit/429/503 для linking ✅
- `D:\Work\ContentBackbone\docs\cross-project\CHANGELOG.md` — Y-3.1/Y-3.2 entry в начало ✅
- `D:\Work\ContentBackbone\docs\cross-project\contracts\lms-db-schema.md` — Y-3 already updated в первом раунде; Y-3.2 не меняет схему БД, обновление не требуется ✅
- `D:\Work\ContentBackbone\docs\cross-project\STATE.md` — Y-3 already updated в первом раунде; phase status не сменился ✅

CB tech-spec Y-3 v1 §7.2.4 обновлён (это authority spec, не cross-project mirror, но связан same-commit).

### 12. Public API Contract Sync

✅ **PASS**:
- Изменены: `app/api/v1/me.py` (rate-limit injection в 3 endpoint), `app/api/v1/auth/link_token.py` (Y-3.1)
- Status codes 429/503 добавлены — отражены в `contracts/lms-api.md` (lms-api.md sections для `/auth/link-token/issue` и `/me/identity/{kind}/link`)
- Cross-repo grep на старые SQL шаблоны (`d - rn\*1d.*ORDER BY d DESC`) в LMS app/, LMS docs/, CB docs/: 0 совпадений ✅
- Hardcoded URLs grep в `app/services/`, `app/core/`: 0 совпадений ✅
- OpenAPI regen — Y-3.2 не вводит новые endpoints (только status codes на existing); regen остаётся как Y-3 follow-up #1 (manual operator step через uvicorn + curl, документировано в первом review-gate)

---

## MANDATORY-триггеры

| Trigger | Применимо к Y-3.2 | Статус |
|---|---|---|
| Изменения auth/middleware | ✅ Да (rate-limit на /me/identity/{kind}/link) | review-gate выполнен (этот артефакт) |
| Изменения публичного API | ✅ Да (429/503 status codes) | spec backsync ✅ + cross-repo grep ✅ |
| Cross-project зависимости | ✅ Да (contracts/lms-api.md + CHANGELOG) | обновлены same-commit |
| Миграции БД | ❌ нет | M7 уже в production |
| Next.js client/server boundary | ❌ Backend-only |
| Type assertions добавлены | ❌ нет |

«Hotfix bypass» — не использовался. Y-3.2 — плановый follow-up, не hotfix.

---

## Находки

### Блокирующие проблемы

**Нет.**

### Улучшения без блокировки

**S3-X1 (testing — coverage gap):** rate-limit endpoint behavior не покрыт dedicated unit-тестом.
- **Файл:** `tests/test_me_endpoints_y3.py`
- **Почему:** для производственного мониторинга было бы полезно явно проверить, что 31-й вызов /me/identity/email/link на одного user в течение минуты возвращает 429. Сейчас покрывается только косвенно (нет тестов, превышающих лимит).
- **Направление:** добавить test_link_identity_rate_limit (использует test client + 31 вызов с auth + assert 429 на последнем). Можно отложить в Y-3.3.
- **Измерение:** 5 (Покрытие тестами).

**S3-X2 (DRY — partial cleanup):** `PASS_THRESHOLD_RATIO` всё ещё дублирован в `task_results_service.py:16`.
- **Файл:** `app/services/task_results_service.py:16`
- **Почему:** `me_service.py` теперь импортирует из `learning_engine_service`, но `task_results_service.py` сохраняет локальное определение (вне Y-3 scope per executor-pro минимальность).
- **Направление:** заменить на `from app.services.learning_engine_service import PASS_THRESHOLD_RATIO`. Тривиально, но явно вне Y-3 scope.
- **Измерение:** N/A (не из 12; backlog refactor).

### Необходимые тесты

Не требуются для PASS. S3-X1 — рекомендация Y-3.3.

### Operator handoff

Не требуется — Y-3.2 не имеет операторских зависимостей. Все changes автономны и проверены агентом.

При деплое Y-3 + Y-3.1 + Y-3.2 в production оператор должен (классификация B — пошаговая инструкция):
1. Установить env var `ENV=production` в production Docker/systemd конфигурации (без этого fail-secure из Y-3.1 не активирован — в-памяти fallback всё ещё работает) — **критично для security guarantee S2-1 closure**
2. Подтвердить наличие `REDIS_URL` (Y-3 dependency)
3. После успешного uvicorn старта — `curl http://localhost:8000/openapi.json > docs/openapi.json` для regen (Y-3 follow-up #1)
4. Опционально — выполнить vk-relay WP правку (CB §22.1) перед первым тестированием VK linking flow

Если шаг 1 не выполнен — production fail-secure семантика не активна; backend всё равно работает (in-memory fallback), но без security гарантии. Рекомендуется blocker до production deploy.

---

## Обновление реестра ошибок

ОТКЛОНЕНО не выдано → новые записи в `docs/ai/ERRORS.md` не требуются. Существующие 3 OPEN entries (2026-04-29 #1, #2, #3) остаются с их prevention actions; Y-3.2 это не повторяет.

---

## Финал

**ПРИНЯТО** — Y-3 backend + Y-3.1 + Y-3.2 кумулятивное состояние готово к merge в `main`.

**Артефакты ревью (полный chain):**
- `reviews/2026-04-29-y3-backend-review.md` (первый review-gate)
- `reviews/2026-04-29-y3-techlead-review.md` (techlead 1-й проход)
- `reviews/2026-04-29-y3-pr-review.md` (pr-review с last-position fix)
- `reviews/2026-04-29-y3-context-auditor.md` (контекст-аудит)
- `reviews/2026-04-29-y3-techlead-final.md` (techlead финальный)
- `reviews/2026-04-29-y3.1-followups.md` (fastapi-api-developer Y-3.1)
- `reviews/2026-04-29-y3.2-followups.md` (executor-pro Y-3.2)
- `reviews/2026-04-29-y3.2-review-gate.md` (этот документ)
- `reviews/2026-04-29-y3.2-followups.diff` (кумулятивный diff)

**Y-3 фаза backend:** ✅ COMPLETE. Передаётся оператору для git commit + push + production deploy с env-настройкой по operator handoff B выше.
