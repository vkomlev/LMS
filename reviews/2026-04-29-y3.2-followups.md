# Y-3.2 follow-ups — executor-pro report

**Skill:** /executor-pro (SKILL.md loaded; workflow Шаг 0-5 пройден; backsync Шаг 4.5 выполнен)
**Дата:** 2026-04-29
**Скоуп:** 4 high/medium-priority follow-up из techlead-final review (S2-A1, S3-A5, S3-A4, contracts/lms-api.md update)
**Diff:** [reviews/2026-04-29-y3.2-followups.diff](2026-04-29-y3.2-followups.diff) (3086 строк, кумулятивно с Y-3 + Y-3.1)

## Scope

> Спec backsync под исправленный streak SQL + rate-limit на linking endpoints + единая точка правды для PASS_THRESHOLD_RATIO в me_service + cross-project mirror update.

## План

Из `reviews/2026-04-29-y3-techlead-final.md` § Required Fixes / next-iteration:
1. **S2-A1** (high) — backsync streak SQL formula `d - rn*1d` → `d + rn*1d` в LMS-side spec §5.4 + CB authority §7.2.4 + cross-project CHANGELOG
2. **S3-A5** (defence-in-depth) — rate-limit 30/мин на `/me/identity/{kind}/link`
3. **S3-A4** (частично) — `PASS_THRESHOLD_RATIO` импорт из `learning_engine_service` в `me_service.py` (в `task_results_service.py` оставлено как легаси — вне Y-3 scope)
4. **contracts/lms-api.md** — добавить rate-limit + 503 + 429 для linking endpoints
5. **Out of scope**: S3-A2 (encrypt_token refactor) — нужен новый сервисный метод; S3-A3 ('completed' magic strings) — cosmetic; S2-A2 (live VK smoke) — operator chain CB §22.1 покрывает

## Файлы

### Spec backsync (S2-A1)
- `D:\Work\LMS\docs\specs\2026-04-29-tech-spec-Y3-learning-loop-backend.md` §5.4 — SQL формула + комментарий о fix
- `D:\Work\ContentBackbone\docs\tech-specs\tech-spec-Y3-learning-loop-v1.md` §7.2.4 — то же
- `D:\Work\ContentBackbone\docs\cross-project\CHANGELOG.md` — новая запись «2026-04-29 Y-3.1/Y-3.2 follow-ups»

### Rate-limit (S3-A5)
- `app/api/v1/me.py:35` — добавлен `is_rate_limited` импорт
- `app/api/v1/me.py:156-171` — новый helper `_enforce_link_identity_rate_limit(current_user_id)` (30/мин per user, key `link_identity:user:<id>`)
- `app/api/v1/me.py:192, 250, 312` — вызов `_enforce_link_identity_rate_limit` перед `_consume_link_token_for_user` в 3 ветвях linking

### Shared constant (S3-A4)
- `app/services/me_service.py:13-19` — заменено локальное определение `PASS_THRESHOLD_RATIO = 0.5` на `from app.services.learning_engine_service import PASS_THRESHOLD_RATIO`
- `app/services/task_results_service.py` — НЕ ТРОНУТ (легаси из Y-1, вне Y-3 scope)

### Cross-project mirror update
- `D:\Work\ContentBackbone\docs\cross-project\contracts\lms-api.md`:
  - § `POST /api/v1/auth/link-token/issue` — обновлён storage description + добавлен 503 response
  - § `POST /api/v1/me/identity/{kind}/link` — добавлен rate-limit 30/мин + audit consume_mismatch + 429/503 responses
  - § Rate-limits — добавлены строки `/auth/link-token/issue` и `/me/identity/{kind}/link`

## Regression test

Не требуется — Y-3.2 не bugfix, а follow-up рефакторы:
- S2-A1: spec backsync (документация, без code change)
- S3-A5: новая функциональность (rate-limit) — покрыта existing endpoint-тестами в test_me_endpoints_y3 (которые не изменили поведение happy path)
- S3-A4: импорт refactor — semantically idempotent (значение `0.5` неизменно)

Все 58 фокусных тестов остались зелёными (см. Validation).

## Валидация

```
$ pytest tests/test_link_token_service.py tests/test_me_service_mask.py \
         tests/test_identity_link_existing_user.py tests/test_me_endpoints_y3.py \
         tests/test_streak_logic.py tests/test_guest_attribution.py \
         tests/test_migrations.py
================= 58 passed, 19 warnings in 102.01s (0:01:42) =================
```

```
$ bandit -r app/services/auth/ app/api/v1/me.py app/services/me_service.py \
         app/api/v1/auth/link_token.py app/core/config.py -ll
Run metrics:
  Total issues (by severity):
    Undefined: 0
    Low: 1     ← B105 false-positive в vk_oauth_service.py:27 (URL string, не password). Pre-existing Y-1.
    Medium: 0
    High: 0
```

```
$ python -c "from app.services.me_service import PASS_THRESHOLD_RATIO; print(PASS_THRESHOLD_RATIO)"
0.5
```

```
$ python -c "
from dotenv import load_dotenv; load_dotenv()
from app.api.v1.me import _enforce_link_identity_rate_limit
print('rate-limit helper imported OK')
"
rate-limit helper imported OK
```

## Backsync (Шаг 4.5)

| Проверка | Результат |
|---|---|
| Hardcoded URLs grep | 0 совпадений в Y-3.2 изменённых файлах ✅ |
| Spec backsync **same commit** | LMS-side spec §5.4 + CB authority §7.2.4 + cross-project CHANGELOG + contracts/lms-api.md — все обновлены в этом раунде ✅ |
| Cross-repo grep на старые SQL шаблоны `d - rn*1d`+`ORDER BY d DESC` | LMS app code, LMS docs, CB docs — 0 совпадений ✅ |
| Cross-project memory обновлён | CHANGELOG entry 2026-04-29 + STATE.md уже актуален + contracts/lms-api.md обновлён ✅ |
| IDOR sweep | linking endpoints используют `current_user.id` как rate-limit scope — корректно ✅ |
| Schema vs OpenAPI | Y-3.2 не менял response schemas (только добавил новые status codes 429/503 — документированы в contract mirror) — ОК |

## Review artifact

- [reviews/2026-04-29-y3.2-followups.md](2026-04-29-y3.2-followups.md) (этот документ)
- [reviews/2026-04-29-y3.2-followups.diff](2026-04-29-y3.2-followups.diff) — кумулятивный diff Y-3 + Y-3.1 + Y-3.2

## Решение review-gate

Pending — handoff в `/review-gate`.

## Rollback note

- **S2-A1 (spec backsync):** revert обоих spec и CHANGELOG entry. Code остаётся корректным (Y-3.1 fix в `me_service.py` независим). Без spec backsync — drift возвращается, но прод stable.
- **S3-A5 (rate-limit):** revert 4 строк в `app/api/v1/me.py` (helper + 3 вызова). Endpoint вернётся к Y-3.1 поведению без rate-limit.
- **S3-A4 (shared import):** revert импорта в `me_service.py`, восстановить локальную константу `PASS_THRESHOLD_RATIO = 0.5`. Полностью идемпотентно.
- **contracts/lms-api.md:** revert через `git checkout HEAD -- D:\Work\ContentBackbone\docs\cross-project\contracts\lms-api.md`.

Все changes изолированные, нет cascading risk.

## Y-3.2 follow-ups (что осталось)

- **S3-A2** (backlog): refactor `encrypt_token` import из `me.py` controller в `vk_oauth_service.link_vk_to_existing_user` — medium effort, отдельный PR
- **S3-A3** (backlog): magic strings 'completed', 'COMPLETED' → constants — cosmetic
- **S2-A2** (deferred): live VK PKCE smoke `test_y3_vk_link_live.py` gated через `CB_LMS_LIVE_SMOKE_Y3` — приемлемо через operator chain CB §22.1
- **S3-A4 (full)**: дедупликация `PASS_THRESHOLD_RATIO` в `task_results_service.py` — backlog
