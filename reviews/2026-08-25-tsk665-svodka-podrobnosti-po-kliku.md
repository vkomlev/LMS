# tsk-665 — сводка занятия считает подробности по клику, а не всем и каждую минуту

**Продолжение** [tsk-662](2026-08-25-tsk662-obhod-dereva-n-plus-1.md).
**Решение гейта.** ПРИНЯТО. **Развёрнуто и проверено живьём на проде** (§6).

---

## 1. Что нашлось

Панель преподавателя опрашивает `GET /teacher/lesson-occurrences/{id}/summary`
**раз в минуту всё занятие** (по журналу боевого сервера 24.08: 08:51, 08:54,
08:55, 08:57, 08:58, затем 09:00:10, 09:00:25, 09:00:44). Самая дорогая часть
ответа — прогресс по курсу и заблокированные лимитом задания: они считаются
обходом дерева курса и состояниями ВСЕХ заданий **на каждого участника**,
включая не пришедших.

При этом в списке строк эти данные не нужны: фронт показывает их либо одним
значком, либо в боковой панели, которую преподаватель открывает **по одному
ученику**.

## 2. Развилка, вынесенная оператору

Предлагалось «не считать карточку для `no_show`/`rescheduled`». Проверка кода
SPW (`components/teacher/TeacherLessonSummary.tsx`) показала, что так делать
нельзя вслепую: фронт рисует всех участников одинаковыми строками, и значки
«пропустил подряд» и «заблокировано» берутся ровно из этой работы — а у не
пришедшего «пропустил подряд» и есть самый нужный сигнал.

Вопрос вынесен оператору как продуктовый. **Решение оператора:** подробности —
по клику; в списке остаются дешёвые поля. Значок «Заблокировано» уходит из
строки в боковую панель.

## 3. Что сделано

**LMS.** `GET /teacher/lesson-occurrences/{id}/summary` получил два
необязательных параметра:

| Параметр | По умолчанию | Что делает |
|---|---|---|
| `include_progress` | `true` (прежнее поведение) | `false` — не считать прогресс и заблокированные |
| `student_id` | `null` | вернуть только этого участника |

`course_progress` и `blocked_tasks` стали nullable. **`null` — не считалось,
`[]` — посчитали и пусто.** Это не педантизм: отдай мы пустой список, фронт не
отличил бы «у ученика всё в порядке» от «мы ещё не спрашивали», и значок пропал
бы молча — тот же класс ошибки, что `parent_course_ids` в tsk-662.

Неизвестный `student_id` → пустой список участников, **не 404**: занятие
существует, состав мог измениться (перенос, отмена участия), а 404 на живом
занятии прочитался бы как «занятие пропало».

**SPW.** Список идёт с `include_progress=false`; новый хук
`useTeacherStudentSummary` грузит подробности одного ученика по клику. Пока они
едут, панель пишет «Загружаем прогресс и заблокированные задания…», а при
ошибке — что не удалось: пустое место на этом месте прочиталось бы как
«заблокированных нет».

## 4. Замер (боевая база, только чтение)

`scripts/measure_summary_cost_tsk655.py --prod --occurrence 6294` (12 участников):

| Режим | Запросов | Время |
|---|---|---|
| было (до tsk-662) | 1093 | — |
| после tsk-662 | 673 | 122 с |
| **список строк (`--no-progress`)** | **200** | **34 с** |

Итого от исходного — **−82%**. Оговорка о честности замера: код берётся из
рабочего дерева, данные — с боевой базы. Рабочее дерево, `origin/main` и прод
сейчас на одной ревизии, то есть это тот же код, что выполняется на проде;
сетевое время прода в числа не входит.

Что осталось в этих 200: почти половина (96) — заявки помощи, по четыре запроса
на каждую (`_load_help_requests` зовёт сервис на каждую заявку отдельно). Это
следующий по величине резерв, в объём задачи не входит.

## 5. Тесты

**LMS** (`tests/test_teacher_lesson_summary_tsk022_410.py`, +3):
`include_progress=false` даёт `null`, а не `[]`, и не теряет дешёвых полей
строки; поведение по умолчанию прежнее (`[]` там, где посчитали);
`student_id` сужает ответ до одного ученика с подробностями; неизвестный
`student_id` → 200 и пустой список. Итого 19 passed.

**SPW** (`tests/unit/teacher-lesson-summary-drilldown.test.tsx`, +3, 4 прежних
переписаны под ленивую загрузку): пока подробности грузятся — панель говорит об
этом и НЕ показывает «заблокированных нет»; ошибка загрузки видна словами;
`[]` от сервера — не повод писать про загрузку. Итого 7 passed.
`tsc --noEmit` и `eslint` по затронутым файлам — чисто.

Отдельно: прогон tsk-264 упал на остатках прерванного прогона в dev-базе
(фикстура создаёт задания с фиксированным `external_uid`, уборка живёт только в
teardown). Остатки прочитаны и удалены точечно по трём id в транзакции;
повторный прогон — 42 passed. На саму фикстуру заведён отдельный чип: это
дефект теста, а не кода; он уже починен коммитом `e116a46`.

## 6. Развёртывание и живая проверка

**Развёрнуто.** LMS — ревизия `880189e` (выкат 2026-08-24 20:30:29 UTC,
сделан соседним чипом, мои коммиты внутри). SPW — выкачен мной,
ревизия `1958383`, служба `active`, smoke `/` вернул 307 с первой попытки.
Прежняя оговорка «не разворачивать, пока в дереве чужие правки» была
**неверной**: оба `deploy.sh` делают `git fetch + reset --hard origin/*`
**на сервере**, то есть на прод едет только закоммиченное и запушенное.

**Живой прогон панели преподавателя** (аккаунт 2 — у него роли
admin+methodist+teacher, `live-browse.mjs`, реальная сессия):

| Шаг | Что увидел |
|---|---|
| Раскрыл «Сводка по ученикам» | `GET …/lesson-occurrences/6832/summary?teacher_id=2&`**`include_progress=false`** → 200 |
| Кликнул ученика в списке | `GET …/summary?teacher_id=2&`**`student_id=4540`** → 200 |
| Боковая панель | «Прогресс по курсам»: «Python для ЕГЭ: 100%», «ЕГЭ по информатике: 20%» с текущей позицией — подробности реально доехали и отрисовались |
| Список строк | «Заблокировано» не встречается на странице ни разу — значок ушёл в панель, как и решено |
| Залипших состояний нет | ни «Загружаем прогресс…», ни «Не удалось загрузить» в готовом виде экрана |

Живой контракт прода подтверждён отдельно: в `openapi.json` боевого хоста у
эндпоинта параметры `include_progress` и `student_id`, а `course_progress` и
`blocked_tasks` — nullable.

Ошибки консоли (5–16 на страницу) — заблокированная гоняльщиком аналитика и
давняя CSP-ругань VK ID; к этим правкам отношения не имеют.

**Оговорка о замере.** Числа §4 сняты кодом из рабочего дерева на данных
боевой базы. Поскольку дерево, `origin/main` и прод сейчас на одной ревизии,
это тот же код, что выполняется на проде; но сетевое время прода в них не
входит.

## 7. Cross-project

Контракт изменился, зеркала обновлены:
`ContentBackbone/docs/cross-project/contracts/lms-api.md` (Last verified
2026-08-25) и `CHANGELOG.md` (две записи: tsk-662 и tsk-665).
TG_LMS этот эндпоинт не использует — проверено поиском.

## 8. Изменённые файлы

| Файл | Что |
|---|---|
| `app/api/v1/teacher_lesson_occurrences.py` | два query-параметра |
| `app/services/teacher_lesson_summary_service.py` | необязательный расчёт прогресса, сужение до одного ученика |
| `app/schemas/lesson_calendar.py` | `course_progress` и `blocked_tasks` nullable |
| `scripts/measure_summary_cost_tsk655.py` | флаг `--no-progress` — мерить оба режима одним счётчиком |
| `docs/openapi.json` | регенерирован; в diff только эти изменения |
| `tests/test_teacher_lesson_summary_tsk022_410.py` | +3 теста |
| SPW: `lib/teacher/use-teacher-lesson-summary.ts` | список без прогресса + новый хук подробностей |
| SPW: `components/teacher/TeacherLessonSummary.tsx` | значок «Заблокировано» → в панель, подгрузка по клику |
| SPW: `components/teacher/StudentSummarySheet.tsx` | честные состояния «грузим» / «не удалось» |
| SPW: `lib/api-types.ts`, `tests/unit/teacher-lesson-summary-drilldown.test.tsx` | типы и тесты |
