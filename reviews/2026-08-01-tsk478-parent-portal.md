# Review-gate: tsk-478 — кабинет родителя (доступ + UI)

**Решение: ПРИНЯТО**

## Контекст

Зависимая от [[tsk-494]] (закрыта, задеплоена, живьём проверена). Спека:
`docs/specs/2026-08-01-spec-tsk478-parent-portal.md` (`/spec-writer`).
Реализация — LMS (`/fastapi-api-developer`) + SPW (`/executor-pro`), один
сквозной проход, без промежуточных отдельных `/change-plan-architect`/
`/tech-spec-composer` (спека уже содержала файловый план и разведку — решение
задокументировано в `.skill-engaged-note.md` обоих репозиториев).

## Изменения

**LMS:** миграция роли `parent` + `parent_student_links`; репо/сервис/API
связки (методист/админ-only запись); `GET /me/children` (self-service);
композитный ACL-гейт `GET /students/{id}/dashboard` (`can_edit_progress` ИЛИ
роль `parent` + `is_linked`). 8 новых тестов.

**SPW:** роут-группа `app/(parent)/parent/*`, серверный role-guard, минимальная
шапка, read-only страница-дашборд, переключатель в `AppHeader`. 3 новых теста
(layout guard).

## Проверка по 12 измерениям

1. **Соответствие целям** — все критерии готовности спеки покрыты: миграция
   ревёрсибельна, связка только оператору, IDOR-гейт дашборда, self-service
   `/me/children`, SPW-зона с guard'ом, read-only (ни одной кнопки действия
   в UI, ни одного write-эндпоинта под ролью `parent`). DRIFT нет.
2. **Корректность** — композитный гейт (`_ensure_dashboard_access`) читается
   явно: `can_edit_progress` → рано return; иначе — только при роли `parent`
   И существующей связке. 403 с единым текстом (не раскрывает, какая ветка
   могла бы сработать).
3. **Безопасность данных/миграций** — `parent_student_links` создаётся/
   удаляется транзакционно (Alembic DDL). `roles.id` НЕ serial/identity в
   этой БД (подтверждено на dev И на проде) — первая версия миграции
   содержала реальный баг (`WHERE NOT EXISTS` в SELECT-агрегате не гейтит
   INSERT целиком при пустом входе агрегации: `MAX()` над нулём строк даёт
   NULL→COALESCE 0+1=1→PK-конфликт с существующим id=1 при повторном
   upgrade). Найдено и исправлено round-trip тестированием
   upgrade→downgrade→upgrade (дважды подряд, дочиста) — не доверием к
   первому success. Финальная версия: `VALUES (subquery) ON CONFLICT (name)
   DO NOTHING` — идемпотентна, проверено 3 цикла подряд.
4. **Безопасность/IDOR** — параноидальная проверка `_ensure_dashboard_access`:
   `is_linked(parent_id=current_user.id, student_id=target)` — параметризованный
   SQL, точная пара. Тест `test_parent_cannot_see_unlinked_student_dashboard`
   подтверждает 403 для чужого ученика. `can_edit_progress`/
   `ensure_can_edit_progress` (`manual_progress_service.py`) НЕ изменены —
   родительская логика целиком в новом composite-гейте ОДНОГО роута, никакой
   другой write-эндпоинт её не наследует (grep подтверждает: `is_linked`
   используется только в `student_dashboard.py`).
5. **Тесты** — 8 backend (LMS) + 3 frontend (SPW layout guard) новых, все на
   реальной БД/реальном рендере (не моки бизнес-логики). Полный прогон: LMS
   1319 passed/11 skipped/0 failed; SPW 802 passed (1 ПРЕДСУЩЕСТВУЮЩИЙ
   unhandled error в `methodist-schedule.test.tsx`, не связан с этой задачей
   — не трогали `SlotDetail.tsx`). `type-check`/`lint`/`build` SPW чистые.
6. **Docs/Config drift** — спека, `.skill-engaged-note.md` обоих репозиториев
   актуальны. `docs/openapi.json` регенерирован в этом коммите (220
   endpoints). `lib/api-types.ts` перегенерирован из свежего openapi.json.
7. **Phase integrity** — staged-набор ОБОИХ репозиториев сверен `git status`
   построчно с этим списком файлов — только tsk-478. Побочный шум:
   `tsconfig.json` в SPW автоматически переписан Next.js при `npm run build`
   (форматирование + smoketest-типы) — НЕ семантическое изменение, НЕ
   коммитится (исключён из pathspec).
8-9. Не применимо (нет доменных справочников/классификаторов).
10. **Date/Time Critical** — не затронуто этой задачей (дата/время дашборда
    уже покрыто tsk-494; эта задача только про ACL/UI).
11. **Cross-project memory** — ЕЩЁ НЕ обновлены на момент этого ревью
    (сознательно отложено на шаг после PASS, как и с tsk-494) — обязательный
    шаг перед закрытием задачи, не блокер самого ревью.
12. **Public API Contract Sync** — три новых эндпоинта + расширение гейта
    существующего — задокументированы в `contracts/lms-api.md` СРАЗУ ПОСЛЕ
    этого PASS (тот же порядок, что tsk-494); hardcoded URL — 0 совпадений
    (новые файлы не содержат абсолютных URL).

## Блокирующие проблемы
Нет.

## Улучшения без блокировки

- `/me/children` возвращает `list[UserRead]` без явного порядка — для v1
  (один ребёнок) не важно, но при будущей многодетности стоит завести
  `ORDER BY parent_student_links.linked_at` в репозитории для
  детерминированного порядка выбора "первого" ребёнка на фронте.
- SPW `children.data?.[0]` — осознанное, не заблокированное решение под
  многодетность (виджет переключателя добавится позже без переписывания
  структуры компонента, `child` уже единая точка данных).

## Гочта auto-assign роли `student` — подтверждено безопасной

Родитель, входящий впервые по email/TG, получает роль `student` от
существующего self-heal (`ensure_student_role`, вне рамок этой задачи) ДО
того, как оператор создаст связку и добавит роль `parent`. Роль `student` —
самая слабая в системе (доступ только к собственным данным вызывающего,
никаких чужих) — присутствие этой роли у родителя не создаёт повышенных
прав, только потенциальную UX-путаницу (пустой "мой прогресс"), задокументировано
как операционная инструкция оператору, не баг.

## DB Findings

Round-trip `alembic upgrade head` → `downgrade -1` → `upgrade head` (×2 цикла
подряд) на dev — чисто. Прод НЕ трогали (запись только через `/db-check`-
подобный протокол деплоя, см. следующий шаг).

## Necessary tests
Покрыто. Дополнительных тестов для PASS не требуется.

## Operator handoff
Категория А — review-gate ПРИНЯТО → коммит/пуш сам (оба репозитория), деплой
обоих (LMS backend + SPW frontend) и живая проверка в браузере — следующий
шаг этой же сессии (durable-авторизация tsk-359).
