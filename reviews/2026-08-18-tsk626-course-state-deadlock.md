# tsk-626 — взаимоблокировка на кеше состояний курсов

**Дата:** 2026-08-18
**Проект:** LMS Core API
**Класс:** отказ на проде (500 ученику), устраняется причина, а не ответ
**Ветка:** `worktree-tsk-626-deadlock`

---

## 1. Что случилось

17.08.2026 в 12:56:38 UTC на проде `GET /api/v1/learning/next-item` упал с
`asyncpg.exceptions.DeadlockDetectedError`. Ученик получил 500.

```
Process 1599269 waits for ShareLock on transaction 832460; blocked by process 1599234.
Process 1599234 waits for ShareLock on transaction 832461; blocked by process 1599269.
[SQL: INSERT INTO student_course_state (student_id, course_id, state, updated_at)
      VALUES ($1,$2,$3, now())
      ON CONFLICT (student_id, course_id) DO UPDATE SET state = EXCLUDED.state, updated_at = now()]
[parameters: (3, 1455, 'NOT_STARTED')]
```

Это отдельный класс от перегрузки пула (tsk-624): под обработчик 503 он не
попадает и до правки отдавал голую 500.

---

## 2. Что показала боевая база (read-only, MCP)

Запрос по строкам кеша ученика 3 и по всей таблице:

| Замер | Значение |
|---|---|
| Всего строк `student_course_state` | 536 (48 учеников, 54 курса) |
| Строк у ученика 3 | 26 |
| Из них с ОДНИМ `updated_at` (2026-08-17T18:40:00.913Z) | 25 |
| Самая крупная группа по одному `updated_at` во всей таблице | **460 строк, 40 учеников** |

`now()` в PostgreSQL — время **начала транзакции**. Значит 460 строк по 40
ученикам записаны ОДНОЙ транзакцией, и она держала все 460 блокировок строк до
самого конца прохода. Это фоновый тик `course_dependency_state_cron_service`
(tsk-541) — вторая сторона аварии.

Порядок захвата у него был вдобавок невоспроизводимым: внешний цикл шёл по
курсам, внутренний — по `Set[int]` учеников, то есть в порядке хеша. Согласовать
такой порядок с порядком любого другого писателя нельзя в принципе.

Первая сторона — сам `next-item`: он пишет кеш узла текущей позиции
(`learning_engine_service.py:968`), затем кеш каждой зависимости корня
(`:997`). Набор и порядок зависят от позиции ученика, то есть у двух
параллельных запросов одного ученика они разные.

**Итог разбора:** порядок захвата строк не согласован ни у одного писателя.
Сортировка самих строк проблему не закрывает — набор курсов у писателей разный
и заранее неизвестен (узел позиции резолвится по ходу обхода).

---

## 3. Решение

**Правило одно на всех: любой писатель `student_course_state` до первой записи
берёт транзакционную блокировку ученика.** Порядок задаётся не строками, а
одним ключом — учеником; цикл ожидания собирать не из чего.

| Файл | Правка |
|---|---|
| `app/services/learning_engine_service.py` | `lock_course_state()` + `upsert_course_state()` — единственная точка записи в таблицу. `compute_course_state` пишет только через неё. `backfill_dependency_state` обходит учеников по возрастанию `student_id`. |
| `app/services/course_dependency_state_cron_service.py` | Проход раскладывается по ученикам и идёт по возрастанию `student_id`, коммит — **после каждого ученика**, а не один на весь тик. Блокировка одного worker'а переведена с транзакционной на сессионную (транзакционная слетела бы на первом коммите) и снимается в `finally`. |
| `app/services/user_merge_service.py` | Слияние удаляет строки кеша source — тоже писатель, берёт ту же блокировку (обе учётки, по возрастанию id). |
| `app/api/error_handlers.py` | `is_deadlock_error()` — распознавание по SQLSTATE `40P01`, не по тексту и не по имени класса драйвера. |
| `app/api/v1/learning.py` | `_resolve_next_item_with_retry` — одна повторная попытка `next-item` после взаимоблокировки. |

Схема БД не менялась, миграция не нужна.

### Почему многоучениковые писатели сортируют учеников

Блокировка транзакционная, значит транзакция, пишущая нескольких учеников,
копит их блокировки. Два таких писателя во встречном порядке замкнули бы круг
уже на самих блокировках. Возрастание `student_id` — общий порядок для тика и
бэкфилла.

### Почему тик коммитит по ученику

Прямое следствие замера: одна транзакция на весь проход держала 460 строк
несколько секунд. Теперь блокировки живут ровно столько, сколько считается один
ученик, и параллельный `next-item` ждёт миллисекунды вместо всего тика.

---

## 4. Что показывать ученику, если взаимоблокировка всё же случится (пункт 5 декомпозиции)

**Решение: одна повторная попытка внутри `next-item`; при повторном отказе —
прежняя 500, без маскировки под 503.**

Обоснование:

1. **Повтор здесь безопасен, а не «попробуем и посмотрим».** PostgreSQL снимает
   транзакцию-жертву ЦЕЛИКОМ. За снятой стороной не остаётся ничего: записи
   кеша откачены, уведомление методисту (`escalate_course_completion`) — это
   строки `inbox`/`task_results.metrics` в той же транзакции, наружу за время
   запроса не уходит ни одного вызова. Повтор — буквально то же самое с чистого
   листа.
2. **Одна попытка, а не три.** Основное лечение — блокировка ученика; после неё
   этот класс не собирается вовсе. Повтор оставлен сеткой на случай ДРУГОГО, ещё
   не известного порядка захвата. Такой случай вторая и третья попытки уже не
   спасли бы — только удлинили бы ожидание ученика.
3. **Только `next-item`, не общий обработчик.** Общий повтор задваивал бы
   отправку там, где транзакция успела отправить что-то наружу.
4. **Не 503.** 503 сказал бы «сервис занят, приди позже» — неправда: сервис
   свободен, это дефект порядка блокировок. Замаскировав его, мы потеряли бы
   сигнал о новом лок-ордер баге. Оставшаяся 500 — честный громкий отказ.
5. **Пережитая взаимоблокировка видна в логе.** Повтор пишет `WARNING` с именем
   класса исключения, поэтому `grep -c DeadlockDetected /opt/lms/logs/app.log`
   считает и её тоже. Молчаливого «само прошло» не будет.

---

## 5. Проверка

### Тест на воспроизведение против настоящего PostgreSQL

`tests/test_tsk626_course_state_deadlock.py` (вне общей откатываемой
транзакции — взаимоблокировку на одном соединении не воспроизвести).

Сцена одна и та же в обоих ключевых тестах, меняется только способ записи:
две транзакции пишут пересекающиеся курсы одного ученика в обратном порядке,
расстановка односторонняя и потому детерминированная.

| Тест | Что доказывает | Итог |
|---|---|---|
| `test_raw_upsert_deadlocks_on_reversed_course_order` | Контроль: голый upsert (код до правки) на этой сцене ДАЁТ `40P01`. Без него зелёный тест правки ничего не значил бы | PASS |
| `test_locked_upsert_survives_reversed_course_order` | Та же сцена через `upsert_course_state` проходит целиком, обе строки кеша на месте | PASS |
| `test_deadlock_detector_ignores_other_db_errors` | Ошибка внешнего ключа НЕ принимается за взаимоблокировку — повтор не срабатывает не на своём классе | PASS |
| `test_cron_tick_releases_worker_lock` | Сессионная блокировка worker'а снимается. Проверка идёт по долгоживущему соединению: на `NullPool` соединение закрывается само и утечка была бы незаметна | PASS |
| `test_cron_tick_commits_per_student` | У двух учеников РАЗНЫЙ `updated_at` — проход больше не идёт одной транзакцией на всех | PASS |

**Мутационная проверка** (тест обязан кусаться, а не просто быть зелёным):
временно убран `pg_advisory_unlock` из `finally` тика →
`test_cron_tick_releases_worker_lock` **упал**. Код восстановлен.

### Прогоны

```bash
# новые + соседние тесты фонового тика (tsk-541)
python -m pytest tests/test_tsk626_course_state_deadlock.py \
                 tests/test_tsk541_subcourse_dependency_state_backfill.py -q
# → 10 passed
```

Полный прогон LMS — раздел 7.

### Что проверено стендом, а что живьём

- **Стендом (настоящий PostgreSQL):** сама взаимоблокировка, её устранение,
  распознавание `40P01`, поведение фонового тика.
- **Живьём на проде взаимоблокировку воспроизвести нельзя** — для этого
  пришлось бы намеренно устроить гонку на боевых данных. Живая проверка
  подтверждает только, что `next-item` работает после выката, и что новых
  записей `DeadlockDetected` в прод-логе не появилось.

---

## 6. Риски и компромиссы

| Риск | Оценка |
|---|---|
| Параллельные запросы одного ученика теперь выстраиваются в очередь | Практически без изменений: они и раньше блокировали друг друга на тех же строках (и иногда падали). Новая задержка появляется только у запросов одного ученика с НЕПЕРЕСЕКАЮЩИМИСЯ наборами курсов — редкий случай |
| Сессионная блокировка тика не снимется при жёстком падении процесса | Соединение при этом умирает, блокировка снимается вместе с ним |
| Тик стал коммитить частями: при сбое в середине часть учеников пересчитана, часть нет | Приемлемо — это кеш, следующий тик через 15 минут доберёт остальных |
| Скрипты в `scripts/`, пишущие кеш напрямую | Идут своими транзакциями мимо правила. В рамки задачи не входят, вынесено в follow-up |

---

## 7. Полный прогон тестов

```bash
python -u -m pytest -v
# ========== 2418 passed, 12 skipped, 72 warnings in 811.17s (0:13:31) ==========
```

Эталон на чистом дереве до задачи: 2413 прошли, 12 пропущено, 0 упавших за
13 мин 39 с. Сейчас **2418 прошли, 12 пропущено, 0 упавших за 13 мин 31 с** —
ровно +5 новых тестов, ни одного падения, время не выросло.

Дополнительно: `mypy` по всем изменённым файлам — без замечаний;
`app.openapi()` побайтово совпадает с `docs/openapi.json` (публичный контракт
не тронут).

---

## 8. Решение `/review-gate`

### **ПРИНЯТО**

Проверено по 12 измерениям, блокирующих находок нет.

| Измерение | Итог |
|---|---|
| 1. Соответствие целям | Все 5 пунктов декомпозиции закрыты. Одно осознанное отклонение — см. Н3 |
| 2. Корректность | Порядок захвата блокировок согласован у всех писателей; цикла с advisory-локами `manual_progress_service._lock(student, item)` нет — там порядок всегда «сначала блокировка элемента, потом кеша», обратного пути в коде не существует |
| 3. Миграции / данные | Схема не менялась, миграции нет |
| 4. Безопасность | Секретов нет, все параметры SQL связанные, новых эндпоинтов и прав нет |
| 5. Тесты | 5 новых против настоящего PostgreSQL, включая контрольный (доказывает, что сцена ловит дефект) и мутационную проверку |
| 6. Docs drift | `docs/learning-engine-next-item.md` обновлён — там было «один запрос при `update_state_table=True`» |
| 7. Полнота фазы | TODO и заглушек нет; набор файлов = набор задачи |
| 8-9. Данные / справочники | Не применимо |
| 10. Дата/время | `now()` только внутри SQL, сравнений дат не добавлено |
| 11. Cross-project | Триггеров нет: HTTP-контракт, схема БД и Pydantic-схемы не менялись (проверено сверкой `openapi.json`). Обновление `docs/cross-project/` не требуется |
| 12. Публичный API | URL, метод, схемы и коды ответов не менялись; поведение при взаимоблокировке — повтор внутри, наружу тот же 200 |

### Находки без блокировки

- **Н1 (низкая).** Скрипты в `scripts/`, пишущие `student_course_state` прямым
  SQL, идут своими транзакциями мимо правила. В рамки задачи не входят —
  вынесено в follow-up.
- **Н2 (низкая).** Слияние учёток берёт блокировку кеша, хотя риск там
  минимальный (source — только что созданный дубль). Оставлено ради полноты
  правила: исключение из «одного правила для всех писателей» пришлось бы
  помнить.
- **Н3 (информационная, DRIFT).** Декомпозиция предлагала «сортировать по
  `(student_id, course_id)` перед вставкой». Сделано иначе — блокировка по
  ученику. Причина в §3: набор курсов у писателей разный и заранее неизвестен
  (узел позиции резолвится по ходу обхода), поэтому сортировка строк
  согласованного порядка не даёт. Идея сортировки применена там, где она
  работает — к порядку обхода УЧЕНИКОВ у многоучениковых писателей.

### Operator handoff

Ветвь **А** целиком: тесты, mypy, read-only SQL по проду, коммит, пуш, выкат и
живой прогон — агент делает сам (стоячая авторизация, tsk-359). Операторских
шагов нет.

---

## 9. Живая проверка после выката

_(заполняется по факту выката)_
