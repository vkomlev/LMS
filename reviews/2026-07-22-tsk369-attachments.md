# tsk-369 — задания требуют файл-приложение, а файла нет

Дата: 2026-07-22 · База: прод `learn` (5.42.107.253) · Задача: [[tsk-369]] (P0)

## Что было

Задания ЕГЭ №3, 9, 10, 17, 18, 22, 24, 26, 27 формулируются коротко («Откройте файл
электронной таблицы…», «Текстовый файл состоит из…»), а всё различие между задачами одного
типа — в приложенном файле. Ученик открывал такое задание и видел текст без данных: решить
его невозможно в принципе. Наружу это не всплывало ничем — у задания есть и условие, и
правило проверки, формально всё исправно (в отличие от класса tsk-361, где правило пустое).

## Две поправки к исходной постановке

1. **`task_content.media` тут ни при чём.** Поле пусто у ВСЕХ 6303 активных заданий (`[]`
   либо отсутствует) — механизм не использует ни один импорт. Счёт «125 рабочих / 108
   сломанных» из постановки получился из-за того, что `'[]'` не попал в фильтр
   `IN ('', 'null')`. Рабочих по этому признаку не было вовсе.

2. **Мягкие переносы.** sdamgia расставляет U+00AD внутри слов («Тек­сто­вый файл
   со­сто­ит»), и поиск по условию терял целую партию. После их снятия картина другая:

| | заданий |
|---|---|
| активных заданий с файловым условием | 339 |
| из них файл был | 116 |
| **файла не было** | **223** |

Файл виден ученику только ссылкой `/api/v1/media/...` внутри `stem`: клиент SPW рисует
условие как HTML и поле `attached_file_paths` не читает (проверено по коду SPW). Значит и
чинить надо ссылкой в условии — метаданные ставятся заодно, для машинного учёта.

## Что сделано

**Привязано 223 задания, 200 файлов (411 МБ). Остатка нет.**

| источник | заданий | как получен файл |
|---|---|---|
| kompege | 147 | `GET /api/v1/task/<id>` → `files[].url` |
| sdamgia | 35 | страница задачи → `/get_file?id=N` |
| yandex | 34 | `POST /api/v5/gpttr` → `markup.resources[].link` |
| близнец в LMS (Крылов) | 6 | готовый `sha_ext` задания `crylov:v<В>t<З>` |
| локальный архив книги Крылова | 1 | `ZADANIE-9/9var03.ods` |

По курсам: 151 → 63, 153 → 43, 160 → 35, 149 → 26, 145 → 19, 138 → 16, 141 → 15, прочие → 3.

Файлы уложены штатным помощником ContentBackbone `store_bytes_to_cas` (тот же CAS-путь,
тот же прод-бакет, идемпотентно по содержимому) и **каждый** проверен запросом к боевому
эндпоинту `GET /api/v1/media/<sha_ext>` → 200. Запись в БД — одной транзакцией, с бэкапом
прежних условий и построчной проверкой после COMMIT.

## Гейт: почему файл считается «тем самым»

ID сам по себе не доказательство (обжиг tsk-362: задание 3108 ссылалось на sdamgia:68243,
а по тому ID лежит другая задача). Поэтому:

1. **Базовый гейт** — дословный фрагмент 60 букв из середины условия LMS есть в тексте
   источника, И все значимые числа источника (3+ цифр) присутствуют в условии LMS.
2. **Второй гейт для `weak`** (сошёлся один признак из двух) — верный ответ LMS совпадает
   с ответом источника ИЛИ доля общего текста ≥ 0.75. Через него прошли 10 заданий: у
   части импорт обрезал таблицу-пример (её числа «пропали»), у части KaTeX рвал текст.
3. **Тип файла против формулировки** — «файл электронной таблицы» → xls/ods/xlsx/csv,
   «текстовый файл» → txt/odt. Расхождение = стоп.

**Независимая массовая проверка.** У 188 пар «задание ↔ источник» верный ответ есть с обеих
сторон: **184 совпали точно**, 3 — с точностью до формы записи («2; 6; 2; 3» = «2623»),
1 разошлась — у задания 3791 в LMS записан мусорный ответ «ы» (см. раздел про ответы).

**Шесть задач решены по самому файлу — ответ сошёлся:**

| задание | что считали | получилось | ожидали |
|---|---|---|---|
| 3573 | минимальный отрезок, где `E` ровно 100 раз | 353 | 353 |
| 3000 | максимальный отрезок, где `K9` ≤ 150 раз и конец не `D` | 246960 | 246960 |
| 2837 | последняя строка с двумя парами повторов, сумма её чисел | 331 | 331 |
| 3409 | самый длинный отрезок с максимумом параллельных процессов | 5 мс | 5 |
| 3177 | процессов, завершённых за первые 17 мс | 12 | 12 |
| 3058 | сумма чисел последней подходящей строки | 901 | 901 |

Третья — файл из локального архива книги Крылова (варианта 3 нет онлайн), и решение по
файлу здесь единственный доступный гейт; последние три — добор по уточнениям оператора.

## Живая проверка на проде

Под учётной записью оператора (`live-browse.mjs --surface spw`), 6 заданий разных партий:

* `tg:ege:982` (курс 151), `ext:pdf:…crylov:v3…v3t9` (160), `wp_nav:24:d5f0aab6` (151),
  `sdamgia:41001` (153), `tg:ege:955` (153), `tg:ege:569` (138), а также добор
  `tg:ege:733`, `tg:ege:915`, `tg:ege:425` — HTTP 200, блок «Файл к заданию» найден в живом
  DOM, ссылка вида `/api/v1/media/<sha>.xlsx` присутствует в разметке.
* **Ловушка прогона:** без `--wait` снимок дважды сняли на экране «Загружаем…» и получили
  ложное NOT FOUND. Страница задания в SPW дорисовывается клиентом — проверять её надо
  с ожиданием (`--wait 8000`), иначе «файла нет» окажется неправдой о своей же правке.
* Скачивание проверено обоими путями: `learn.victor-komlev.ru/api/v1/media/<sha>` и
  `api.learn.victor-komlev.ru/...` → 200, размер совпадает с загруженным файлом
  (7 000 000 Б и 46 809 Б на двух проверенных).

## Последние три — закрыты уточнениями оператора

Оператор дал ключи, которых автоматика получить не могла: у двух заданий в шапке условия
**опечатка в номере задачи**, у третьего подтверждение пришло диаграммой Ганта.

| id | было | стало | чем подтверждено |
|---|---|---|---|
| 3177 | kompege:27360 | **kompege:23760** | Условие совпало с источником дословно, включая вопрос. Задача решена по файлу: за первые 17 мс завершаются 12 процессов — ровно ответ источника. |
| 3058 | источник не опознан | **kompege:23747** | Задача решена по файлу: последняя подходящая строка 41990, сумма её чисел 901 — ровно ответ источника. |
| 3409 | kompege:17876, сверка не прошла | тот же, подтверждён | Таблица в файле совпала с диаграммой оператора построчно 13 из 13. Пересчёт по файлу: максимум 4 одновременных процесса, самый длинный такой отрезок 5 мс (мс 15…19 — выделенные на диаграмме колонки). Ответ 5 сошёлся. |

Подтверждение по 3409 записано в `reviews/2026-07-22-tsk369-operator-confirmed.json` — файл
читается сборщиком плана (`--confirmed`), то есть ручное решение проходит через тот же
конвейер и остаётся в истории, а не обходит гейт молча.

**Обжиг про ложное совпадение ответа.** У 3177 старый ID 27360 давал ответ 113 — ровно тот,
что записан в LMS, — но по этому ID лежит совсем другая задача («исполнитель с командами
A, B, C»). Совпал бы гейт «по ответу» в одиночку — привязали бы чужой файл. Спасло то, что
текстовый признак не сошёлся.

## Попутные ошибки в ответах — по решению оператора исправлены

| id | было | стало | обоснование |
|---|---|---|---|
| 3177 | 113 | **12** | 113 — ответ чужой задачи 27360, попал по опечатке в номере. 12 подтверждено источником 23760 и расчётом по файлу. |
| 3058 | ответа нет, уходило на ручную проверку | **901**, авто-проверка | Ответ источника 23747, подтверждён расчётом по файлу. |
| 3409 | в условии нет самого вопроса | **вопрос дописан** | Формулировка взята дословно у источника kompege:17876; ответ 5 уже стоял и подтверждён расчётом. |

Скрипт `scripts/tsk369_fix_answers.py` (транзакция, бэкап `reviews/2026-07-22-tsk369-answers-backup.json`,
построчная проверка после COMMIT). Живая проверка: у 3409 дописанный вопрос виден на
странице задания под учётной записью оператора.

**Осталось оператору (класс tsk-368):** у задания **3791** (sdamgia:72584) в LMS записан
ответ «ы», источник даёт «4229&23». Файл привязан, сверка условия прошла — чинить надо ответ.

**Не тронуто намеренно:** у 3409 преамбула условия продублирована (импорт вставил её дважды).
Это отдельный класс правок текста, решения по нему не было.

## Постоянный чек

`scripts/check_missing_attachments.py` + еженедельная обёртка
`scripts/check_missing_attachments_weekly.ps1` (пара к `check_ungradable_tasks_weekly.ps1`).
Ищет активные задания, где условие требует файл, а ссылки `/api/v1/media/` в условии нет.
Отдельно подсвечивает случай «файл есть в метаданных, но ученику не виден» — он чинится
дописыванием ссылки. Read-only, коды выхода 0/1/2.

Прогон после правки: **было 223 → стало 0.**

## Скрипты (порядок прогона)

```
scripts/tsk369_collect.py       # инвентарь + разрешение источника (read-only)
scripts/tsk369_fetch_files.py   # скачать файлы + сверка «текст/числа/тип файла»
scripts/tsk369_probe_unknown.py # перебор источника по ID; близнецы Крылова внутри LMS
scripts/tsk369_archive_file.py  # файл из локального архива книги (решение по файлу)
scripts/tsk369_build_plan.py    # отбор по гейтам + второй гейт для weak
scripts/tsk369_store_cas.py     # CAS + прод-S3, проверка доступности каждого файла
scripts/tsk369_apply.py         # запись в БД: транзакция, бэкап, проверка после COMMIT
```

Артефакты: `reviews/2026-07-22-tsk369-attach-evidence.json` (по каждому заданию: источник,
sha файла, вердикт и доказательство), `reviews/2026-07-22-tsk369-stems-backup*.json`
(прежние условия до правки — материал для отката).

## Риски и хвосты

* **Подпись безымянного файла.** У 40 заданий источник не дал имени файла, и подписью
  бралось «Файл к заданию» — на экране выходило «Файл к заданию: Файл к заданию». Исправлено
  на «скачать» (`scripts/tsk369_fix_link_label.py`, бэкап рядом), проверено живьём.
* **Формат ссылки** одинаков для всех 223 заданий (`<p><strong>Файл к заданию:</strong> …`
  в начале условия). Партия Крылова из tsk-317 использует свой вид (`<p><a>Файл к
  заданию</a></p>`) — визуально они рядом, но не идентичны; при желании привести к одному
  виду — отдельная мелкая задача.
* **Яндекс отдаёт один и тот же набор данных в 2–4 форматах** (csv/ods/xls/xlsx) — все
  привязаны, ученик выбирает удобный. Это увеличило объём хранилища, но не число сущностей.
* **Во время работы параллельная сессия** деактивировала задания 3332 и 3455 и завела
  взамен 9567 — это учтено (9567 получил файл, те два выбыли из выборки).
* `attached_file_paths` теперь заполнен у 223 заданий, но клиентом не используется. Если
  когда-нибудь SPW начнёт рисовать вложения из метаданных — ссылка в условии станет
  дублем, и её нужно будет убрать одним проходом.
