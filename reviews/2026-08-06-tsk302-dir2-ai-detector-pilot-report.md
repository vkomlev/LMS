# tsk-302 (направление 2) — пилот детектора ИИ-авторства кода ученика: отчёт

**Дата:** 2026-08-06
**Задача:** [tsk-302](D:\Work\Root\tasks\tsk-302-avtoproverka-koda-uchenikov-detektor-ii-avtootsenka-kachestva-koda.md), направление 2
**Архитектурное решение:** [docs/ai/adr/0005-ai-authorship-detector-pilot-scope.md](../docs/ai/adr/0005-ai-authorship-detector-pilot-scope.md)
**Статус:** пилот проведён (read-only, аналитически, без уведомления учеников). **В прод НЕ внедрено** — это отчёт для решения оператора, не код.

## Главный вывод — раньше, чем цифры

**Реальных ученических сдач для анализа не существует.** Курс 165 (turtle_sim) на проде запущен 2026-08-05, за сутки до этого отчёта. В `task_results` для turtle_sim всего 4 строки, и все 4 — от `user_id=142` (аккаунт оператора, используемый для живых прод-проверок, см. `project_prod_live_testing` в памяти), а не реальных учеников. Формулировка задачи предполагала «прогнать эвристику на исторических сдачах и посчитать процент» — этого материала физически нет, и никакая инженерная работа его не создаст быстрее, чем ученики реально начнут сдавать задания.

Поэтому ниже — два раздела вместо одного: (1) честный прогон на всех 4 существующих строках (это не «исторические сдачи учеников», а собственные тестовые прогоны оператора — маркирую явно), и (2) синтетическая калибровка на специально сконструированных примерах, чтобы ответить на вопрос «работает ли сама идея», раз статистики по реальным ученикам пока нет.

## Часть 1 — прогон на 4 существующих строках прод-БД

| id | task | код (как сдан) | вердикт | обоснование |
|---|---|---|---|---|
| 15563 | 10029 (квадрат 50px) | `import turtle t = turtle.Turtle() for _ in range(4): t.forward(50) t.right(90) turtle.done()` (без переводов строк — артефакт капчи ответа, не авторства) | **AMBIGUOUS** | Минимальный, «учебниковый» код без единого стилистического маркера ни в одну сторону: нет докстрингов/комментариев (не ИИ-маркер), но и нет магических чисел/неряшливости (не ученический маркер). Ровно то решение, которое написал бы и опытный автор задачи, и ИИ, и аккуратный ученик — код слишком короткий, чтобы нести сигнал. |
| 15564 | 10029 | тот же код | **AMBIGUOUS** | Идентично 15563. |
| 15565 | 10029 | тот же код с нормальными переводами строк | **AMBIGUOUS** | Идентично, форматирование — не отпечаток авторства при такой длине. |
| 15566 | 10030 (узор 12×150°) | `import turtle t = turtle.Turtle() for _ in range(5): t.forward(50) t.right(72)` — рисует ПЯТИУГОЛЬНИК, а не запрошенный в условии узор из 12 отрезков под 150° | **AMBIGUOUS**, но с отдельной пометкой | Код не соответствует условию своей же задачи (5 сегментов/72° вместо 12/150°) — это не признак ИИ или ученика, это явно скопированный тестовый фикстур не под ту задачу (совпадает буквально с `PENTAGON_CODE` из `tests/test_turtle_sandbox_tsk412.py`). Технически «неверный», но не автор-специфичный сигнал. |

**Интерпретация:** все 4 строки на самом деле — не ученические сдачи, а опознаваемые тестовые фикстуры из набора tsk-412 (`SQUARE_CODE`/`PENTAGON_CODE`), которые оператор вручную вставлял при живой проверке песочницы. Ни один из четырёх не является ни примером ИИ-списывания, ни примером органического ученического кода — это третья категория («переиспользование известного тестового фрагмента»), которую рубрика (AI_LIKELY / STUDENT_LIKELY) вообще не предполагала и не обязана классифицировать. Никакого процента флагуемых случаев из этого посчитать нельзя и не нужно пытаться — 0% сигнала не значит «детектор не работает», значит «здесь нечего детектировать».

## Часть 2 — синтетическая калибровка (сконструированные примеры)

Раз реальных данных нет, проверяю саму рубрику на контрастных примерах, которые я написал(а) специально для этого — они НЕ из прод-БД.

### Пример А — сознательно «ИИ-стиль»

```python
import turtle


def draw_square(turtle_obj, side_length=50):
    """
    Draws a square using turtle graphics.

    Args:
        turtle_obj: The turtle instance used for drawing.
        side_length: The length of each side of the square in pixels.
    """
    for _ in range(4):
        turtle_obj.forward(side_length)  # move forward by side_length
        turtle_obj.right(90)  # turn right by 90 degrees


my_turtle = turtle.Turtle()
draw_square(my_turtle)
turtle.done()
```

**Вердикт: AI_LIKELY.** Google-style докстринг с секцией `Args:`, построчные англоязычные комментарии, дублирующие очевидное (`# move forward by side_length` к строке `forward(side_length)`), выделение магического числа `50` в именованный параметр с default — избыточная инженерная культура для задачи «нарисовать квадрат» уровня «изучил циклы и функции». Школьник, только осваивающий функции, крайне редко пишет `Args:`-докстринги по конвенции Google Python Style Guide без явного указания это делать.

**Побочная находка:** типичный ИИ-паттерн `if __name__ == "__main__":` в этот пример НЕ включён специально — проверено отдельно (`guard.check_code_is_safe`), что он **автоматически отклоняется песочницей tsk-412** («обращение к имени `__name__` запрещено», dunder-имя под запретом AST-стража). То есть часть наиболее узнаваемых ИИ-идиом уже физически не может дойти до детектора — они бракуются на этапе приёма ответа как `forbidden_construct`, независимо от вопроса авторства. Детектору не нужно ловить эту разновидность вообще.

### Пример Б — сознательно «ученический стиль»

```python
import turtle
t=turtle.Turtle()
for i in range(4):
    if i==2:
        t.color("red")
    t.forward(50)
    t.right(90)
turtle.done()
```

**Вердикт: STUDENT_LIKELY.** Отсутствие пробелов вокруг `=`/`==` (типичная привычка новичка, не проходящая через автоформаттер, которым ИИ-инструменты обычно результат прогоняют), точечное условие `if i == 2` без объяснения назначения (не рефакторено в параметр/константу — сырой экспериментальный код: «раскрашу третью сторону, потому что интересно, как это будет выглядеть»), отсутствие функций и докстрингов. Живой, слегка «грязный» стиль ручной итерации, а не готовый чистовик.

### Пример В — пограничный (минимальный, но чистый)

Совпадает по факту с реальными сдачами из Части 1. Отдельно синтетический пример не строился — Часть 1 уже это демонстрирует: **AMBIGUOUS остаётся честным ответом, когда кода слишком мало для сигнала**, а не артефактом слабости рубрики.

## Ограничение, которое не решается инженерно

Задания turtle_sim на этом уровне (нарисовать квадрат/пятиугольник, 5-10 строк кода) **структурно не несут достаточно стилистического сигнала** для надёжного различения — минимальное правильное решение практически ОДНО и то же независимо от источника. Детектор станет осмысленным только на заданиях, дающих ученику пространство для решения (больше 15-20 строк, разветвление логики, несколько подходов) — короткие «нарисуй фигуру» задачи для этой цели плохо подходят структурно, не из-за слабости эвристики.

## Рекомендация

1. **Не внедрять в прод сейчас.** Ни статистики, ни калибровки на реальных учениках нет — решение было бы основано на 4 самоповторяющихся тестовых фикстурах, это не решение, а угадывание.
2. **Естественная точка возврата** — когда у turtle_sim (или любого другого Python-задания с открытым решением) накопится реальный объём ученических сдач (ориентир: от нескольких десятков разных учеников, не привязан к календарной дате). До этого момента возвращаться к пилоту нет смысла — данных не прибавится от повторного анализа тех же 4 строк.
3. **При возврате к теме** — рубрика выше (Часть 2) готова к использованию как стартовая точка, включая уже найденное ограничение (`__main__`/dunder-идиомы уже отсекаются guard.py — не дублировать эту проверку в детекторе).
4. **Если оператор решит продолжать** — следующий шаг не код, а решение по открытым вопросам из ADR-0005 (Go/No-Go продакшн-интеграции): порог ложноположительных, видимость вердикта (по аналогии с направлением 1 — вероятно тоже только методисту), какие типы заданий (кроме turtle_sim) в scope.

## Что НЕ было сделано в этом заходе (осознанно)

- Не написан автоматизированный скрипт вызова LLM API (Anthropic) в batch-режиме — при n=4 это оверинжиниринг; рубрика прогнана вручную в этой сессии.
- Не добавлено поле/таблица в БД под вердикты пилота — отчёт живёт как markdown-артефакт, не как постоянная схема (при решении «внедрять» — отдельная задача со своим ADR).
- Ученики не уведомлялись, доступ был read-only (`learn_prod_db`, MCP), никаких записей в прод-БД пилот не производил.
