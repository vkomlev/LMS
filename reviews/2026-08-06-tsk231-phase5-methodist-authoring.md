# Ревью — tsk-231, Фаза 5: авторинг мини-курса в `/methodist`

**Skill:** `/claude-booster` (документация, не код) · **Дата:** 2026-08-06
**План:** [docs/specs/2026-08-06-plan-tsk231-mini-kursy-blokirovka.md](../docs/specs/2026-08-06-plan-tsk231-mini-kursy-blokirovka.md), Фаза 5

## Изменение

`skills/core/methodist/references/lms-wp-export.md` — новый раздел §1.1.2 «Мини-курс повторения
и закрепления через `course_dependencies`». Задокументирован: (1) авторинг — обычный курс, без
новой сущности; (2) технические ограничения — только между корневыми курсами; (3) **явное
предупреждение о глобальной/мгновенной семантике блокировки** (решение оператора tsk-231) с
практической рекомендацией — узкий курс темы, не зонтичный; (4) снятие блокировки — стандартный
`COMPLETED`; (5) анти-цикл — существующий task-level `BLOCKED_LIMIT`, отдельного механизма не
нужно.

## Anti-bloat check

1. Покрыто существующим? Частично — §1.1 уже упоминает `course_dependencies` одной строкой без
   деталей поведения/семантики; §1.1.1 уже занял термин «мини-курс» для другого смысла (размер
   ≤20). Явно разведено в тексте нового раздела, не задублировано.
2. Локально/глобально? Локально — специфика LMS-графа курсов, только `lms-wp-export.md`.
3. Вынести в отдельный reference-файл? Нет — один связный раздел ~30 строк, файл (242 строки)
   остаётся в разумном размере, не создаёт нагромождения.
4. Дублирует соседний skill? Нет — `/methodist` единственный skill, отвечающий за авторинг курсов.
5. Устарело ли что-то старое? Нет удаляемых утверждений — только новое дополнение.

## Validation

- Файл синтаксически корректен (markdown, заголовки без разрывов нумерации — проверено вручную:
  §1.1.2 идёт после §1.1.1, до §1.2, порядок и нумерация последовательны).
- `SKILL.md` ссылается на `lms-wp-export.md` целиком (Шаг 0/Шаг 6), новый подраздел
  подхватывается автоматически — точечных правок `SKILL.md` не требуется.

## Residual

Раздел описывает МЕХАНИЗМ (как связать), но не даёт готового шаблона диалога с оператором для
интерактивного создания мини-курса — тот же общий флоу `/methodist`, что и для обычного курса,
плюс один API-вызов в конце. Отдельного мастера не требуется по объёму (2 шага).
