diff --git a/deploy/local/deploy-lms.cmd b/deploy/local/deploy-lms.cmd
index 9d0b080..58306bb 100644
--- a/deploy/local/deploy-lms.cmd
+++ b/deploy/local/deploy-lms.cmd
@@ -1,5 +1,6 @@
 @echo off
 chcp 65001 >nul
-powershell -NoProfile -ExecutionPolicy Bypass -File "%~dp0deploy-lms.ps1"
+REM Аргументы пробрасываются в ps1: deploy-lms.cmd -HoldOk (см. tsk-672).
+powershell -NoProfile -ExecutionPolicy Bypass -File "%~dp0deploy-lms.ps1" %*
 echo.
 pause
diff --git a/deploy/local/deploy-lms.ps1 b/deploy/local/deploy-lms.ps1
index f2156ff..d26cf07 100644
--- a/deploy/local/deploy-lms.ps1
+++ b/deploy/local/deploy-lms.ps1
@@ -1,17 +1,37 @@
-﻿[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
-
-# Локальный триггер деплоя LMS на прод (VPS lms-spw-vds).
+﻿# Локальный триггер деплоя LMS на прод (VPS lms-spw-vds).
 # Запускает deploy/vps/deploy.sh на сервере через ssh -tt (pty обязателен —
 # see D:\Work\LMS\TODOS.md, Defaults use_pty в sudoers app-deploy).
 # Всегда деплоит origin/main HEAD (deploy.sh делает git fetch + reset --hard).
+#
+# -HoldOk — осознанно выкатить, даже если в ветке есть работа, помеченная как
+# ждущая решения оператора (строка Hold-For-Operator в теле коммита, tsk-672).
+# Без флага такой выкат останавливается с кодом 2 и ничего не трогает.
+
+param(
+    [switch]$HoldOk
+)
+
+[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
+
+$remoteCmd = if ($HoldOk) {
+    "sudo -u app env DEPLOY_HOLD_OK=1 bash /opt/lms/deploy/vps/deploy.sh"
+} else {
+    "sudo -u app bash /opt/lms/deploy/vps/deploy.sh"
+}
 
 Write-Host "== Деплой LMS (origin/main -> lms-spw-vds:/opt/lms) ==" -ForegroundColor Cyan
+if ($HoldOk) {
+    Write-Host "Пометки «ждёт решения оператора» проигнорированы (-HoldOk)." -ForegroundColor Yellow
+}
 
-ssh -tt lms-spw-vds "sudo -u app bash /opt/lms/deploy/vps/deploy.sh"
+ssh -tt lms-spw-vds $remoteCmd
 $exitCode = $LASTEXITCODE
 
 if ($exitCode -eq 0) {
     Write-Host "`nДеплой LMS завершён успешно." -ForegroundColor Green
+} elseif ($exitCode -eq 2) {
+    Write-Host "`nДеплой остановлен: в ветке есть работа, ждущая решения оператора (см. вывод выше)." -ForegroundColor Yellow
+    Write-Host "Прод не тронут. Решение получено — повторить с флагом -HoldOk." -ForegroundColor Yellow
 } else {
     Write-Host "`nДеплой LMS завершился с ошибкой (код $exitCode). Смотри вывод выше." -ForegroundColor Red
 }
diff --git a/deploy/vps/README.md b/deploy/vps/README.md
index 0effe8d..7fd50fa 100644
--- a/deploy/vps/README.md
+++ b/deploy/vps/README.md
@@ -72,6 +72,48 @@ ssh <user>@<vps-ip>
 sudo -u app bash /opt/lms/deploy/vps/deploy.sh
 ```
 
+## Работа, которая ждёт решения оператора (tsk-672)
+
+Деплой делает `reset --hard origin/main`: на прод уезжает **вся ветка**, а не
+работа выкатывающего. Несколько агентов пишут в одну ветку, поэтому «я свою
+правку не выкатывал» не значит «её нет на проде» — 25.08 неодобренная работа
+уехала прицепом с чужим деплоем.
+
+**Придерживаешь свою работу до ответа оператора** — поставь в тело своего
+коммита строку:
+
+```
+feat: tsk-NNN - краткое описание
+
+Hold-For-Operator: что именно должен решить оператор
+```
+
+Тогда любой деплой (свой или чужой) остановится **до** `reset`, покажет эту
+строку и выйдет с кодом 2. Прод при этом не тронут.
+
+Ключ ищется в сообщении коммита целиком, поэтому не упоминай `Hold-For-Operator`
+в коммитах, которые ничего не придерживают — иначе остановишь чужой выкат зря.
+
+Первое средство всё же другое: если работу можно завести под рубильником
+(`AI_TUTOR_ENABLED`, `code_review_cron_enabled` — env-переменные в `/opt/lms/.env`),
+выкатывай её **выключенной**. Тогда включение — правка `.env` + рестарт, без
+деплоя, и пометка не нужна. Пометка — для того, что рубильником не закрыть
+(схема API, миграция, поведение экрана).
+
+**Решение получено, выкатываем:**
+
+```bash
+sudo -u app env DEPLOY_HOLD_OK=1 bash /opt/lms/deploy/vps/deploy.sh
+```
+
+С машины оператора — тот же смысл: `deploy/local/deploy-lms.ps1 -HoldOk`.
+
+Гейт проверяется тестом (временные репозитории, сервер не нужен):
+
+```bash
+bash deploy/vps/test-hold-gate.sh
+```
+
 ## Откат
 
 Быстрый откат к версии, которая работала до последнего `deploy.sh` (SHA сохраняется
diff --git a/deploy/vps/deploy.sh b/deploy/vps/deploy.sh
index 40c7a60..07deb20 100644
--- a/deploy/vps/deploy.sh
+++ b/deploy/vps/deploy.sh
@@ -67,8 +67,55 @@ main() {
   git rev-parse HEAD > .last-deploy-sha
   echo "Версия перед деплоем (цель отката): $(cat .last-deploy-sha)"
 
-  echo "== git fetch + reset to origin/main =="
+  echo "== git fetch =="
   git fetch origin
+
+  # Выкат делает reset --hard на origin/main, то есть на прод уезжает ВСЁ
+  # закоммиченное в ветке, а не только работа выкатывающего. Чипы работают в
+  # одну ветку, поэтому «я свою правку не выкатывал» не значит «её нет на
+  # проде»: 25.08 неодобренная работа уехала прицепом с чужим деплоем (tsk-672,
+  # увезло tsk-665, увезло работу tsk-658).
+  #
+  # Кто сознательно придерживает работу до решения оператора — ставит в тело
+  # СВОЕГО коммита строку:
+  #   Hold-For-Operator: <что должен решить оператор>
+  # Гейт ниже не даёт соседу увезти такой коммит молча.
+  # Осознанно выкатить всё равно:
+  #   sudo -u app env DEPLOY_HOLD_OK=1 bash /opt/lms/deploy/vps/deploy.sh
+  local range="HEAD..origin/main" ahead held
+  echo "== что уедет на прод сверх текущей версии =="
+  ahead=$(git rev-list --count "$range")
+  if [[ "$ahead" -eq 0 ]]; then
+    echo "Новых коммитов нет: прод уже на origin/main."
+  else
+    echo "Коммитов к выкату: $ahead"
+    git --no-pager log --oneline "$range"
+  fi
+
+  echo "== проверка пометок «ждёт решения оператора» (tsk-672) =="
+  held=$(git log "$range" --grep='Hold-For-Operator:' --format='%h %s' || true)
+  if [[ -z "$held" ]]; then
+    echo "Помеченных коммитов нет."
+  elif [[ "${DEPLOY_HOLD_OK:-}" == "1" ]]; then
+    echo "ВНИМАНИЕ: помеченные коммиты есть, выкат продолжен по DEPLOY_HOLD_OK=1:"
+    echo "$held"
+  else
+    echo "ОСТАНОВЛЕНО: в ветке есть работа, помеченная как ждущая решения оператора." >&2
+    echo "$held" >&2
+    echo "" >&2
+    echo "Чего ждут:" >&2
+    git log "$range" --grep='Hold-For-Operator:' --format='%B' \
+      | grep -i '^Hold-For-Operator:' >&2 || true
+    echo "" >&2
+    echo "Ничего не тронуто: прод остался на прежней версии." >&2
+    echo "Выкат идёт целиком из ветки — выкатить «только своё» нельзя, ветка одна." >&2
+    echo "Что делать:" >&2
+    echo "  1) дождаться решения оператора по пункту выше;" >&2
+    echo "  2) решение получено — повторить деплой с DEPLOY_HOLD_OK=1 и записать это в задачу." >&2
+    exit 2
+  fi
+
+  echo "== reset to origin/main =="
   git reset --hard origin/main
 
   echo "== pip install =="
@@ -87,6 +134,12 @@ main() {
   echo "== smoke: /health (ждём готовности сервера, tsk-640) =="
   wait_for_http_ok http://127.0.0.1:8000/health 30
 
+  # Сверка по факту, а не по намерению (tsk-672/tsk-676): печатаем то, что
+  # реально приехало в рабочее дерево, а не то, что выкатывающий собирался
+  # выкатить. Список до reset — прогноз, этот — факт.
+  echo "== что фактически уехало на прод =="
+  git --no-pager log --oneline "$(cat .last-deploy-sha)..HEAD" || true
+
   echo "Deployed: $(git rev-parse --short HEAD)"
 }
 
diff --git a/deploy/vps/test-hold-gate.sh b/deploy/vps/test-hold-gate.sh
new file mode 100644
index 0000000..4b4276d
--- /dev/null
+++ b/deploy/vps/test-hold-gate.sh
@@ -0,0 +1,95 @@
+#!/usr/bin/env bash
+# Проверка гейта «ждёт решения оператора» из deploy.sh (tsk-672).
+#
+# Гейт не даёт чужому выкату молча увезти на прод работу, помеченную строкой
+# Hold-For-Operator в теле коммита. Проверять его на живом сервере нельзя, но
+# логика — чистый git, поэтому тест поднимает пару временных репозиториев
+# («сервер» + origin) и прогоняет по ним пять случаев.
+#
+# Блок гейта НЕ копируется, а вырезается из соседнего deploy.sh: копия разошлась
+# бы с боевым файлом при первой же правке, и тест проверял бы не то, что поедет.
+#
+# Запуск (git-bash на Windows или bash на сервере):
+#   bash deploy/vps/test-hold-gate.sh
+set -uo pipefail
+
+SRC=${1:-"$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/deploy.sh"}
+[[ -f "$SRC" ]] || { echo "Не найден deploy.sh: $SRC"; exit 1; }
+# Ветку берём из самого deploy.sh, чтобы тест не расходился с ним (main/master).
+BRANCH=$(grep -m1 -oE 'origin/[A-Za-z0-9._-]+' "$SRC" | head -1 | cut -d/ -f2)
+[[ -n "$BRANCH" ]] || { echo "Не удалось определить ветку из $SRC"; exit 1; }
+echo "Проверяем: $SRC (ветка $BRANCH)"
+
+WORK=$(mktemp -d)
+trap 'rm -rf "$WORK"' EXIT
+
+sed -n '/^  local range=/,/== reset to origin/p' "$SRC" | sed '$d' > "$WORK/gate-body.sh"
+if [[ ! -s "$WORK/gate-body.sh" ]]; then
+  echo "ПРОВАЛ: блок гейта не найден в $SRC — его переименовали или удалили."
+  exit 1
+fi
+
+cat > "$WORK/gate.sh" <<EOF
+set -euo pipefail
+gate() {
+$(cat "$WORK/gate-body.sh")
+}
+gate
+EOF
+
+export GIT_AUTHOR_NAME=t GIT_AUTHOR_EMAIL=t@t GIT_COMMITTER_NAME=t GIT_COMMITTER_EMAIL=t@t
+ORIGIN="$WORK/origin"; PROD="$WORK/prod"
+git init -q --bare "$ORIGIN"
+git init -q -b "$BRANCH" "$WORK/dev"
+cd "$WORK/dev"
+echo base > f.txt && git add f.txt && git commit -qm "chore: базовая версия"
+git remote add origin "$ORIGIN" && git push -q origin "$BRANCH"
+git clone -q "$ORIGIN" "$PROD" 2>/dev/null
+cd "$PROD" && git checkout -q "$BRANCH"
+
+run_gate() { (cd "$PROD" && git fetch -q origin && bash "$WORK/gate.sh"); }
+
+pass=0; fail=0
+check() { # check <описание> <ожидаемый код> <фактический код>
+  if [[ "$2" == "$3" ]]; then echo "  OK   $1 (код $3)"; pass=$((pass+1));
+  else echo "  ПРОВАЛ $1: ждали код $2, получили $3"; fail=$((fail+1)); fi
+}
+grep_check() { # grep_check <описание> <текст вывода> <подстрока>
+  if echo "$2" | grep -q "$3"; then echo "  OK   $1"; pass=$((pass+1));
+  else echo "  ПРОВАЛ $1: в выводе нет «$3»"; fail=$((fail+1)); fi
+}
+
+echo "=== 1. Выкатывать нечего — гейт пропускает ==="
+out=$(run_gate 2>&1); check "выкат разрешён" 0 "$?"
+
+echo "=== 2. Обычные коммиты без пометок — гейт пропускает и печатает список ==="
+cd "$WORK/dev"
+echo a >> f.txt && git commit -qam "feat: обычная работа соседа"
+echo b >> f.txt && git commit -qam "fix: ещё одна правка"
+git push -q origin "$BRANCH"
+out=$(run_gate 2>&1); check "выкат разрешён" 0 "$?"
+grep_check "список едущих коммитов напечатан" "$out" "Коммитов к выкату: 2"
+
+echo "=== 3. В ветке есть помеченный коммит — гейт останавливает ==="
+cd "$WORK/dev"
+echo c >> f.txt
+git commit -qam "feat: разбор ответа по критериям
+
+Hold-For-Operator: включение добавляет второй вызов модели на каждой сдаче"
+echo d >> f.txt && git commit -qam "feat: работа соседнего чипа"
+git push -q origin "$BRANCH"
+out=$(run_gate 2>&1); check "выкат остановлен" 2 "$?"
+grep_check "причина пометки показана" "$out" "второй вызов модели"
+
+echo "=== 4. Осознанный обход DEPLOY_HOLD_OK=1 — гейт пропускает ==="
+out=$( (cd "$PROD" && git fetch -q origin && DEPLOY_HOLD_OK=1 bash "$WORK/gate.sh") 2>&1)
+check "выкат разрешён" 0 "$?"
+
+echo "=== 5. Помеченное уже на проде — гейт больше не всплывает ==="
+(cd "$PROD" && git reset -q --hard "origin/$BRANCH")
+out=$(run_gate 2>&1); check "выкат разрешён" 0 "$?"
+grep_check "пометка не повторяется" "$out" "Помеченных коммитов нет"
+
+echo
+echo "ИТОГ: успешно $pass, провалов $fail"
+[[ "$fail" -eq 0 ]]
diff --git a/reviews/2026-08-26-tsk672-deploy-hold-gate.md b/reviews/2026-08-26-tsk672-deploy-hold-gate.md
new file mode 100644
index 0000000..b1ca479
--- /dev/null
+++ b/reviews/2026-08-26-tsk672-deploy-hold-gate.md
@@ -0,0 +1,163 @@
+# tsk-672 — выкат больше не увозит работу, которая ждёт решения оператора
+
+**Дата:** 2026-08-26
+**Проекты:** LMS (`main`), SPW (`master`)
+**Скиллы:** `/executor-pro` (правка выкатных скриптов), `/claude-booster` (правило в глобальном контексте), `/review-gate` (это решение)
+**Захваты:** `clm-337` — `C:\Users\user\.claude\CLAUDE.md`
+
+## Что случилось и почему дисциплина не помогает
+
+`deploy.sh` в LMS и SPW делает `git fetch` + `reset --hard origin/<ветка>`. На прод
+уезжает **вся ветка**, а не работа выкатывающего. Чипы пишут в одну ветку, поэтому
+«я свою правку не выкатывал» не значит «её нет на проде».
+
+25.08 чип tsk-658 сознательно придержал разбор развёрнутого ответа по критериям
+(включение = постоянный расход на модель, решение оператора) и вынес развилку.
+Через час чип tsk-665 выкатил своё — и разбор уехал прицепом. Ущерба не было,
+оператор решил оставить, но решение принял постфактум.
+
+Просить выкатывающего «посмотреть, что рядом в ветке» бесполезно: у него нет этого
+знания и нет повода его искать. Ровно так же не сработало предупреждение хука
+openapi (tsk-495 → рецидив tsk-669) — правило держалось на внимании человека.
+
+## Что выбрано из трёх кандидатов
+
+| Кандидат | Решение | Почему |
+|---|---|---|
+| Рубильник, выключенный по умолчанию | **Взят как первое средство, но правилом, а не кодом** | Рубильник уже есть у части работ (`AI_TUTOR_ENABLED`, `code_review_cron_enabled`). Не хватало не средства, а правила «когда его заводить». Закрывает главный класс — «оператор решает, включать ли»: выкат становится безопасным по построению, включение идёт правкой `.env` без выката. Но рубильником не закрыть схему API, миграцию и поведение экрана. |
+| Отдельная ветка | **Отклонён** | Дороже всего при сегодняшнем темпе: ночью в одном дереве работали до пяти чипов, ветка добавляет мержи, конфликты и память о ней каждому. Общее дерево уже дало инцидент ADR-0008 — вторая ось параллелизма только усложнит. |
+| Предупреждение на выкате | **Взят как вторая сетка, в виде отказа, а не вопроса** | Дёшево: точка `.last-deploy-sha`/HEAD уже есть, диапазон считается одним `git rev-list`. Обычный выкат не получает ни одного лишнего шага — только печать списка. Останавливается только тот выкат, где реально есть помеченная работа. |
+
+Вопрос-подтверждение вместо отказа отвергнут: деплой запускают и через `ssh -tt`,
+и из фонового чипа без tty. Вопрос там либо повиснет, либо проскочит. Отказ до
+`reset` безопасен — прод не тронут, откатывать нечего.
+
+## Что теперь мешает неодобренному уехать
+
+1. **Пометка ставит тот, кто придерживает работу.** Строка в теле своего коммита:
+   `Hold-For-Operator: <что должен решить оператор>`. Внимание требуется от того, у
+   кого знание есть, а не от соседа — в этом отличие от правила, которое не сработало.
+2. **Гейт в `deploy.sh`** (между `fetch` и `reset`, оба проекта): печатает список
+   коммитов, которые уедут сверх текущей версии; если среди них есть помеченный —
+   выводит его и причину, выходит с кодом 2 и **ничего не трогает**.
+3. **Осознанный обход остался:** `DEPLOY_HOLD_OK=1` (по образцу `DBCHECK_OK=1`,
+   `CHIP_SOLO=1`), с машины оператора — `deploy-lms.ps1 -HoldOk` / `deploy-spw.ps1 -HoldOk`.
+   Обход не молчит: печатает, какие именно пометки проигнорированы.
+4. **Сверка по факту, а не по намерению** (урок tsk-676): после `reset` скрипт
+   печатает, что реально приехало (`.last-deploy-sha..HEAD`), а не то, что
+   выкатывающий собирался выкатить.
+
+## Цена обряда
+
+- Обычный выкат (пометок нет): **ноль лишних действий**, +2 строки заголовка и список
+  едущих коммитов. Ничего не спрашивается, ничего не ждёт ввода.
+- Выкат при наличии пометки: остановка, чтение причины, одно решение — ждать или
+  повторить с `DEPLOY_HOLD_OK=1`.
+- Постановка пометки: одна строка в сообщении коммита, только у того, кто и так
+  сознательно придерживает работу.
+
+Это осознанно **не** ещё один шумящий гейт, который начнут обходить не глядя, как
+`ROOT_ALLOW_MULTI_TASK_COMMIT=1`: он молчит на всех выкатах, кроме тех, где есть
+ровно та ситуация, ради которой заведён.
+
+## Файлы
+
+**LMS:**
+- `deploy/vps/deploy.sh` — гейт между `fetch` и `reset` + печать факта после `reset`
+- `deploy/vps/test-hold-gate.sh` — новый, тест гейта на временных репозиториях
+- `deploy/vps/README.md` — раздел «Работа, которая ждёт решения оператора»
+- `deploy/local/deploy-lms.ps1` — флаг `-HoldOk`, отдельная ветка вывода на код 2
+- `deploy/local/deploy-lms.cmd` — проброс аргументов (`%*`)
+
+**SPW:** те же пять файлов (`deploy/vps/deploy.sh`, `test-hold-gate.sh`, `README.md`,
+`deploy/local/deploy-spw.ps1`, `deploy-spw.cmd`), ветка `master`, путь `/opt/spw`.
+
+**Глобально:** `~/.claude/CLAUDE.md` § Operator handoff, ветвь А — правило для
+постановок чипам: рубильник первым средством, пометка вторым, кто ставит и что
+делать после решения оператора.
+
+## Валидация
+
+Тест вырезает блок гейта **из боевого `deploy.sh`** (а не копирует его) и прогоняет
+по паре временных репозиториев — «сервер» и origin. Сервер для этого не нужен.
+
+```
+$ bash deploy/vps/test-hold-gate.sh          # LMS, ветка main
+=== 1. Выкатывать нечего — гейт пропускает ===            OK (код 0)
+=== 2. Обычные коммиты без пометок ===                    OK (код 0) + список напечатан
+=== 3. В ветке есть помеченный коммит ===                 OK (код 2) + причина показана
+=== 4. Осознанный обход DEPLOY_HOLD_OK=1 ===              OK (код 0)
+=== 5. Помеченное уже на проде — гейт молчит ===          OK (код 0)
+ИТОГ: успешно 8, провалов 0
+
+$ bash deploy/vps/test-hold-gate.sh          # SPW, ветка master
+ИТОГ: успешно 8, провалов 0
+```
+
+Дополнительно:
+- `bash -n` на обоих `deploy.sh` — синтаксис чистый.
+- Разбор обоих `.ps1` парсером PowerShell — ошибок нет; `param()` стоит первой
+  инструкцией (иначе PowerShell не принял бы файл), BOM у обоих на месте (`ef bb bf`).
+- Прогон `.ps1` с заглушкой вместо `ssh`: без флага собирается
+  `sudo -u app bash …/deploy.sh`, с `-HoldOk` — `sudo -u app env DEPLOY_HOLD_OK=1 bash …/deploy.sh`,
+  код 2 попадает в свою ветку вывода («прод не тронут»).
+
+## Ограничения — что НЕ закрыто
+
+- **Гейт начнёт действовать со следующего выката.** Сейчас на сервере лежит старая
+  версия скрипта; она приедет вместе с ближайшим деплоем — и только выкат после него
+  будет проверять пометки. Специально деплоить ради этого не стал: в ветках сейчас
+  чужая незавершённая работа (tsk-679, tsk-655, tsk-661), и мой выкат увёз бы её —
+  то есть ровно то, против чего заведена задача.
+- **Полигонные скрипты** (`deploy/poligon/deploy-*-poligon.sh`) и деплой TG_LMS не
+  тронуты: полигон — тестовый стенд, неодобренная работа там не стоит обряда; TG_LMS
+  вне границ задачи.
+- **Ключ ищется по всему сообщению коммита.** Коммит, который просто упоминает
+  `Hold-For-Operator` (например, отменяющий пометку), остановит выкат. Оговорено в
+  README обоих проектов; лечится обходом или переформулировкой сообщения.
+- **Пометка не мешает мержу и не мешает коммиту** — только выкату. Это осознанно:
+  коммитить и пушить работа должна, иначе она живёт в дереве и мешает соседям.
+
+## Как откатить
+
+Изменение — только в выкатных скриптах и документации, состояния нигде не завёл.
+
+```bash
+# LMS
+git revert <sha коммита tsk-672>          # или точечно:
+git checkout <sha до правки> -- deploy/vps/deploy.sh deploy/local/deploy-lms.ps1 deploy/local/deploy-lms.cmd
+# SPW — то же самое своими путями
+```
+
+На сервере откат приедет обычным деплоем. Разовый обход, когда откатывать незачем, —
+`DEPLOY_HOLD_OK=1`. Правило в `~/.claude/CLAUDE.md` — резервная копия
+`~/.claude/skills/claude-booster/references/backups/CLAUDE.md-2026-08-26.md`.
+
+## Решение review-gate
+
+**ПРИНЯТО.**
+
+Проверено по измерениям:
+- **1. Соответствие целям** — все четыре обязательных пункта постановки закрыты:
+  цена обряда оценена и сведена к нулю на обычном выкате; существующие рубильники
+  учтены и стали первым средством, а не заменены новым; правило для постановок чипам
+  записано в глобальный контекст и в README обоих проектов; сверка «что реально
+  уехало» добавлена в скрипт.
+- **2. Корректность** — пять сценариев в тесте, включая «пометка уже на проде»
+  (гейт не всплывает повторно) и «выкатывать нечего». Отказ происходит **до**
+  `reset`, состояние сервера не меняется. Ложное срабатывание безопасно (в сторону
+  остановки), обход задокументирован.
+- **5. Тесты** — есть, прогнаны в обоих репозиториях, проверяют боевой текст скрипта,
+  а не копию. Живого прогона на сервере нет по причине из «Ограничений» — заявлено
+  прямо, не выдано за успех.
+- **6. Docs drift** — README деплоя обоих проектов и глобальный CLAUDE.md обновлены
+  в тех же коммитах.
+- **7. Phase integrity** — заглушек и TODO нет; коммит идёт только своими путями
+  (в деревьях параллельно работают другие чипы), состав проверяется `git show --stat`
+  до пуша.
+- **11. Cross-project** — правило деплоя касается LMS и SPW, запись добавлена в
+  `ContentBackbone/docs/cross-project/CHANGELOG.md`. Контракты API и схема БД не
+  затронуты, `contracts/*.md` и `STATE.md` не менялись.
+- **12. Public API** — не затронут (изменений в `app/api/**` нет).
+
+Блокирующих находок нет.
