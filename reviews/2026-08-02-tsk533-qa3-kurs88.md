# tsk-533: курс 88 — QA часть 3 (Серебрякова) + калибровка глубины лямбда/декораторов

## Контекст
11 пунктов декомпозиции (решения оператора зафиксированы 2026-08-02 в
`D:\Work\Root\tasks\tsk-533-...md`), два источника: заметка оператора о
глубине материала по лямбде/декораторам и QA-письмо Серебряковой Екатерины
«Python для ЕГЭ — часть 3» (30 июля, 6 пунктов, все сверены read-only по
`learn_prod_db` до записи).

## Изменённые/новые файлы
- `scripts/tsk533_qa3_kurs88.py` (новый) — курс 104: dense-реордер 15
  материалов + переписан текст материала 221 (лямбда, поверхностно);
  курс 1451: INSERT нового материала id=3862 «Кэш и мемоизация»; курс 109:
  dense-реордер 29 материалов; задание 235 — стем с объяснением столбцов
  TBL_COM; задание 266 — дифференцированный стем+ответ (не дубль 261).
- `scripts/tsk533_deactivate_509.py` (новый) — материал 509 деактивирован
  после живой проверки: видео на VK удалено правообладателем.

## DB Findings
- Прод (`learn_prod_db`, read-only до и после записи): все целевые
  order_position/content/stem подтверждены независимым каналом после
  `--apply` (см. запросы в транскрипте сессии).
- `is_active`/`requirement_level` не менялись ни для одного материала/
  задания курсов 104/109/1451, КРОМЕ материала 509 (is_active: true->false,
  безопасное направление — снижает знаменатель `compute_course_state`,
  откат прогресса недостижим по конструкции фильтра, precedent
  `project_material_order_triggers_and_activation_risk`).
- Реордер материалов курсов 104 (34 студента) и 109 (34 студента) — только
  order_position (навигация), без is_active/requirement_level — вне
  риск-класса tsk-524/tsk-530 (там риск был в активации).
- Задания 261/266: у обоих есть historical `task_results` (17/16) —
  изменение стема/ответа 266 влияет только на будущие попытки, прошлые
  оценки не пересчитываются.

## Validation Commands
```
DBCHECK_OK=1 python scripts/tsk533_qa3_kurs88.py            # dry-run preview
DBCHECK_OK=1 python scripts/tsk533_qa3_kurs88.py --apply     # запись + verify до COMMIT
DBCHECK_OK=1 python scripts/tsk533_deactivate_509.py         # dry-run preview
DBCHECK_OK=1 python scripts/tsk533_deactivate_509.py --apply # запись + verify до COMMIT
```
Плюс независимая верификация через MCP `learn_prod_db` (SELECT по
materials/tasks) — вывод приложен в транскрипте сессии, совпадает с планом.

## Review-gate: ПРИНЯТО

Шаг 0 (якорь контекста): сверено с tsk-533 (11 пунктов декомпозиции, решения
оператора зафиксированы 2026-08-02) — все пункты покрыты, drift не найден.
Шаг 0.5 (реестр ошибок `docs/ai/ERRORS.md`): ни один известный паттерн
(PG `\b`-regex, falsy-string normalization, auth/API drift, SQL formula bugs)
не применим — изменения не касаются auth/API/regex/датавремени.

12 измерений:
1. Соответствие целям — все 11 пунктов выполнены и верифицированы. PASS.
2. Корректность — dense-реордер проверен на коллизии order_position (0 в
   обоих курсах); задание 235: убрана противоречивая инструкция «два числа
   через пробел» для табличного (TBL_COM) типа — сама эта путаница и была
   дефектом; solution_rules не менялись, формат ответа (`8 4`) остаётся
   совместим с новой формулировкой столбцов. PASS.
3. Безопасность данных/миграций — транзакции, verify-до-COMMIT, независимая
   пост-верификация через MCP `learn_prod_db`; INSERT в 1451 идемпотентен
   (guard по title), UPDATE-операции идемпотентны при повторном запуске. PASS.
4. Секреты/инъекции — все SQL через параметризованные `$1/$2` (asyncpg), DSN
   не хардкожен в коде (читается из `.mcp.json`, который в `.gitignore`).
   PASS.
5. Тесты — одноразовые data-скрипты (не app-код), покрытие — dry-run +
   in-tx assert + независимый read-only канал после COMMIT (эквивалент live
   smoke для этого класса задач, паттерн tsk524/tsk530). PASS.
6-10, 12 — не применимы (нет изменений auth/API/schema/DDL/datetime/JS
   client-server boundary).
11. Cross-project sync — не триггерится (нет нового/изменённого HTTP
    endpoint, Alembic-миграции, Pydantic-схемы, bump зависимостей). N/A.

Блокирующих находок нет. Non-blocking: материал 509 деактивирован без
замены видео — прослежено отдельным чипом оператору (см. Follow-ups),
не код-дефект.

## Risks / Follow-ups
- Материал 509 деактивирован без замены (нет доступа к загрузке нового
  видео) — вынесено оператору отдельным чипом (spawn_task
  `task_2f245cb5`): снять/загрузить новое видео по теме «Срезы, кортежи,
  операторы списков» и реактивировать материал.
- tsk-529 (курс 112, материалы 385/391) на момент координации была ещё
  `active` — оставлена заметка в её «Историю движения» со ссылкой на новый
  материал id=3862 (курс 1451), чтобы будущее исполнение не задваивало
  объяснение `@lru_cache`. (tsk-529 впоследствии закрыта независимо от
  этой задачи.)
- CreateCourses: без изменений — курс 88 WP-происхождения, локальной папки
  под него в `CreateCourses/courses/` нет.
