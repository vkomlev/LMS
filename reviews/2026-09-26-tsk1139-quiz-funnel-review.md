# Review-gate tsk-1139: воронка сайта (LMS + SPW + TG_LMS)

**Решение: ПРИНЯТО** (после обновления cross-project памяти — измерение 11).

План: `docs/specs/2026-09-26-plan-tsk1139-quiz-funnel.md` (утверждён оператором 26.09).

## Проверено
- Тесты: LMS `tests/test_tsk1139_quiz_funnel.py` 12/12, смежные 252 зелёных (1 не связанный таймаут
  `test_code_quality_tsk302`); TG_LMS unit 492/492 (в т.ч. новый `test_tsk1139_quiz_funnel_bot.py`);
  SPW `tsc --noEmit`, eslint — чисто.
- Миграция `tsk1139_quiz_funnel`: upgrade → downgrade → upgrade на dev.
- Живой прогон на dev (LMS :8000 + SPW :3000): `?for=adult` → ветка, метки в сессии; итог — часть
  (без шкал и описания) + ссылка в бот; вход по ссылке → знакомство → `/quiz/{uid}/done`: полный
  разбор, заявка `quiz` с учеником и метками, запись на бесплатный курс; ручной выбор на развилке.
- Доступ: `/integrations/quiz-funnel/bot/*` — только сервисный ключ + сверка tg_id (чужой → 404,
  тест); claim — чужая сессия → 409 (существующий сервис привязки).
- Старый квиз без строки в `quiz_funnel_branch` и при выключенном рубильнике — ответы прежние
  (14 тестов tsk-053).

## Не блокирует
- Бот не прогнан в живом Telegram (dev-бота на стенде нет): покрыт модульными тестами и API-тестами
  LMS. Живая проверка — после включения рубильника на проде.
- Тексты напоминаний и подписи бота — черновые, финал от контент-сессии.
- `enroll_self_free` коммитит внутри себя: заявка фиксируется вместе с записью — приемлемо.
- Ветка «родитель» до текста согласия: `registration_enabled=false` у её строки.
