# /review-gate — tsk-372 follow-up (SA-разрыв + has_evidence)

**Решение: ПРИНЯТО**

Коммиты: LMS `2781e48`, SPW `6c449d4`, cross-project docs ContentBackbone `2806a64`.
Задеплоено на прод, проверено живым прогоном против реальных данных.

## Проверка по измерениям

1. **Соответствие целям** — оператор указал 3 конкретные проблемы (SA
   невидим, нужна выборка "есть материал", вопрос про типизацию). Read-only
   разведка прод-БД дала точные числа (1495/419/930), фикс адресует именно
   найденный разрыв, а не гипотезу. Данные о типизации (26 заданий с
   `requires_attachment=false` при явном запросе файла) сознательно НЕ
   смешаны с этим фиксом — отдельный follow-up, вынесен явно.
2. **Корректность** — найден и исправлен САМОСТОЯТЕЛЬНО, до деплоя, баг
   трёхзначной SQL-логики (`has_evidence` фильтр `false` терял строки из-за
   `false OR NULL = NULL`) — пойман собственным regression-тестом
   (`test_pending_has_evidence_filter_false`), не прод-инцидентом.
3. **БД/миграции** — не применимо, read-only query-параметры + вычисляемое
   поле на существующей колонке `answer_json`.
4. **Безопасность** — ACL/identity-гейт не затронуты, оба новых параметра —
   фильтры поверх уже ACL-scoped выборки.
5. **Тесты** — LMS +7 (SA в mandatory/optional, has_evidence×3, фильтр×2),
   SPW +6 (тумблер, URL, badge). Полный прогон: LMS 1527/11 (было 1519/11),
   SPW 909 passed (было 902, +1 pre-existing unrelated flake не в счёте).
   **Живой прогон против прод-данных**: до фикса 511 optional-работ у
   teacher_id=2, после — 930 (=511+419, точное совпадение с числом из
   read-only разведки БД ДО деплоя — предсказание подтвердилось буквально).
   Фильтр «Только с материалом» сузил до 362, бейдж «Есть материал» виден
   на карточках рядом с «Авто: неверно».
6. **Docs drift** — cross-project контракт обновлён тем же вечером,
   отдельным коммитом (ContentBackbone `2806a64`).
7. **Phase integrity** — находка про 26 mistyped-по-флагу заданий явно
   вынесена как follow-up, не бандлится молча.
8-10. Не применимо.
11. **Cross-project memory sync** — выполнено.
12. **Public API Contract Sync** — `docs/openapi.json` обновлён (хук),
    `lib/api-types.ts` SPW перегенерирован (заодно подтянул легитимный
    несвязанный догон по `get_course_tree`, не чужой WIP — см.
    project-memory про этот хук).

## Operator handoff
Категория А целиком — коммит+пуш+деплой+живая проверка сделаны в этой же
сессии по правилу LMS `~/.claude/CLAUDE.md`.

## Улучшения без блокировки / явные follow-up
- 26 активных `SA_COM`-заданий просят файл/скриншот в тексте, но
  `requires_attachment=false` — нужна ручная сверка методистом, отдельная
  задача (не создана автоматически — оценочное решение оператора, когда
  приоритизировать).
