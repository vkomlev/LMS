# Стенд судейского разбора — 2026-09-01

Прогон: `scripts/llm_model_bakeoff.py --judge`. Ось отдельная от наставницкой:
судья не стримит и с учеником не разговаривает — там важны дисциплина формата,
ПОЛНОЕ время ответа и цена одного разбора.

Промпт боевой (`code_review_service`), образец работы синтетический. Бюджет попытки на бою — 30 c.

| Модель | Вердикт | Формат | Время мед. | Худший | $/разбор | $/1000 разборов | Комментарий |
|---|---|---|---|---|---|---|---|
| `google/gemini-3.7-flash` | **ОШИБКА** | 3/3 | — | — | 0.00000 | 0.00 | HTTP 502: {"error":{"code":"upstream_error","message":"Available balance plus the USD 0.10 Marketplace credit line cannot cover the fixed USD 0.05 call reservation. Add b |
| `openai/gpt-5.4` | **МЕДЛЕННО** | 3/3 | 93.3 c | 151.5 c | 0.00136 | 1.35 | худший прогон 151.5 c > бюджета попытки 30 c |
| `google/gemini-3.6-flash` | **МЕДЛЕННО** | 3/3 | 35.9 c | 41.4 c | 0.00185 | 1.85 | худший прогон 41.4 c > бюджета попытки 30 c |
| `openai/gpt-5.6-luna` | **ГОДЕН** | 3/3 | 10.9 c | 12.7 c | 0.00007 | 0.07 | медиана 10.9 c, худший 12.7 c |

## Пригодны для судьи

- `openai/gpt-5.6-luna` — медиана 10.9 c, формат 3/3

## Ответы кандидатов (читать глазами — метрики не всё)

### `google/gemini-3.7-flash`

```
HTTP 502: {"error":{"code":"upstream_error","message":"Available balance plus the USD 0.10 Marketplace credit line cannot cover the fixed USD 0.05 call reservation. Add b
```

### `openai/gpt-5.4`

```
{
  "language": "Python",
  "code_quality": {
    "score": 6,
    "notes": [
      "В конструкциях `forward(100)`, `range(4)` и `forward(40)` используются числовые значения без названий, хотя для учебной задачи это размеры фигур.",
      "Переменная цикла `i` не используется; имя `t` слишком короткое и не объясняет, что это черепашка.",
      "После рисования квадрата команды крыши начинаются из точки `(0, 0)`, поэтому крыша не располагается над квадратом и геометрически не образует правильную крышу.",
      "Рисование окна в блоке после комментария `# risuem okno` отсутствует в условии задания и делает решение лишним и менее сфокусированным."
    ]
  },
  "ai_authorship": {
    "verdict": "student_likely",
    "reasoning": "На самостоятельный ученический код указывают транслитерированный комментарий `# risuem okno`, короткое имя `t` и добавленная без необходимости часть с окном. Геометрическая ошибка в расположении крыши также характерна для ручного решения новичка."
  }
}
```

### `google/gemini-3.6-flash`

```
{
  "language": "Python",
  "code_quality": {
    "score": 9,
    "notes": [
      "Логика рисования квадрата повторяется дважды (для стен и для окна), ее можно вынести в функцию",
      "Размеры и координаты (100, 40, 30) указаны напрямую в коде без использования переменных"
    ]
  },
  "ai_authorship": {
    "verdict": "student_likely",
    "reasoning": "Комментарий на транслите (`# risuem okno`), естественное добавление окна от себя и простой стиль написания характерны для начинающего ученика."
  }
}
```

### `openai/gpt-5.6-luna`

```
{
  "language": "Python",
  "code_quality": {
    "score": 5,
    "notes": [
      "Числа 100, 90, 60, 120, 30 и 40 записаны прямо в коде без названий.",
      "Имя переменной t слишком общее, а переменная i в циклах не используется.",
      "Квадрат для окна повторяет структуру цикла стен; повторяющийся рисунок можно было вынести в функцию.",
      "После завершения квадрата черепашка находится в исходной точке, поэтому крыша начинается снизу справа и геометрически не образует крышу над квадратом.",
      "Окно не требовалось в задании и добавляет лишние действия."
    ]
  },
  "ai_authorship": {
    "verdict": "student_likely",
    "reasoning": "Транслитерированный комментарий «risuem okno» и простой неровный стиль характерны для самостоятельного ученического кода. Формальных докстрингов, избыточных конструкций или других явных признаков генерации ИИ нет."
  }
}
```
