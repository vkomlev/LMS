# QA-отчёт: tsk-321, ветка «247 ручных-без-ответа + 2 always-wrong (курсы 157,159)»

**Дата:** 2026-07-24 · Только чтение (MCP `learn_prod_db`), запись не понадобилась.

## Результат

Оба дефекта из исходной находки F10 (`docs/qa/2026-07-19-tsk299-import-audit.md`) на текущий момент **отсутствуют в проде**:

- «247 SA_COM без эталона, ручная проверка, скоуп ЕГЭ (курсы 138–160,162,163)» — проверка по точному критерию аудита F10 даёт **0**.
- «2 always-wrong, курсы 157/159» (`short_answer` заполнен, но `accepted_answers` пуст → любой ответ ученика засчитывается неверным) — проверка даёт **0** строк.

Дополнительно проверено **вне** исходного скоупа F10 — по всей активной базе `tasks` нет ни одного задания с пустым `accepted_answers` при `manual_review_required=false` (это и есть точный триггер бага «always-wrong»). Найдены 174 задания с пустым `accepted_answers`, но у всех `manual_review_required=true` — то есть баг не воспроизводится; по выборке стемов подтверждено, что это легитимные ручные форматы (код-программы ОГЭ, скриншот/видео-доказательство ботов, эссе-вопросы QA-manual курса) — не «потерянный эталон».

## Спорные случаи для оператора

**Отсутствуют.** Заполнять/оспаривать нечего — дефект устранён предыдущими партиями правок этой же дорожки (tsk-358/361/367/371/373, закрытие «хвоста-28» этой же tsk-321 сегодня, а также дедуп tsk-350, деактивировавший часть дублей ЕГЭ).

## Полный разбор

`reviews/2026-07-24-tsk321-247-2-alwayswrong-inventory.md` — точные SQL-критерии, разбивка 174 «ложных срабатываний» по типам заданий и обоснование легитимности каждой группы.

## Что не делалось

- Запись в БД — не потребовалась (нечего чинить).
- Расширение `scripts/ege_answer_invariant.py` — не потребовалось: существующий предикат уже покрывает оба паттерна F10 внутри скоупа ЕГЭ независимо от значения `manual_review_required`; за пределами скоупа ЕГЭ (ОГЭ/QA-manual/chat-boty) инвариант «есть эталон» неприменим по дизайну этих курсов.
