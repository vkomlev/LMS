# Стенд судейского разбора — 2026-09-16 (частичный, 09:35)

> **Частичный прогон.** Мерялись только заданные `--models`: `z-ai/glm-5.3`. Это не разбор всего каталога и не замена отчёту дня — выводы верны лишь для перечисленных моделей. Отчёт дня этот прогон не трогал.

Прогон: `scripts/llm_model_bakeoff.py --judge`. Ось отдельная от наставницкой:
судья не стримит и с учеником не разговаривает — там важны дисциплина формата,
ПОЛНОЕ время ответа и цена одного разбора.

Промпт боевой (`code_review_service`), образец работы синтетический. Бюджет попытки на бою — 30 c.

| Модель | Вердикт | Формат | Время мед. | Худший | $/разбор | $/1000 разборов | Комментарий |
|---|---|---|---|---|---|---|---|
| `z-ai/glm-5.3` | **НЕСТАБИЛЕН** | 3/3 | 12.8 c | 12.8 c | 0.00016 | 0.16 | успешных прогонов 1 из 3 |

## Пригодны для судьи

- среди заданных `--models` годных нет: модели ответили, но забракованы по вердиктам выше. Это оценка выборки, а не каталога

## Ответы кандидатов (читать глазами — метрики не всё)

### `z-ai/glm-5.3`

```
{"language":"Python","code_quality":{"score":9,"notes":["Решение корректное и простое: цикл while True с break по вводу 0, сумма и количество считаются верно; сигнальный 0 из условия задания магическим числом не является","Имя kol слишком сокращено — лучше полное kolichestvo или то же слово, что в выводе","summa = summa + n и kol = kol + 1 можно записать короче через += (мелочь, для новичка допустимо)"]},"ai_authorship":{"verdict":"student_likely","reasoning":"Транслитерация в именах и строках (summa, kol, 'vvedi chislo', 'kolichestvo') — типичный ручной почерк русскоязычного школьника; код ровно соответствует уровню задания, без избыточно «причёсанных» конструкций."}}
```
