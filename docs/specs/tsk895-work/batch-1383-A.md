# tsk-895 — курс 1383, группа A — вычислительная проверка банка «Задание 5 ЕГЭ»

Метод: для каждого id — честная симуляция алгоритма на Python (проверена на иллюстративном
примере из условия), затем поиск ответа полным перебором N (или, где это доказуемо корректно,
конструктивным перебором эквивалентного меньшего пространства — см. id 2292) в границах, взятых
из условия задачи либо обоснованных масштабом искомого ответа. Все 23 вычисленных ответа сверены
с эталоном `solution_rules.short_answer.accepted_answers[].value` из
`D:\Work\LMS\docs\specs\tsk895-task5-dump.json`.

Итог: **23 из 23 совпадают**. Расхождений с эталоном в БД не найдено.

Один нюанс вскрылся в процессе: для id 3659 первая версия симулятора неверно трактовала вопрос
"минимальное R, большее 151" как "R в первом N, где R впервые превысило порог" — из-за
немонотонности R(N) это дало 166 вместо 163. Ошибка была не в понимании алгоритма (пример из
условия воспроизводился верно), а в трактовке вопроса — после исправления (полный перебор всех N,
глобальный минимум среди R>151) ответ совпал с эталоном. Оставляю это как предупреждение для
похожих формулировок «минимальное R, большее X» — они требуют минимума по R, а не по N.

## Таблица результатов

| id | title | признаки | пространство перебора | вычислено | эталон в БД | совпадает | заметки |
|---|---|---|---|---|---|---|---|
| 2292 | Минимальное N с результатом R 214 | функция R немонотонна по разбиению; конструктивная реконструкция битовой строки вместо перебора N (N слишком велико для честного перебора — см. заметки) | не перебор N (134 млн — неподъёмно), а перебор всех 7 точек разреза двоичной записи числа 214 на "count(1)"+"count(0)", для каждого разреза построен доказуемо минимальный N с нужным числом единиц/нулей, каждый кандидат перепроверен полной симуляцией R(N) | 134217759 | 134217759 | да | Перебор N напрямую невозможен (ответ ~1.3·10^8). Использован эквивалентный точный метод: т.к. R однозначно определяется двоичной записью числа 214, разрезов всего 7, каждый даёт единственный минимальный N (лидирующая 1, затем все нули, затем оставшиеся единицы) — это доказуемый минимум при фиксированном числе единиц/нулей и длине. Каждый из 7 кандидатов прогнан через реальную функцию R(N), несовпавшие отброшены, из совпавших взят минимальный |
| 2157 | Максимум простых двузначных чисел | построение максимального числа (по перестановкам цифр); подсчёт с разной трактовкой (с повторами/без) даёт одинаковый результат | полный перебор N=100..999 (900), для каждого — все 6 упорядоченных пар позиций цифр | 731 (макс. число простых = 6 из 6 возможных пар) | 731 | да | Проверены обе трактовки подсчёта простых чисел (с учётом кратности при повторяющихся цифрах и по множеству различных значений) — совпадают, т.к. у победителя все 3 цифры различны |
| 3665 | Максимальное число при вставке нуля | не двоичная и не десятичная система счисления (пятеричная); результат — не перевод в десятичную, а прямое чтение пятеричной записи как десятичной строки | полный перебор N=1..4999 | 74 | 74 | да | |
| 3680 | Количество чисел после дописывания цифр | построение через медианную (не макс/не мин) цифру трёхзначного числа | полный перебор N=100..999 с попарно различными цифрами (900) | 56 (чисел N, дающих целый R); 46 различных значений R | 56 | да | Вопрос трактован как "сколько N дают целый R" — совпало с эталоном. Также посчитано число различных значений R (46) на случай альтернативной трактовки — не понадобилось |
| 3643 | Минимальное число после дописывания битов | функция R(N) немонотонна — нужен полный перебор всех N, а не первого попавшегося | полный перебор N=1..19999, минимум среди всех R>213 | 216 | 216 | да | |
| 3645 | Число по разности двузначных чисел | построение макс./мин. двузначных чисел из цифр трёхзначного N | полный перебор N=300..400 (101) | 12 | 12 | да | |
| 3646 | Минимальное число с нулями в пятеричной записи | не двоичная и не десятичная (пятеричная); ветвление использует последнюю цифру ДЕСЯТИЧНОЙ записи N, а не пятеричной — тонкость, подтверждённая примером | полный перебор N=1..99999 | 50 | 50 | да | |
| 3647 | Минимальный результат двоичного преобразования | функция R(N) немонотонна, нужен полный перебор всех N≥26 в разумных границах | перебор N=26..26025, устойчив к расширению до 26..20025 (×10) | 26 | 26 | да | |
| 3650 | Дописка пятеричных цифр по делимости на 25 | не двоичная и не десятичная (пятеричная) | полный перебор N=1..199999 | 100 | 100 | да | |
| 3652 | Преобразование троичной записи с дублированием | не двоичная и не десятичная (троичная); замена цифр 0→1,1→2,2→0 перед дублированием | полный перебор N=1..99999 | 9 | 9 | да | |
| 3653 | Разные значения после удаления двух бит | операция эквивалентна R(N)=N div 4 (удаление 2 последних бит = целочисленное деление на 4) | полный перебор N=20..600 (581), подсчёт различных значений N//4 | 146 | 146 | да | |
| 3655 | Минимальное число после двоичного преобразования | функция R(N) немонотонна, нужен полный перебор всех N≥33 | перебор N=33..32032... фактически 33..2032, устойчив к расширению до 33..20032 (×10) | 65 | 65 | да | |
| 3658 | Переворот двоичной записи с допиской бита | построение через переворот двоичной записи + приписывание собственного крайнего бита | полный перебор N=1..99999, берётся первый (минимальный) N с R>99 | 39 | 39 | да | |
| 3662 | Троичное дописывание по количеству цифр | не двоичная и не десятичная (троичная); счётчики цифр 2/1/0 пересчитываются на РАСТУЩЕЙ строке (после каждой дописки), а не на исходной — подтверждено примером | полный перебор N=1..99999, максимум среди N с R<1000 | 35 | 35 | да | |
| 3663 | Преобразование числа в шестеричной системе | не двоичная и не десятичная (шестеричная); многошаговый алгоритм (замена нулей на 5 при count≥2, ветвление по чётности первой цифры: приписать остаток слева / приписать произведение цифр справа) | полный перебор N=1..4999, максимум среди N с R<109 | 102 | 102 | да | |
| 3728 | Кодирование числа по сумме цифр | не двоичная и не десятичная (восьмеричная); ветвление приписывает контрольную сумму то слева, то справа в зависимости от чётности | перебор N=483..20482, минимум R; устойчив к расширению до 483..200482 (×10) | 4106 | 4106 | да | |
| 3718 | Контрольная сумма трёхзначных чисел | два независимых входных числа (пара трёхзначных); контрольная сумма строится позиционной арифметикой (тысячи/сотни/десятки итогового числа), а не строковым срезом — важно из-за переменной длины сумм разрядов (1 или 2 цифры) | перебор A=999..100 (убывание) × B=100..999 для каждого A, с ранним выходом при первом найденном A | A=190 (свидетель B=100) | 190 | да | Контрольная сумма вычислена через целочисленную арифметику (div/mod по разрядам), а не конкатенацией строк — это критично, т.к. суммы разрядов сотен/десятков/единиц могут быть однозначными или двузначными, и от этого зависит итоговая длина числа |
| 3670 | Кодирование четырёхзначного числа по цифрам | построение числа из двух независимо вычисленных частей (квадрат суммы чётных цифр; куб разности макс/мин цифры), сортировка частей по возрастанию перед конкатенацией | полный перебор N=1000..9999 (9000), минимальный N с R=4343 | 1027 | 1027 | да | |
| 3714 | Преобразование числа в пятеричной системе | не двоичная и не десятичная (пятеричная) | перебор N=101..199999, первый (минимальный) N с R>1000 | 114 | 114 | да | |
| 3671 | Числа, делящиеся на свои цифры | не про R(N) вообще — чисто теоретико-числовая проверка (делимость N и перевёрнутой записи N на все их цифры), без построения новой записи в другой системе счисления | полный перебор N=100..999 без нулей в записи (900) | 16 | 16 | да | |
| 3673 | Преобразование числа через шестнадцатеричную запись | экзотическое основание (>12): шестнадцатеричная система, F — особая цифра (не инкрементируется) | полный перебор N=100..999 (трёхзначные десятичные), минимальный N с R=12 | 106 | 106 | да | |
| 3675 | Преобразование числа в троичной системе | не двоичная и не десятичная (троичная); асимметричное ветвление — чётная сумма цифр СОКРАЩАЕТ длину записи на 1 разряд (два левых разряда схлопываются в один "2"), нечётная сохраняет длину (заменяются на "20") — подтверждено обоими примерами из условия; двухуровневая минимизация (сначала минимальное R>75, затем минимальное N среди N, дающих это R) | полный перебор N=1..99999 | N=44 (даёт R=78, минимальное R>75) | 44 | да | Вопрос требует сначала найти минимальное ДОСТИЖИМОЕ значение R, большее 75 (не любое R>75), а затем минимальное N, дающее именно это R — не просто первое N с R>75 |

## Сводка по признакам (сколько раз встретился приём)

- Не двоичная и не десятичная система счисления: 3665 (5), 3646 (5), 3650 (5), 3652 (3), 3662 (3), 3663 (6), 3728 (8), 3714 (5), 3675 (3) — 9 заданий
- Экзотическое основание (>12): 3673 (шестнадцатеричная, 16)
- Функция R(N) немонотонна, обязателен полный перебор (не направленный поиск от "очевидного" места): 2292 (в комбинаторном пространстве разрезов), 3643, 3647, 3655, 3659, 3675 — не менее 6 заданий; на 3659 это стоило одной итерации исправления симулятора (см. врезку выше)
- Два независимых входных числа: 3718 (пара трёхзначных чисел, контрольная сумма)
- Построение макс./мин. числа из цифр: 2157, 3645
- Другое: построение через медиану цифр (не макс/мин) — 3680; счётчики цифр, пересчитываемые на растущей строке — 3662; операция, эквивалентная целочисленному делению — 3653; переворот двоичной записи с допиской собственного бита — 3658; ветвление, меняющее длину записи (схлопывание двух разрядов в один) — 3675; чисто теоретико-числовая задача без построения R в другой системе счисления — 3671; конкатенация двух независимо вычисленных величин с сортировкой по возрастанию — 3670

## Итог

Все 23 эталона в БД (`solution_rules.short_answer.accepted_answers[].value`) подтверждены честной
вычислительной симуляцией. Расхождений, требующих правки банка заданий по группе A курса 1383, не
найдено. Рабочий код симуляторов — `batch-1383-A-solvers.py` в этой же папке.
