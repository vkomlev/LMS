# Журнал ошибок AI-контуров

## Как использовать
1. Добавляйте запись при каждом значимом промахе/сбое.
2. Заполняйте `Класс ошибки` и `Серьезность`.
3. На еженедельном разборе переносите профилактику в правила/skills/workflows.

## Классы ошибок
- `SPEC`, `CONTEXT`, `LOGIC`, `INTEGRATION`, `DATA`, `TEST`, `SAFETY`, `COST`, `PROCESS`

## Серьезность
- `S1` критично
- `S2` высоко
- `S3` средне
- `S4` низко

## Шаблон записи
| Дата | Проект | Контекст | Симптом | Корневая причина | Класс | Severity | Как обнаружено | Исправление | Профилактика | Статус |
|---|---|---|---|---|---|---|---|---|---|---|
| 2026-02-27 | LMS | <task-context> | <symptom> | <root-cause> | LOGIC | S2 | smoke test | <fix> | <prevention> | done |
| 2026-03-03 | LMS | teacher help request detail | 500: `TypeError '<' not supported between 'str' and 'datetime.datetime'` | raw SQL/text date value compared with `now` without normalization/type-guard | DATA | S1 | runtime manual test + logs | normalize date via helper before compare; add service type-guards | update Cursor fastapi agents (normalization + negative tests + type-guards), add FastAPI-specific techlead review checks (raw SQL->types->now, runtime smoke detail/list, reproducer test) | done |
| 2026-04-28 | LMS / SPW | Y-1 auth API contract drift cross-repo | SPW Y-2 spec ссылался на `/auth/magic-link/request`, `GET /consume`, `/auth/logout`. Реализация Y-1: `POST /send`, `POST /verify`, `POST /auth/session/logout`. SPW клиент звал бы 404 на всех auth-путях | executor Y-1 переименовал роуты на идиоматичные глаголы (`/send`,`/verify`) и унифицировал `/session/*` префикс, не обновил Y-1 spec / LMS-0001 ADR / CB-0011 ADR. Y-2 spec писался от устаревших артефактов. Review-gate в первом проходе классифицировал drift как non-blocking, считая что нет downstream-потребителей (но Y-2 spec уже существовал в CB) | INTEGRATION | S2 | RCA после анализа Y-2 spec | путь A: LMS spec §6.1-6.5 + ADR-0001 §«Phase 1» обновлены под код; CB ADR-0011 / Y-1 spec-копия / Y-2 spec / brief обновлены | review-gate БЛОКИРУЕТ любые URL/method-расхождения в публичных API файлах (`app/api/v1/auth/*`, `me.py`, `embed_api.py`) — non-blocking классификация для них запрещена; spec обновлять в том же коммите что и роутер; cross-repo grep на ссылки старых путей перед merge; OpenAPI export diff vs spec markdown | done |
| 2026-04-28 #4 | LMS | Y-1.5.1 hotfix — orphan email → 500 вместо 409 | `get_or_create_user_by_vk` и `get_or_create_user_by_email` проверяли email overlap **только** через `identity_link`. Источник правды UNIQUE — partial index на `users.email`; identity_link — secondary mapping. Если `users.email=X` exists, но `identity_link kind='email' value=X` отсутствует (manual DELETE или race-corruption после успешного INSERT users + failed INSERT identity_link) — auto-create не видит overlap → INSERT users падает с UniqueViolationError внутри savepoint → re-raise → **500** в FastAPI. Воспроизведено в продакшене на `victor.v.komlev@gmail.com` через VK callback после magic-link first-time create | identity_link используется как primary check для overlap, но он secondary — partial UNIQUE index на users.email источник правды. Race-paths из B2 (orphan users) сами могут создавать orphan email-state. ADR-0021 §2 предписывает 409 для overlap, но реализация не покрывала случай orphan email-в-users без identity_link | INTEGRATION | S2 | live smoke MG-VK на стороне SPW (commit d022d77) | option A из handoff: дополнительный SELECT `users WHERE lower(email)=lower(input)` перед savepoint INSERT в обоих сервисах; raise `IdentityConflictError("email_already_linked_to_orphan_user", existing_kinds=[])` → router маппит на 409. `IdentityConflictError` вынесен в `app/services/auth/exceptions.py` для shared use. 2 regression теста. spec §6.2/§6.4 обновлены | (a) для каждой связи «main table UNIQUE constraint ↔ secondary mapping» при auto-create — проверять обе стороны, не полагаться только на secondary; (b) test matrix для auto-create включать orphan-state сценарий (UNIQUE на users.email exists, identity_link удалён); (c) при появлении новых identity_link kinds — проверять симметрию orphan-defense | FIXED |
| 2026-04-29 #1 | LMS | Y-3 backend execution (inline /executor-pro path) | Spec §8 «Tests» listed `test_streak_logic.py` с edge cases (gap=1, gap=2, today_active, single day) как обязательный — executor написал 4 других test files но НЕ test_streak_logic. Acceptance criterion §11 «/me/streak корректно считает streak с edge cases» прошёл по умолчанию (отсутствие failing-тестов трактовано как PASS). В Y-3.1 раунде edge tests добавлены — поймали критический bug в SQL формуле `d - rn*1d` для `ORDER BY d DESC` (math incorrect — каждый день в своей grp → streak всегда =1) | executor-pro inline path не проверил line-by-line spec §«Tests» список перед closure; SKILL.md не имеет явного шага «грепнуть spec на упомянутые test_*.py и подтвердить существование». **REPEAT** того же класса как 2026-04-28 #3 (`tests/test_y15_live_smoke.py` не создан вопреки tech-spec §16) — 2-й случай подряд для inline-executor path | test-gap + scope-violation | S2 (поймал реальный bug — streak SQL давал streak=1 вместо N для всех multi-day users) | Y-3.1 раунд /fastapi-api-developer добавил test_streak_logic.py с 8 edge cases + исправил SQL formula `d - rn*1d` → `d + rn*1d` для DESC | (a) `~/.claude/skills/executor-pro/SKILL.md` § Output Contract — добавить «Spec Test Coverage Audit: для каждого test_*.py упомянутого в spec §Tests/§Tests/Unit/§Tests/Integration — подтвердить существование в репо + статус pass/fail»; (b) **ESCALATION** в `/claude-booster` Режим A — повтор паттерна 2 раза подряд требует аудита /executor-pro и /fastapi-api-developer на «spec test list adherence»; (c) `~/.claude/skills/fastapi-api-developer/SKILL.md` Шаг 4 — добавить bullet «grep spec на конкретные имена test_*.py — все ли созданы и зелёные» | FIXED 2026-05-02 |
| 2026-04-29 #2 | LMS | LMS-side spec написание (Y-3 backend) | LMS-side spec §5.4 содержит SQL шаблон для streak gap-detection с буггованной формулой `d - rn*1d` для `ORDER BY d DESC` (математически неверно — каждый день в своей grp). Формула скопирована verbatim из CB authority spec §7.2.4 без semantic validation | tech-spec-composer/spec-writer paths не имеют шага «mentally trace SQL formula on 3-day input». Принят upstream-формулу как есть. CB authority spec тоже buggy — root cause в spec authoring, не в impl | api-drift (точнее: «specification-reuse-without-verification») | S2 (bug ушёл бы на прод без edge-тестов) | Y-3.1 fix в коде; spec backsync — Y-3.2 follow-up (S2-A1 в techlead-final review) | (a) `~/.claude/skills/tech-spec-composer/SKILL.md` или `~/.claude/skills/spec-writer/SKILL.md` Шаг «Анализ» — добавить «если spec включает raw SQL с window-функциями (ROW_NUMBER, gap-detection, рекурсивные CTE) — trace на 3-input примере до фиксации»; (b) cross-link в `~/.claude/skills/claude-booster/references/api-contract-rules.md` — раздел «SQL formula verification» | FIXED 2026-05-02 |
| 2026-04-29 #3 | LMS | Y-3 backend первый techlead-review проход (`reviews/2026-04-29-y3-techlead-review.md`) | Найден gap: «test_streak_logic.py отсутствует ... gap=1, gap=2 не покрыты» — классифицирован S3 medium (non-blocking). С учётом того, что отсутствие тестов укрыло реальный bug в SQL формуле (см. #1), severity должен был быть S2 (likely functional defect risk) | review-checklist.md «Testing Checks» не содержит явного правила: «если spec §«Tests» перечисляет required test files и они отсутствуют → S2, не S3» | test-gap (meta: severity-misclassification) | S3 (сам по себе review-defect), но привёл к S2 implication | Y-3.1 раунд закрыл gap | `~/.claude/skills/techlead-code-reviewer/references/testing-checks.md` — добавить новый раздел «Spec-Mandated Test Files»: «если spec явно перечисляет required test files (через §«Tests» / §«Test Coverage» секцию) и они отсутствуют — это S2, не S3, потому что spec author явно классифицировал их как обязательные для acceptance» | FIXED 2026-05-02 |
| 2026-04-28 #3 | LMS | Y-1.5 review-gate fail-then-pass | (1) `try/db.rollback()` для concurrent INSERT users откатывал основную tx → magic_link.consumed_at становился NULL → token re-usable (security regression); (2) аналогично TG/VK auto-create — orphan users при race на UNIQUE(kind,value); (3) `tests/test_y15_live_smoke.py` не создан вопреки tech-spec §16; (4) LMS spec §6.2-6.4 не обновлены под auto-create в same-commit | executor-pro прочёл tech-spec §5 «Atomic INSERT … ON CONFLICT DO NOTHING» но реализовал через try/rollback вместо savepoint/ON CONFLICT — anti-pattern для multi-step транзакций. §16 live smoke упущен из чек-листа (был только в §6.7 как 4 файла). LMS spec обновление пропущено: cross-project mirror был обновлён, LMS-собственный spec — нет (повтор паттерна 2026-04-28 #1) | LOGIC + PROCESS | S2 | review-gate 12 измерений (1-й проход) | savepoint pattern через `db.begin_nested()` (3 сервиса); `tests/test_b1_regression_consumed_at_persists_after_savepoint_rollback` reproduces race; `tests/test_y15_live_smoke.py` создан с CB_LMS_LIVE_SMOKE_Y15 gate; LMS spec §6.2-6.4 + шапка обновлены под Y-1.5 семантику | executor-pro чеклист после auto-create реализации: (a) для INSERT во время уже-открытой tx использовать `db.begin_nested()` или `INSERT … ON CONFLICT DO NOTHING`, никогда не `db.rollback()`; (b) tech-spec §«Live smoke» / §«Тесты» обоих читать как обязательные артефакты; (c) при изменении public response schema — grep на свой spec и обновить same-commit (повтор prevention 2026-04-28 #2) | FIXED | (1) Письмо не пришло из-за hardcoded URL `https://learn.victor-komlev.ru/auth/verify` без `/magic-link/consume` segment + RESEND_API_KEY=null в dev; (2) `MeResponse` ожидался как `{full_name, identities[]}` но реализация плоская `{id,email,tg_id,is_service}`; (3) `AuthTokenResponse` ожидался с вложенным `user`, реализация — только `{access_token,refresh_token,token_type}`; (4) frontend route paths в Y-2 spec ошибочно переименованы под API-имена в первом Path-A (SPW использует `/request`+`/consume` как frontend Next.js routes, API endpoints — `/send`+`/verify`) | Тот же класс что и #1: реализация Y-1 ушла от ранних черновиков ADR-0011/LMS-0001 (плоские схемы вместо вложенных user) и захардкодила prod-URL без env. Backsync-цикл к spec не выполнен. Дополнительно: при первом Path-A я слил frontend Next.js routes и LMS API endpoints в единое имя (`/verify`), не различив слои | INTEGRATION + SPEC | S2 | live smoke MG-1 (попытка реального входа через SPW) | LMS hotfix: `Settings.public_base_url` env + dev-fallback (логирование готовой ссылки в stdout если RESEND_API_KEY пуст). LMS spec §6.6 + §6.7: добавлены явные contracts MeResponse/AuthTokenResponse и URL-схема письма. LMS-0001 ADR строка 196: плоский CurrentUser. CB Y-2 spec: откат frontend route paths (`/request`+`/consume`). SPW commit `6877e04`: типы клиента под фактический контракт | (a) при любом изменении публичного response schema — обновить spec/ADR в том же PR (gate в review-gate); (b) preflight-проверка перед smoke: `grep "victor-komlev.ru" app/services/` — hardcoded prod-URL запрещён, всё через `settings.public_base_url`; (c) различать в spec **frontend Next.js routes** (UX-имена) и **API endpoints** (контракт между SPW и LMS) — отдельные таблицы; (d) skill `/executor-pro` обязан после реализации API сравнить response-модели с описанием в ADR/spec и эскалировать любое расхождение, не делать молчаливое упрощение | done |
| 2026-07-07 | LMS (cross-project: ContentBackbone) | Сквозной `/review-gate` по деплой-инфраструктуре Timeweb Cloud (LMS+SPW+TG_LMS+CB), запрошен через `TODOS.md` пункт «Провести общий /review-gate по всей деплой-инфраструктуре» | Найден реальный прод-пароль БД `cb_prod` + прод-IP, закоммиченный в `ContentBackbone/tests/test_content_hub_dsn_normalization.py` (коммит `bd56b00`, уже в `origin/main`). Полная запись и профилактика — в `ContentBackbone/docs/ai/ERRORS.md` (2026-07-07). Также найдено: `spw/.mcp.json` **трекается** в git и **не в `.gitignore`** (в отличие от LMS/CB, где `.mcp.json` игнорируется) — сейчас там только dev-заглушка, но нет защиты от повторения того же паттерна утечки, если туда добавят прод-DSN | В рамках tsk-005 (переезд LMS+SPW+TG_LMS на VPS) `.mcp.json` получил прод-подключения (`learn_prod_db`, `content_backbone_prod_db`) в LMS/CB после того, как оба файла были untracked+gitignored — сам этот процесс сработал верно для LMS/CB. Но параллельный SPW-репозиторий никогда не получал такой же gitignore-правки, и тестовый файл в CB был написан с реальным DSN вместо синтетического независимо от этого процесса | SAFETY | S1 | `/review-gate` дименсия 4: целевой `git log --all -p -S"<известный prod-маркер>"` по LMS и ContentBackbone (протокол задан в промпте ревью) | См. ContentBackbone ERRORS.md (ротация `cb_prod`, переписывание теста). Для SPW: добавить `.mcp.json` в `spw/.gitignore` и `git rm --cached .mcp.json`, аналогично коммиту `bb95a05` в LMS | Любой новый проект семьи (LMS/SPW/TG_LMS/CB/…) при первом добавлении `.mcp.json` с DB-подключением обязан сразу получить `.gitignore`-запись — не полагаться на то, что паттерн из одного проекта автоматически распространится на остальные. Чеклист `/project-docs`/`/db-check` при первой настройке MCP postgres — проверять gitignore на `.mcp.json` как обязательный шаг | done |

| 2026-07-17 | LMS | tsk-262 (вынесена из tsk-261): нормализация ответа-кода через AST. Класс найден при простановке флага `code_ast` — сработал контроль самого скрипта («code_ast и lower вместе бессмысленны») | Регулярка-классификатор `CODE_RX` в `scripts/fix_code_answer_lower_tsk261.py` молча теряла три из семи веток. Задания с эталоном `import random` / `import math` / `import turtle` (5468, 5617, 5748) не попали в фикс tsk-261 и **сохранили `lower`** — то есть `IMPORT RANDOM` продолжал засчитываться за `import random` на проде ещё сутки после «закрытия» жалобы QA | Регулярка написана в синтаксисе **Python**, но исполняется **PostgreSQL** (`a->>'value' ~ $1`). В PG ARE `\b` — это символ **backspace**, а не граница слова (граница — `\y`). Поэтому ветки `\bprint\b\|\bimport\b\|\bdef\b` не совпадали никогда, и задания ловились только по скобкам/точке/`=`/`[...]`. У голого `import X` ничего этого нет. Ошибка бесшумна: невалидного синтаксиса нет, PG просто не находит совпадений, и выборка молча оказывается уже задуманной | LOGIC (точнее: «regex-dialect-mismatch») | S2 (живой ложный зачёт на проде; тихий недоохват выборки — класс, который прячет масштаб правки) | Контроль внутри `scripts/set_code_ast_flag_tsk262.py` перед прод-записью (assert «у code_ast не должно быть lower») + прямая проверка в PG: `select 'import random' ~ '\bimport\b'` → false, `~ '\yimport\y'` → true | Данные исправлены `set_code_ast_flag_tsk262.py --apply` (52 задания, `lower` снят у всех, включая 3 пропущенных). Живая проверка на проде: `IMPORT RANDOM` → незачёт. Дефектные регулярки помечены комментарием в `fix_code_answer_lower_tsk261.py` и `measure_ast_normalization_tsk262.py` (не перезапускать, не копировать) | (a) **Регулярка, уезжающая в SQL, — не Python-регулярка.** Перед использованием проверять её В САМОЙ БД (`select '<пример>' ~ '<rx>'`), а не в голове и не в Python. Различия: `\b` (PG: backspace, граница — `\y`), lookbehind `(?<=)` не поддерживается, именованные группы `(?P<x>)` не поддерживаются, флаги `re.I/re.S` → `~*` или `(?i)/(?s)`. Совпадают: `\d`, `\s`, `\w`, `\r`, `\n`, `\.` — проверено на проде 2026-07-17. (b) Аудит проекта проведён: PG-регулярки есть **только** в разовых `scripts/` (6 штук), в боевом коде `app/` их нет вообще; из шести дефектна одна — `CODE_RX`. Соседние проекты (ContentBackbone, TG_LMS) чисты — их `\b` живут в `re.compile`, то есть Python-side. (c) **Не классифицировать домен регуляркой там, где есть разбор.** Для «код ли это» надёжна не регулярка, а `ast.parse` + признак «нет кириллицы в именах»: этот путь не зависит от диалекта БД вообще. (d) Осторожно и при проверке: в `grep -E` шаблон `\b` — тоже граница слова; искать литерал нужно `grep -F '\b'` (на этом сам аудит споткнулся с первого захода) | FIXED |
| 2026-07-17 (tsk-278) | LMS / ContentBackbone / TG_LMS | Follow-up tsk-262: исчерпывающий аудит класса «`\b` в PG-регексе» по трём проектам + добор остаточных данных | Мёртвая ветка `\bimport\b|\bdef\b|\bprint\b` из `fix_code_answer_lower_tsk261` оставила остаточный `lower` не только у 3 заданий с голым `import X` (их добрал tsk-262), но и у код-заданий с **неполным сниппетом-эталоном** (`while True:`, `for jivotnoe in spisok:`, `return rezultat`): AST-классификатор tsk-262 их отверг, т.к. `ast.parse` не разбирает неполный оператор → `lower` уцелел → ложный зачёт (`while true:` засчитан за `while True:`, хотя `true` — не Python) | Тот же корень, что у tsk-262 (regex-dialect-mismatch, `\b`=backspace в PG ARE). Новое: остаточные данные не покрылись ни регуляркой (мёртвая), ни AST (неполный сниппет не парсится) — попали в зазор между двумя подходами | LOGIC + DATA | S2 (живой ложный зачёт на 4 активных заданиях) | Кросс-сверка «файл содержит И PG-regex-оператор И `\b`» по всем 3 деревьям (read-only) + прод-запрос активных заданий с `lower` и код-эталоном | **Аудит регулярок: класс единичный, других PG-`\b` регулярок нет** — ни в LMS `app/`, ни в CB `monolith/external_tasks`+repos, ни в TG_LMS. Все прочие `\b` — Python `re` (HTML/PDF/текст-парсеры) или валидные PG-эскейпы (`\s`,`\.`,классы). **Данные:** снят `lower` у 5527/5613/5636/5768 через `scripts/fix_code_lower_incomplete_tsk278.py` (dry-run→apply, /db-check), верифицировано read-only. `code_ast` не добавлялся — по движку (`checking_service._matches_short_answer`) он инертен для непарсящихся сниппетов, чинит именно снятие `lower`. Спорные keyword-задания («назови слово маленькими буквами»: 8626/6124/8653 и elif/for/input) оставлены сознательно (решение оператора) | (a) **Guard НЕ заводим** (anti-bloat): находок `\b`-в-PG вне уже известной `CODE_RX` — ноль; grep-правило «`\b` в строке для PG» дало бы шумные ложные срабатывания на десятках легитимных Python-`re` с `\b` при нулевой ценности для единичного исторического случая. Записи в ERRORS достаточно. (b) Метод аудита на будущее: кандидат = файл, где PG-regex-оператор (`~`,`~*`,`!~`,`regexp_*`,`SIMILAR TO`) И литерал `\b` встречаются вместе; голый grep по `\b` их не различает — нужна кросс-сверка + глаза. (c) При правке нормализации сверять с движком оценки: `code_ast` — только доп. путь к зачёту, ужесточить проверку он не может, для неполных сниппетов инертен | FIXED |

## Чеклист weekly review
- Выгрузить все `open` + `in_progress`.
- Отдельно разобрать `S1/S2`.
- Обновить минимум 1 артефакт процесса (rule/skill/workflow) на повторяющиеся причины.
| 2026-07-22 (tsk-363) | LMS / TG_LMS | Боевая VK-регистрация на SPW: новый пользователь без почты (user_id=4505, 07:14:17 UTC) | Через минуту бот методиста перестал загружать список студентов: `GET /api/v1/users/?role=student` → 500 `Internal server error`; `role=methodist` → 200 (в том списке битой строки нет). Ломался бы любой список пользователей с этим студентом — админ-бот, преподаватель, SPW | VK userinfo вернул `email` **пустой строкой**, а не отсутствием ключа. Проверка `if email:` в `fetch_vk_userinfo` пропускала нормализацию (falsy → ветка не выполнялась), и `''` уходила в `users.email`. Схема ответа `UserRead.email: Optional[EmailStr]` пустую строку не принимает → падал **весь ответ списка**, а не одна запись. Вторая, не успевшая сработать мина: уникальный partial index `users_email_unique_partial (email) WHERE email IS NOT NULL` держал занятым слот `''` — следующая VK-регистрация без почты упала бы на конфликте уникальности | LOGIC + DATA | S1 (боевой отказ user-facing флоу) | Жалоба оператора → лог бота (запрос уходит, ответ 500) → лог LMS (`loc=('response','items',9,'email')`, `input=''`) → сверка с прод-БД read-only (ровно 1 строка с `email=''`) → аудит `user.registered.via_vk email_provided=False` | **Данные:** `/db-check` режим записи, `UPDATE users SET email = NULL WHERE id = 4505 AND email = ''` в транзакции (1 строка, верификация до/после). **Код:** `vk_oauth_service` — `email = (user.get("email") or "").strip().lower() or None`; `UserRead._blank_email_as_none` — валидатор гасит историческую пустоту в БД, чтобы одна битая строка не роняла весь список. Тесты `tests/test_vk_blank_email_tsk363.py` (8 шт., оба конца). Развёрнуто 374385f, живая проверка: student/teacher/methodist → 200, тело разобрано клиентской моделью бота | (a) **Falsy-проверка — не нормализация.** `if x:` перед `x.strip() or None` оставляет пустую строку нетронутой: нормализовать надо безусловно — `(x or "").strip() or None`. Класс шире почты: любое опциональное строковое поле от внешнего провайдера. (b) **Внешний провайдер отдаёт пустую строку, а не отсутствие ключа** — тесты на «поля нет» дефект не ловят: старый `test_first_time_vk_no_email_creates_user_email_null` подавал `email=None` напрямую в сервис, минуя разбор userinfo, где и был баг. Тестировать разбор ответа провайдера, а не только сервис за ним. (c) **Одна битая строка не должна ронять весь список** — при `EmailStr`/строгих типах в схеме выдачи поверх исторических данных нужна подстраховка на уровне схемы, иначе единичная запись даёт полный отказ эндпоинта. (d) Клиентская модель TG_LMS `UserRead.email` — тоже `Optional[EmailStr]`: пустая строка от сервера уронила бы и сам бот, страховка на сервере закрывает оба | FIXED |
| 2026-08-07 (tsk-574) | LMS | Прод 2026-08-06: скрипт пакетного назначения курса группе учеников отработал дважды | Первый прогон создал все пять связей, второй вернул пять `500 Internal server error`. Оператор не мог отличить «уже назначено» от отказа сервера и повторял запрос вслепую; выглядело как полный отказ. В логах — `asyncpg.UniqueViolationError: duplicate key value violates unique constraint "user_courses_pkey"` | `POST /api/v1/user-courses/` вставлял связь без перехвата `IntegrityError`: нарушение составного PK доходило до глобального обработчика `Exception` и превращалось в 500. При этом собственное описание эндпоинта с самого начала обещало 400 на дубликат — обещание не выполнялось никогда. **Рецидив класса 2026-04-28 #4** («orphan email → 500 вместо 409»): та же ошибка на другом эндпоинте, prevention-action прошлой записи была сформулирована узко — про пару «users.email ↔ identity_link», а не про класс «UNIQUE/PK из пользовательского ввода» | LOGIC + INTEGRATION | S2 (прод, user-facing операторский флоу; данные не пострадали — вставки не было) | Жалоба оператора + `logs/app.log` (`Unhandled exception at /api/v1/user-courses/`) | Перехват в `UserCoursesService.create` двумя слоями: ранняя проверка `get_by_keys` (до доназначения курсов-зависимостей) + перехват `IntegrityError` по SQLSTATE 23505 на гонке → `DomainError` 409 с телом `{"error": "domain_error", "detail": "Курс уже назначен этому ученику", "payload": {...}}`. Обязательный `db.rollback()` перед выбросом. Описание/`responses`/docstring/`docs/openapi.json`/`docs/API_STUDENTS_MANAGEMENT.md` приведены к 409. Второй путь (`assign_course_with_order`) отдавал 400 — расхождение убрано. Тесты `tests/test_user_courses_duplicate_tsk574.py` (штатный дубль + гонка) | (a) **Любая вставка по ключу из пользовательского ввода обязана маппить `IntegrityError` на 409** — не только пара «users.email ↔ identity_link» из записи 2026-04-28. При добавлении эндпоинта записи проверять: что будет при повторе того же тела? (b) **Обещание в описании эндпоинта — не реализация.** `responses={400: ...}` в декораторе не создаёт поведения; если код ответа заявлен, нужен тест, который его проверяет. (c) Ранняя проверка существования НЕ отменяет перехват `IntegrityError` — между ними гонка; и наоборот, перехват без ранней проверки делает бесполезные побочные вставки | FIXED |
| 2026-08-06 (tsk-303 Фаза 1) | LMS | Review-gate фазы 1 «лестница помощи»: CHECK на непустую вебинар-ссылку в миграции `tsk303_help_ladder` | Проверка «ссылка непустая», написанная как `length(btrim(webinar_link)) > 0`, пропускала строку из табуляции и перевода строки (`E'\t\n'`). До прода не дошло — поймано собственным тестом с набором `("", "   ", "\t\n")` | В PostgreSQL `btrim(string)` **без второго аргумента срезает только ПРОБЕЛЫ**, а не whitespace целиком (в отличие от `str.strip()` в Python, откуда и была перенесена интуиция). `length(btrim(E'\t\n'))` → 2, то есть условие `> 0` выполнялось. Класс тот же, что у 2026-07-17 (tsk-262/278) — «SQL-семантика ≠ интуиция из Python», но конкретная ловушка другая: не диалект регулярок, а умолчание аргумента функции | LOGIC | S3 (поймано до merge; дошло бы до прода — ученик получил бы кнопку «Перейти к разбору» в пустоту при формально отвеченной заявке) | Тест `tests/test_help_ladder_schema_tsk303.py::test_blank_webinar_link_rejected` (набор пустых значений включал табуляцию) + прямая проверка в БД: `SELECT length(btrim(E'\t\n'))` → 2, `SELECT E'\t\n' ~ '\S'` → false | Ограничение переписано на `webinar_link ~ '\S'` (есть хоть один непробельный символ) — проверено в самой БД до фиксации | (a) **«Непустая строка» в PG — это `~ '\S'`, а не `length(btrim(x)) > 0`.** Если всё же нужен btrim — задавать набор символов явно: `btrim(x, E' \t\r\n')`. (b) Тестовый набор «пустых» значений обязан включать НЕ ТОЛЬКО `''` и пробелы, но и табуляцию/перевод строки — иначе дефект этого класса тест не увидит. (c) Подтверждает и расширяет профилактику 2026-07-17: проверять в БД надо не только регулярки, но и умолчания строковых функций | FIXED |
| 2026-08-08 | LMS | tsk-396 — гибридный режим проверки; правится общий scoring-движок | SA_COM/TBL_COM с эталоном и `manual_review_required=true` засчитывался СРАЗУ на заведомо неверный ответ: `state=PASSED`, `score=max_score`, `is_correct=true` при `checked_at IS NULL`. Курс кредитовался до проверки человеком; обход tsk-395 не держал гейт, на который рассчитывал. 45 активных заданий прода | Условие оптимистичного зачёта (`attempts.py` 2.3d, tsk-210) опиралось на `check_result.is_correct is None`. У SA_COM/TBL_COM этот признак возникает по ДВУМ разным причинам: «эталона нет» (замысел) и «эталон есть, но включён ручной гейт» (tsk-230) — `_check_short_answer` короткозамыкает ДО чтения `short_answer`-правил. Вторая причина попала в условие непреднамеренно; комментарий рядом описывал ТОЛЬКО первую («SA_COM без правил») | LOGIC | S1 | Живой прогон пробы на dev (SA_COM + эталон «12 516,30», ответ «999 999») при сверке посылки задачи с контрактом `docs/frontend-contract-sa-com.md` — расхождение между тем, что задача считала намеренным поведением, и тем, что делает код | Условие переведено на признак «сверять нечем» — `not solution_rules.has_reference_answer()` (тот же единый предикат, что у UX-сигнала tsk-547). Миссии без эталона оптимистичный зачёт сохраняют; задания с эталоном + ручным гейтом больше не засчитываются вслепую. Регресс-тесты в `tests/test_partial_auto_check_tsk396.py` (обе стороны развилки) | (a) **Признак-триггер, вычисляемый несколькими путями, нельзя использовать как признак ОДНОГО из них.** `is_correct is None` означало и «нечем сверять», и «ждём человека» — условие обязано спрашивать о причине напрямую (`has_reference_answer()`), а не о её следствии. (b) Ранний `return` по флагу ДО чтения правил создаёт такие склейки — при добавлении флага в `checking_service` проверять, какие ещё ветки читают его следствия. (c) Юнит-тест на `CheckingService` (tsk-230 проверял `score=0`) НЕ ловит переопределение на слое роутера — для гейтов зачёта тест обязан идти через `POST /attempts/{id}/answers` и `GET /learning/tasks/{id}/state`, а не через сервис | FIXED |
| 2026-08-08 | LMS | tsk-586 — TG-боты не могли писать в LMS: legacy-дверь `get_api_key` принимала только `?api_key=` | TG_LMS с `8ceed6f` (tsk-497, 2026-08-03) перевёл клиент на заголовок `X-API-Key`, а `app/api/deps.py::get_api_key` читал ТОЛЬКО `APIKeyQuery`. С выката ботов 2026-08-07 12:53 UTC весь класс эндпоинтов на `Depends(get_db)` (~116 пар метод+путь) отвечал ботам 403, бот показывал «Недостаточно прав». Живёт бок о бок второй дефект: `hasattr` на незагруженной связи ORM в `repos/base.py::paginate` читал её и падал `MissingGreenlet` → 500 на `GET /courses/` и `GET /meta/tasks` | Транспорт авторизации поменяли на ОДНОЙ стороне (клиент), вторая осталась на старом контракте. Тесты TG_LMS мокают HTTP-клиент (329/329 зелёные при полностью сломанной авторизации), тесты LMS на этих путях ходили с query-ключом — ни один слой не проверял связку «транспорт клиента ↔ дверь сервера» | INTEGRATION | S1 | Жалоба оператора («в боте методиста невозможно привязать курс ученику») → curl с прод-сервера: те же пути заголовком 403, query 200 → обход всех маршрутов за `get_api_key` реальным ASGI-клиентом | `get_api_key` принимает заголовок И query (заголовок первым); `paginate`/`update` определяют тип через `isinstance`, а не `hasattr` | (a) **Смена транспорта авторизации — всегда двусторонняя правка**: правишь клиента — в том же охвате проверь дверь сервера, и наоборот. (b) Класс «отвалилась общая дверь» точечным тестом не ловится — нужен обход ВСЕХ маршрутов за этой дверью реальным ASGI-клиентом (`tests/test_service_key_header_gate_tsk586.py`), мок HTTP-клиента здесь бесполезен по определению. (c) `hasattr` на атрибуте-связи SQLAlchemy — не проверка наличия поля, а чтение: в async-сессии это lazy-load и `MissingGreenlet`; проверять тип через `isinstance` | FIXED |
| 2026-08-08 | LMS / TG_LMS / ContentBackbone | tsk-595 — ротация сервисных ключей после находки §6.4 в ревью tsk-586 | Считали, что утёк один ключ (27 строк в `app.log`). На деле открытым текстом лежали ВСЕ четыре ключа `VALID_API_KEYS`: 25 163 строки в `app.log` и ~83 000 в логах nginx, включая 77 431 строку с ключом ТГ-ботов, который по `app.log` выглядел чистым. Тем же разбором нашлись 12 строк `sudo … /usr/bin/env DATABASE_URL=… VALID_API_KEYS=… python …` в `auth.log` и журнале systemd — весь набор секретов LMS (пароль БД, ЮKassa, Resend, magic-link, ключ подписи сессий, Fernet, VK, S3, Redis, CloseRouter) | Маскировка tsk-496 — фильтр питоновского логгера LMS; access-лог nginx она не покрывает вовсе, а nginx пишет строку запроса целиком. Отдельно: секреты, переданные аргументами команды под `sudo`, попадают в аудит sudo и в `ps` | SECURITY | S1 | пересчёт по всем ключам при разборе §6.4 (проверяли только один) | Три новых ключа окном двойных ключей, старые четыре отозваны; клиенты CB и QSMImport переведены на `X-API-Key`; маскировка `api_key` добавлена в `log_format` nginx; 108 564 вхождения затёрты в файлах логов; журнал systemd обрезан до суток | Утечку секрета считать по ВСЕМ значениям переменной и по ВСЕМ приёмникам логов (приложение + nginx + auth.log + journal), а не по одному файлу; не передавать секреты аргументами команды (`sudo -u app bash -c 'set -a; . .env; set +a; …'`); ротация логов LMS подключена (`/etc/logrotate.d/lms`, `copytruncate`) | done |
| 2026-08-14 (tsk-611) | LMS | Локальная разработка: чистый прогон `pytest tests/` на машине оператора | 1 упавший + 25 ошибок. Выглядит как регрессия кода: 19 ошибок в `test_y5_guest_endpoints.py` и 6 в `test_tsk423_demo_task_limit.py` падают внутренностями драйвера (`OSError: Connect call failed ('127.0.0.1', 6379)`), а `test_session_refresh_http_race_tsk235.py` — несовпадением ожидания (`[200, 401, 401, 401, 401]` вместо пяти 200), то есть ровно как «сломали tsk-235». На доказательство обратного (изоляция файла → изоляция теста → чтение кода grace-окна) ушло полчаса | На локальной машине не был поднят Redis, а ни один из тестов об этом не сообщал. Глубже: Redis-совместимый сервер (Memurai) установлен и служба стоит на «Автоматически», но бесплатная Developer-редакция **сама себя выключает** после нескольких суток непрерывной работы (`Memurai Developer Edition automatic shutdown` в `memurai-log.txt`, 12.08) и обратно уже не поднимается. Симптом у третьего теста другой, потому что окно благодати на ротацию refresh пишется в Redis best-effort и fail-open: без Redis окна просто нет | TEST + PROCESS | S3 (только локальная разработка; прод здоров — там Redis внешний, соединение есть) | Независимая проверка круга 14.08 | Маркер `requires_redis` + однократная проверка PING на этапе сборки тестов (`tests/conftest.py`) — помеченные тесты **пропускаются с причиной**, а не падают драйвером. Скрипты `scripts/dev/`: `ensure-redis.ps1` (идемпотентно поднимает Redis: сперва служба, потом процесс пользователя без прав администратора), `run-tests.ps1` (проверка + прогон), `install-redis-autostart.ps1` (задача планировщика: вход в систему + перепроверка раз в 15 минут). Проверено обоими способами: с Redis — 2262 passed / 11 skipped / 0 failed / 0 errors; без Redis — 2234 passed / 39 skipped / 0 failed / 0 errors (26 бывших непрозрачных падений стали явными пропусками) | (a) **Тест, которому нужна внешняя служба, обязан проверять её сам и пропускаться с причиной.** Падение внутренностями драйвера читается как регрессия кода и стоит следующему человеку тех же получаса. (b) **Пропуск обязан быть виден в итоговой строке** — зелёный прогон, где четверть сотни тестов не выполнялась, вводит в заблуждение сильнее, чем красный. (c) **«Служба стоит на автозапуске» ≠ «служба работает»**: у Memurai Developer есть самовыключение по времени работы, поэтому нужна периодическая перепроверка, а не только автозапуск. (d) Симптом «тест ждал 200, получил 401» при fail-open зависимости — сигнал искать отсутствующую внешнюю службу, а не сломанную логику | FIXED |
| 2026-08-25 (tsk-669) | LMS | Общее рабочее дерево, несколько сессий (ADR-0008). pre-commit пересобирал `docs/openapi.json` из всего рабочего дерева и сам делал `git add` | Ночь на 25.08: три эпизода за час. Коммит одной сессии уносил незакоммиченную схему другой (tsk-655 утащил `RubricReview` у tsk-658; ad56b33 унёс схему tsk-665). Откат присвоенного файла `git checkout HEAD -- docs/openapi.json` дополнительно стёр регенерацию у пострадавшего | Источником схемы было рабочее дерево, а не то, что коммитится. **Рецидив tsk-495/tsk-461**: прошлая правка выбрала не блокирующее предупреждение и явно отклонила чистый снимок как избыточный. Предупреждение работало — и не помогало: текст проматывался, коммит проходил. Плюс хук жил только в `.git/hooks/` одной рабочей копии: его не было видно в ревью и он не приезжал в свежий клон | PROCESS | S2 (история коммитов, чужая работа; данные не пострадали) | Разбор состава трёх коммитов оператором | Хук переписан: пропуск, если в коммите нет `app/*.py`; сборка из снимка индекса (`git checkout-index --prefix`), результат кладётся в индекс через `hash-object --path` + `update-index`; рабочая копия не перезаписывается, если там своя версия; невозможность снимка при незастейдженных правках под `app/` **блокирует** коммит. Хук перенесён в репозиторий (`.githooks/` + `scripts/install_git_hooks.sh`, `core.hooksPath`), `.gitattributes` держит LF | (a) **Предупреждение, которое не останавливает, не считается защитой** — если правило важно, гейт обязан блокировать. (b) **Инструмент, влияющий на состав коммита, обязан жить в репозитории**, иначе починка не видна в ревью и живёт на одной машине. (c) Всё, что генерируется в pre-commit, собирается из индекса, а не из рабочего дерева — дерево общее | FIXED |
