# API документация: Управление связями преподаватель ↔ курс

**Версия API:** v1  
**Базовый URL:** `http://localhost:8000/api/v1`  
**Дата обновления:** 26 января 2026

> 📚 **См. также:** [API_TEACHERS_MANAGEMENT.md](./API_TEACHERS_MANAGEMENT.md) - Полная документация по управлению преподавателями (CRUD, поиск, фильтрация, привязка к студентам и курсам)

## Быстрая навигация

### Основные эндпойнты:

1. **Список преподавателей курса**
   - `GET /api/v1/courses/{course_id}/teachers` - получить всех преподавателей курса с пагинацией и сортировкой

2. **Список курсов преподавателя**
   - `GET /api/v1/users/{teacher_id}/courses` - получить все курсы преподавателя с пагинацией и сортировкой

3. **Привязка преподавателя к курсу**
   - `POST /api/v1/courses/{course_id}/teachers/{teacher_id}` - привязать преподавателя к курсу
   - `POST /api/v1/teacher-courses/` - создать связь (RESTful вариант)

4. **Отвязка преподавателя от курса**
   - `DELETE /api/v1/courses/{course_id}/teachers/{teacher_id}` - отвязать преподавателя от курса
   - `DELETE /api/v1/teacher-courses/{teacher_id}/{course_id}` - удалить связь (RESTful вариант)

5. **Список всех связей**
   - `GET /api/v1/teacher-courses/` - получить все связи с фильтрацией и сортировкой

## Содержание

1. [Аутентификация](#аутентификация)
2. [Эндпойнты для работы со списками](#эндпойнты-для-работы-со-списками)
3. [Эндпойнты для управления связями](#эндпойнты-для-управления-связями)
4. [RESTful эндпойнты](#restful-эндпойнты)
5. [Схемы данных](#схемы-данных)
6. [Коды ответов и ошибки](#коды-ответов-и-ошибки)
7. [Особенности работы с иерархией курсов](#особенности-работы-с-иерархией-курсов)
8. [Примеры использования](#примеры-использования)
9. [Часто задаваемые вопросы (FAQ)](#часто-задаваемые-вопросы-faq)

---

## Аутентификация

Все эндпойнты требуют API ключ в query-параметре `api_key`.

**Пример:**
```
GET /api/v1/courses/1/teachers?api_key=bot-key-1
```

**Доступные API ключи:** Настраиваются в переменной окружения `VALID_API_KEYS` (через запятую).

**Ошибка аутентификации:**
- **Код:** `403 Forbidden`
- **Ответ:** `{"detail": "Invalid or missing API Key"}`

---

## Эндпойнты для работы со списками

### 1. GET /api/v1/courses/{course_id}/teachers - Список преподавателей курса

**Описание:** Получить список всех преподавателей, привязанных к указанному курсу, с пагинацией и сортировкой.

**URL:** `/api/v1/courses/{course_id}/teachers`

**Метод:** `GET`

**Параметры пути:**

| Параметр | Тип | Обязательный | Описание | Примеры |
|----------|-----|--------------|----------|---------|
| `course_id` | `int` | Да | ID курса | `1`, `2`, `3` |

**Параметры запроса (все опциональны):**

| Параметр | Тип | Обязательный | По умолчанию | Описание | Примеры |
|----------|-----|--------------|--------------|----------|---------|
| `skip` | `int` | Нет | `0` | Смещение для пагинации (сколько записей пропустить) | `0`, `10`, `50` |
| `limit` | `int` | Нет | `50` | Максимум результатов на странице (от 1 до 200) | `10`, `50`, `100` |
| `sort_by` | `enum` | Нет | `linked_at` | Поле для сортировки: `linked_at`, `email`, `full_name` | `linked_at`, `email`, `full_name` |
| `order` | `enum` | Нет | `desc` | Направление сортировки: `asc` (по возрастанию) или `desc` (по убыванию) | `asc`, `desc` |
| `api_key` | `string` | Да | - | API ключ для аутентификации | `bot-key-1` |

**Особенности:**
- Сортировка по умолчанию: `linked_at DESC` (последние привязанные первыми)
- Поддерживается пагинация через `skip` и `limit`
- Если у курса нет привязанных преподавателей, возвращается пустой массив

**Примеры запросов:**

```bash
# Получить всех преподавателей курса (сортировка по умолчанию: linked_at DESC)
GET /api/v1/courses/1/teachers?api_key=bot-key-1

# Получить преподавателей, отсортированных по email
GET /api/v1/courses/1/teachers?sort_by=email&order=asc&api_key=bot-key-1

# Получить вторую страницу (по 10 записей)
GET /api/v1/courses/1/teachers?skip=10&limit=10&api_key=bot-key-1
```

**Пример успешного ответа (200 OK):**

```json
{
  "items": [
    {
      "id": 16,
      "email": "test_teacher_1@example.com",
      "full_name": "Преподаватель Тестовый 1",
      "tg_id": null,
      "created_at": "2026-01-26T14:21:50.253Z"
    }
  ],
  "meta": {
    "total": 1,
    "limit": 50,
    "offset": 0
  }
}
```

**Коды ответов:**

| Код | Описание | Причина |
|-----|----------|---------|
| `200` | Успешно | Список преподавателей получен (может быть пустым) |
| `403` | Forbidden | Неверный или отсутствующий API ключ |
| `404` | Not Found | Курс с указанным ID не найден |

---

### 2. GET /api/v1/users/{teacher_id}/courses - Список курсов преподавателя

**Описание:** Получить список всех курсов, привязанных к указанному преподавателю, с пагинацией и сортировкой.

**URL:** `/api/v1/users/{teacher_id}/courses`

**Метод:** `GET`

**Параметры пути:**

| Параметр | Тип | Обязательный | Описание | Примеры |
|----------|-----|--------------|----------|---------|
| `teacher_id` | `int` | Да | ID преподавателя | `16`, `17` |

**Параметры запроса (все опциональны):**

| Параметр | Тип | Обязательный | По умолчанию | Описание | Примеры |
|----------|-----|--------------|--------------|----------|---------|
| `skip` | `int` | Нет | `0` | Смещение для пагинации | `0`, `10`, `50` |
| `limit` | `int` | Нет | `50` | Максимум результатов на странице (от 1 до 200) | `10`, `50`, `100` |
| `sort_by` | `enum` | Нет | `linked_at` | Поле для сортировки: `linked_at`, `title`, `created_at` | `linked_at`, `title`, `created_at` |
| `order` | `enum` | Нет | `desc` | Направление сортировки: `asc` или `desc` | `asc`, `desc` |
| `api_key` | `string` | Да | - | API ключ для аутентификации | `bot-key-1` |

**Особенности:**
- Сортировка по умолчанию: `linked_at DESC` (последние привязанные первыми)
- Поддерживается пагинация
- Если у преподавателя нет привязанных курсов, возвращается пустой массив

**Примеры запросов:**

```bash
# Получить все курсы преподавателя
GET /api/v1/users/16/courses?api_key=bot-key-1

# Получить курсы, отсортированные по названию
GET /api/v1/users/16/courses?sort_by=title&order=asc&api_key=bot-key-1
```

**Пример успешного ответа (200 OK):**

```json
{
  "items": [
    {
      "id": 1,
      "title": "Основы Python",
      "access_level": "auto_check",
      "description": "Введение в Python",
      "parent_course_ids": [],
      "created_at": "2025-02-06T11:42:52.674613Z",
      "is_required": false,
      "course_uid": "COURSE-PY-01"
    }
  ],
  "meta": {
    "total": 1,
    "limit": 50,
    "offset": 0
  }
}
```

**Коды ответов:**

| Код | Описание | Причина |
|-----|----------|---------|
| `200` | Успешно | Список курсов получен (может быть пустым) |
| `403` | Forbidden | Неверный или отсутствующий API ключ |
| `404` | Not Found | Преподаватель с указанным ID не найден |

---

## Эндпойнты для управления связями

### 3. POST /api/v1/courses/{course_id}/teachers/{teacher_id} - Привязать преподавателя к курсу

**Описание:** Привязать преподавателя к курсу. При привязке к родительскому курсу автоматически привязываются все дочерние курсы.

**URL:** `/api/v1/courses/{course_id}/teachers/{teacher_id}`

**Метод:** `POST`

**Параметры пути:**

| Параметр | Тип | Обязательный | Описание | Примеры |
|----------|-----|--------------|----------|---------|
| `course_id` | `int` | Да | ID курса | `1`, `2`, `3` |
| `teacher_id` | `int` | Да | ID преподавателя | `16`, `17` |

**Параметры запроса:**

| Параметр | Тип | Обязательный | Описание | Примеры |
|----------|-----|--------------|----------|---------|
| `api_key` | `string` | Да | API ключ для аутентификации | `bot-key-1` |

**Особенности:**
- ⚠️ **ВАЖНО:** При привязке преподавателя к родительскому курсу, триггер БД автоматически привяжет всех детей курса
- Операция идемпотентна: повторный вызов с теми же параметрами не вызовет ошибку
- Оба объекта (преподаватель и курс) должны существовать в системе

**Пример запроса:**

```bash
POST /api/v1/courses/1/teachers/16?api_key=bot-key-1
```

**Пример успешного ответа (204 No Content):**

Тело ответа пустое.

**Коды ответов:**

| Код | Описание | Причина |
|-----|----------|---------|
| `204` | No Content | Связь успешно создана (или уже существовала) |
| `403` | Forbidden | Неверный или отсутствующий API ключ |
| `404` | Not Found | Преподаватель или курс не найден |

**Пример ошибки (404 Not Found):**

```json
{
  "detail": "Преподаватель с ID 16 не найден"
}
```

---

### 4. DELETE /api/v1/courses/{course_id}/teachers/{teacher_id} - Отвязать преподавателя от курса

**Описание:** Отвязать преподавателя от курса. При отвязке от родительского курса автоматически отвязываются все дочерние курсы.

**URL:** `/api/v1/courses/{course_id}/teachers/{teacher_id}`

**Метод:** `DELETE`

**Параметры пути:**

| Параметр | Тип | Обязательный | Описание | Примеры |
|----------|-----|--------------|----------|---------|
| `course_id` | `int` | Да | ID курса | `1`, `2`, `3` |
| `teacher_id` | `int` | Да | ID преподавателя | `16`, `17` |

**Параметры запроса:**

| Параметр | Тип | Обязательный | Описание | Примеры |
|----------|-----|--------------|----------|---------|
| `api_key` | `string` | Да | API ключ для аутентификации | `bot-key-1` |

**Особенности:**
- ⚠️ **ВАЖНО:** При отвязке преподавателя от родительского курса, триггер БД автоматически отвяжет всех детей курса
- Операция идемпотентна: если связи не было, возвращается 204 без ошибки

**Пример запроса:**

```bash
DELETE /api/v1/courses/1/teachers/16?api_key=bot-key-1
```

**Пример успешного ответа (204 No Content):**

Тело ответа пустое.

**Коды ответов:**

| Код | Описание | Причина |
|-----|----------|---------|
| `204` | No Content | Связь успешно удалена (или не существовала) |
| `403` | Forbidden | Неверный или отсутствующий API ключ |

---

## RESTful эндпойнты

### 5. GET /api/v1/teacher-courses/ - Список всех связей

**Описание:** Получить список всех связей преподавателей с курсами с пагинацией, фильтрацией и сортировкой.

**URL:** `/api/v1/teacher-courses/`

**Метод:** `GET`

**Параметры запроса (все опциональны):**

| Параметр | Тип | Обязательный | По умолчанию | Описание | Примеры |
|----------|-----|--------------|--------------|----------|---------|
| `teacher_id` | `int` | Нет | `null` | Фильтр по ID преподавателя | `16`, `17` |
| `course_id` | `int` | Нет | `null` | Фильтр по ID курса | `1`, `2`, `3` |
| `skip` | `int` | Нет | `0` | Смещение для пагинации | `0`, `10`, `50` |
| `limit` | `int` | Нет | `50` | Максимум результатов на странице (от 1 до 200) | `10`, `50`, `100` |
| `sort_by` | `enum` | Нет | `linked_at` | Поле для сортировки: `linked_at`, `teacher_id`, `course_id` | `linked_at`, `teacher_id`, `course_id` |
| `order` | `enum` | Нет | `desc` | Направление сортировки: `asc` или `desc` | `asc`, `desc` |
| `api_key` | `string` | Да | - | API ключ для аутентификации | `bot-key-1` |

**Особенности:**
- Можно фильтровать по `teacher_id` и/или `course_id`
- Поддерживается пагинация и сортировка
- Если связей нет, возвращается пустой массив

**Примеры запросов:**

```bash
# Получить все связи
GET /api/v1/teacher-courses/?api_key=bot-key-1

# Получить все связи для конкретного преподавателя
GET /api/v1/teacher-courses/?teacher_id=16&api_key=bot-key-1

# Получить все связи для конкретного курса
GET /api/v1/teacher-courses/?course_id=1&api_key=bot-key-1

# Комбинированная фильтрация и сортировка
GET /api/v1/teacher-courses/?teacher_id=16&sort_by=linked_at&order=asc&limit=10&api_key=bot-key-1
```

**Пример успешного ответа (200 OK):**

```json
{
  "items": [
    {
      "teacher_id": 16,
      "course_id": 1,
      "linked_at": "2026-01-26T14:21:50.221Z"
    }
  ],
  "meta": {
    "total": 1,
    "limit": 50,
    "offset": 0
  }
}
```

**Коды ответов:**

| Код | Описание | Причина |
|-----|----------|---------|
| `200` | Успешно | Список связей получен (может быть пустым) |
| `403` | Forbidden | Неверный или отсутствующий API ключ |

---

### 6. POST /api/v1/teacher-courses/ - Создать связь (RESTful)

**Описание:** Создать связь между преподавателем и курсом через RESTful API.

**URL:** `/api/v1/teacher-courses/`

**Метод:** `POST`

**Параметры запроса:**

| Параметр | Тип | Обязательный | Описание | Примеры |
|----------|-----|--------------|----------|---------|
| `api_key` | `string` | Да | API ключ для аутентификации | `bot-key-1` |

**Тело запроса (JSON):**

| Поле | Тип | Обязательный | Описание | Примеры |
|------|-----|--------------|----------|---------|
| `teacher_id` | `int` | Да | ID преподавателя | `16`, `17` |
| `course_id` | `int` | Да | ID курса | `1`, `2`, `3` |

**Особенности:**
- ⚠️ **ВАЖНО:** При привязке преподавателя к родительскому курсу, триггер БД автоматически привяжет всех детей курса
- Операция идемпотентна: если связь уже существует, возвращается существующая связь
- Оба объекта должны существовать в системе

**Пример запроса:**

```bash
POST /api/v1/teacher-courses/?api_key=bot-key-1
Content-Type: application/json

{
  "teacher_id": 16,
  "course_id": 1
}
```

**Пример успешного ответа (201 Created):**

```json
{
  "teacher_id": 16,
  "course_id": 1,
  "linked_at": "2026-01-26T14:21:50.221Z"
}
```

**Коды ответов:**

| Код | Описание | Причина |
|-----|----------|---------|
| `201` | Created | Связь успешно создана |
| `403` | Forbidden | Неверный или отсутствующий API ключ |
| `404` | Not Found | Преподаватель или курс не найден |

**Пример ошибки (404 Not Found):**

```json
{
  "detail": "Преподаватель с ID 16 не найден"
}
```

---

### 7. DELETE /api/v1/teacher-courses/{teacher_id}/{course_id} - Удалить связь (RESTful)

**Описание:** Удалить связь между преподавателем и курсом через RESTful API.

**URL:** `/api/v1/teacher-courses/{teacher_id}/{course_id}`

**Метод:** `DELETE`

**Параметры пути:**

| Параметр | Тип | Обязательный | Описание | Примеры |
|----------|-----|--------------|----------|---------|
| `teacher_id` | `int` | Да | ID преподавателя | `16`, `17` |
| `course_id` | `int` | Да | ID курса | `1`, `2`, `3` |

**Параметры запроса:**

| Параметр | Тип | Обязательный | Описание | Примеры |
|----------|-----|--------------|----------|---------|
| `api_key` | `string` | Да | API ключ для аутентификации | `bot-key-1` |

**Особенности:**
- ⚠️ **ВАЖНО:** При отвязке преподавателя от родительского курса, триггер БД автоматически отвяжет всех детей курса
- Операция идемпотентна: если связи не было, возвращается 204 без ошибки

**Пример запроса:**

```bash
DELETE /api/v1/teacher-courses/16/1?api_key=bot-key-1
```

**Пример успешного ответа (204 No Content):**

Тело ответа пустое.

**Коды ответов:**

| Код | Описание | Причина |
|-----|----------|---------|
| `204` | No Content | Связь успешно удалена (или не существовала) |
| `403` | Forbidden | Неверный или отсутствующий API ключ |

---

## Схемы данных

### TeacherCourseCreate

Схема для создания связи преподаватель ↔ курс.

```json
{
  "teacher_id": 16,
  "course_id": 1
}
```

**Поля:**

| Поле | Тип | Обязательный | Описание |
|------|-----|--------------|----------|
| `teacher_id` | `int` | Да | ID преподавателя |
| `course_id` | `int` | Да | ID курса |

### TeacherCourseRead

Схема для чтения связи преподаватель ↔ курс.

```json
{
  "teacher_id": 16,
  "course_id": 1,
  "linked_at": "2026-01-26T14:21:50.221Z"
}
```

**Поля:**

| Поле | Тип | Обязательный | Описание |
|------|-----|--------------|----------|
| `teacher_id` | `int` | Да | ID преподавателя |
| `course_id` | `int` | Да | ID курса |
| `linked_at` | `datetime` | Да | Дата и время привязки (ISO 8601) |

### Page[T]

Общая схема для пагинированных ответов.

```json
{
  "items": [...],
  "meta": {
    "total": 100,
    "limit": 50,
    "offset": 0
  }
}
```

**Поля:**

| Поле | Тип | Описание |
|------|-----|----------|
| `items` | `Array[T]` | Массив элементов текущей страницы |
| `meta.total` | `int` | Общее количество элементов |
| `meta.limit` | `int` | Максимальное количество элементов на странице |
| `meta.offset` | `int` | Смещение (количество пропущенных элементов) |

---

## Коды ответов и ошибки

### Успешные ответы

| Код | Описание | Когда используется |
|-----|----------|-------------------|
| `200` | OK | Успешное получение списка |
| `201` | Created | Успешное создание связи (RESTful POST) |
| `204` | No Content | Успешное создание/удаление связи (без тела ответа) |

### Ошибки клиента

| Код | Описание | Причина | Пример ответа |
|-----|----------|---------|---------------|
| `400` | Bad Request | Некорректные параметры запроса | `{"detail": "Invalid parameter value"}` |
| `403` | Forbidden | Неверный или отсутствующий API ключ | `{"detail": "Invalid or missing API Key"}` |
| `404` | Not Found | Преподаватель или курс не найден | `{"detail": "Преподаватель с ID 16 не найден"}` |

### Ошибки сервера

| Код | Описание | Причина |
|-----|----------|---------|
| `500` | Internal Server Error | Внутренняя ошибка сервера |

---

## Особенности работы с иерархией курсов

### ⚠️ Автоматическая привязка дочерних курсов

**Важно:** При привязке преподавателя к родительскому курсу, триггер БД автоматически привяжет всех дочерних курсов (прямых и косвенных потомков).

**Пример:**
```
Курс 1 (родитель)
  ├── Курс 2 (ребенок)
  ├── Курс 3 (ребенок)
  │   └── Курс 4 (внук)
  └── Курс 5 (ребенок)
```

При привязке преподавателя к курсу 1, автоматически привязываются курсы 2, 3, 4, 5.

**Ограничение:** Преподаватель не может быть привязан напрямую к дочернему курсу без привязки к родителю. Это ограничение реализовано на уровне БД.

### Автоматическая отвязка дочерних курсов

При отвязке преподавателя от родительского курса, триггер БД автоматически отвяжет всех дочерних курсов.

### Синхронизация при изменении иерархии

При изменении иерархии курсов (добавление/удаление связей родитель-ребенок), триггеры БД автоматически синхронизируют связи преподавателей:

- **При добавлении ребенка к родителю:** Если преподаватель привязан к родителю, он автоматически привязывается к новому ребенку
- **При удалении ребенка из иерархии:** ⚠️ **ТЕХНИЧЕСКИЙ ДОЛГ:** Логика синхронизации реализована в коде API (`TeacherCoursesRepository.sync_on_child_removed`) из-за ограничений PostgreSQL. См. `docs/database-triggers-contract.md`

---

## Примеры использования

### Пример 1: Привязать преподавателя к курсу и получить список

```bash
# 1. Привязать преподавателя к родительскому курсу
POST /api/v1/courses/1/teachers/16?api_key=bot-key-1

# 2. Получить список преподавателей курса
GET /api/v1/courses/1/teachers?api_key=bot-key-1

# 3. Получить список курсов преподавателя (включая автоматически привязанных детей)
GET /api/v1/users/16/courses?api_key=bot-key-1
```

### Пример 2: Использование RESTful эндпойнтов

```bash
# 1. Создать связь через RESTful API
POST /api/v1/teacher-courses/?api_key=bot-key-1
Content-Type: application/json

{
  "teacher_id": 16,
  "course_id": 1
}

# 2. Получить все связи с фильтрацией
GET /api/v1/teacher-courses/?teacher_id=16&sort_by=linked_at&order=desc&api_key=bot-key-1

# 3. Удалить связь через RESTful API
DELETE /api/v1/teacher-courses/16/1?api_key=bot-key-1
```

### Пример 3: Пагинация и сортировка

```bash
# Получить первую страницу (10 записей), отсортированных по email
GET /api/v1/courses/1/teachers?skip=0&limit=10&sort_by=email&order=asc&api_key=bot-key-1

# Получить вторую страницу
GET /api/v1/courses/1/teachers?skip=10&limit=10&sort_by=email&order=asc&api_key=bot-key-1
```

---

## Часто задаваемые вопросы (FAQ)

### 1. Что происходит при привязке преподавателя к родительскому курсу?

При привязке преподавателя к родительскому курсу, триггер БД автоматически привязывает всех дочерних курсов (прямых и косвенных потомков) к этому преподавателю. Это происходит автоматически на уровне БД, без необходимости дополнительных вызовов API.

### 2. Можно ли привязать преподавателя напрямую к дочернему курсу?

Нет. Преподаватель не может быть привязан напрямую к дочернему курсу без привязки к родителю. Это ограничение реализовано на уровне БД для обеспечения целостности данных.

### 3. Что происходит при удалении курса?

При удалении курса все связи преподавателей с этим курсом автоматически удаляются через CASCADE (Foreign Key constraint). Это происходит на уровне БД.

### 4. Что происходит при изменении иерархии курсов?

При изменении иерархии курсов (добавление/удаление связей родитель-ребенок), триггеры БД автоматически синхронизируют связи преподавателей:

- При добавлении ребенка к родителю: если преподаватель привязан к родителю, он автоматически привязывается к новому ребенку
- При удалении ребенка из иерархии: логика синхронизации реализована в коде API (технический долг)

### 5. Операции идемпотентны?

Да, все операции создания и удаления связей идемпотентны:
- Повторный вызов `POST` с теми же параметрами не вызовет ошибку
- Вызов `DELETE` для несуществующей связи вернет 204 без ошибки

### 6. Как работает пагинация?

Пагинация работает через параметры `skip` (смещение) и `limit` (количество записей на странице). В ответе возвращается объект `Page[T]` с полями:
- `items` - массив элементов текущей страницы
- `meta.total` - общее количество элементов
- `meta.limit` - максимальное количество элементов на странице
- `meta.offset` - смещение (равно `skip`)

### 7. Какие поля доступны для сортировки?

**Для списка преподавателей курса (`GET /courses/{course_id}/teachers`):**
- `linked_at` - по дате привязки (по умолчанию)
- `email` - по email преподавателя
- `full_name` - по ФИО преподавателя

**Для списка курсов преподавателя (`GET /users/{teacher_id}/courses`):**
- `linked_at` - по дате привязки (по умолчанию)
- `title` - по названию курса
- `created_at` - по дате создания курса

**Для списка всех связей (`GET /teacher-courses/`):**
- `linked_at` - по дате привязки (по умолчанию)
- `teacher_id` - по ID преподавателя
- `course_id` - по ID курса

### 8. Где можно получить дополнительную информацию?

- **Swagger UI:** `http://localhost:8000/docs` - интерактивная документация API
- **Контракт триггеров:** `docs/database-triggers-contract.md` - описание логики, реализованной на уровне БД
- **План реализации:** `docs/PLAN_TEACHER_COURSES_IMPLEMENTATION.md` - подробный план реализации функционала

---

## Технические детали

### Реализация на уровне БД

Большая часть логики автоматической привязки/отвязки дочерних курсов реализована на уровне БД через триггеры PostgreSQL:

- **Триггер `trg_auto_link_teacher_course_children`:** Автоматически привязывает всех детей при привязке преподавателя к родительскому курсу
- **Триггер `trg_auto_unlink_teacher_course_children`:** Автоматически отвязывает всех детей при отвязке преподавателя от родительского курса
- **Триггер `trg_sync_teacher_courses_on_child_added`:** Синхронизирует связи при добавлении ребенка к родителю
- **Триггер `trg_sync_teacher_courses_on_child_removed`:** ⚠️ **ОТКЛЮЧЕН** - логика перенесена в код API из-за ограничений PostgreSQL

Подробнее см. `docs/database-triggers-contract.md`.

### Технический долг

Логика синхронизации при удалении ребенка из иерархии реализована в коде API (`TeacherCoursesRepository.sync_on_child_removed`) из-за ограничений PostgreSQL на модификацию таблицы в `AFTER DELETE` триггере. Этот метод вызывается явно из `CoursesRepository.set_parent_courses()` при удалении связей родитель-ребенок.

---

## Связанная документация

- **[API_TEACHERS_MANAGEMENT.md](./API_TEACHERS_MANAGEMENT.md)** - Полная документация по управлению преподавателями (CRUD, поиск, фильтрация, привязка к студентам и курсам)
- **[API_STUDENTS_MANAGEMENT.md](./API_STUDENTS_MANAGEMENT.md)** - Документация по управлению студентами
- **[database-triggers-contract.md](./database-triggers-contract.md)** - Контракт по триггерам БД

---

**Дата последнего обновления:** 26 января 2026
