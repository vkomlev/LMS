# TODOS.md — LMS

Открытые задачи-кандидаты, отложенные с ревью планов (`/eng-review`, `/ceo-review`),
не блокирующие текущую работу, но важные для будущих решений.

## OPEN

### Перевод 5 TG-ботов с long polling на webhook
- **Что:** TG_LMS сейчас — 5 отдельных процессов (`main_bots_admin/methodist/teacher/student/marketer`)
  на long polling к Telegram. Перевод на webhook сделает их обычным HTTP-сервисом.
- **Почему:** откроет возможность деплоя ботов на App Platform вместо отдельного VDS, если
  нагрузка на боты вырастет и понадобится авто-масштабирование/меньше ручной эксплуатации.
- **Плюсы/минусы:** плюс — единая PaaS-инфраструктура для всех сервисов, меньше ручного
  сисадминства (systemd, патчи ОС); минус — рефакторинг entrypoint'ов 5 ботов, нужен публичный
  HTTPS-эндпоинт с валидным SSL под webhook (Telegram требует HTTPS).
- **Контекст:** решение принято 2026-07-04 при ревью деплой-стратегии на Timeweb Cloud
  (`docs/briefs/deploy-timeweb-cloud.md`, tsk-005) — на первом этапе оставили long polling +
  VDS + systemd, чтобы не расширять охват первого деплоя.
- **Зависит от / блокируется:** ничем не блокируется; целесообразно делать, когда VDS с ботами
  станет узким местом по ресурсам или потребуется горизонтальное масштабирование.

### Включение автодеплоя App Platform по push в main
- **Что:** сейчас (первые недели после переноса) — ручной триггер деплоя LMS/SPW в панели
  Timeweb App Platform. Автодеплой при пуше в main пока выключен.
- **Почему:** LMS в активной разработке (Phase Y-1, миграции в процессе); автодеплой на каждый
  merge рискован без промежуточного staging-гейта, пока процесс переноса не устоялся.
- **Плюсы/минусы:** плюс автодеплоя — не забыть передеплоить после merge, быстрее итерации;
  минус — merge в main сразу уезжает на живых учеников без ручной паузы для проверки.
- **Контекст:** решение принято 2026-07-04 при ревью деплой-стратегии (tsk-005,
  `docs/briefs/deploy-timeweb-cloud.md`). Переключить на авто, когда Phase Y-1 стабилизируется
  и/или появится отдельный staging-контур.
- **Зависит от / блокируется:** желательно сначала завершить Phase Y-1 (см. tech-spec
  `docs/specs/2026-04-27-tech-spec-Y1-auth-extension.md`).

### Исследование переноса приложения ContentBackbone (пайплайны) на сервер
- **Что:** сейчас переезжает на Timeweb только БД `content_backbone`; сами пайплайны
  ContentBackbone остаются локальными на машине Виктора и подключаются к prod-БД по
  публичному DBaaS-адресу.
- **Почему:** если объём контентных пайплайнов вырастет (больше источников, больше нагрузка на
  локальную машину), может понадобиться перенос самого приложения на сервер — но сейчас нет
  ясности по требованиям к ресурсам (CPU/память под LLM-вызовы, зависимости).
- **Плюсы/минусы:** плюс — не зависеть от того, включён ли ноутбук Виктора; минус — отдельная
  оценка ресурсов и стоимости, отдельный цикл переноса (не тривиальный, как БД).
- **Контекст:** решение принято 2026-07-04 при ревью деплой-стратегии (tsk-005,
  `docs/briefs/deploy-timeweb-cloud.md`). Явно вне охвата текущего деплоя.
- **Зависит от / блокируется:** ничем; триггер — рост нагрузки на локальный пайплайн
  ContentBackbone.

### Переключить LMS на VPC-доступ к DBaaS и балансировщикам, когда Timeweb добавит поддержку для Apps
- **Что:** сейчас App Platform не поддерживает подключение к приватной сети (VPC) — только
  публичный IP. Как только Timeweb добавит поддержку VPC для Apps — переключить (1)
  `DATABASE_URL`/`REDIS_URL` LMS с публичного адреса DBaaS на приватный, и (2) связь
  балансировщик↔приложение (LMS/SPW) с публичного IP приложения на приватный.
- **Почему:** приватная сеть убирает публичную поверхность атаки и не требует платных
  публичных IP (~150 руб/мес за адрес, уточнить точную цену в панели) — ни у БД, ни у самих
  приложений LMS/SPW.
- **Плюсы/минусы:** плюс — безопаснее и дешевле (меньше платных публичных IP); минус —
  требует смены конфигурации и редеплоя, зависит от таймлайна самого Timeweb (не под нашим
  контролем).
- **Контекст:** решение принято 2026-07-04 при ревью деплой-стратегии на Timeweb Cloud
  (tsk-005, `docs/briefs/deploy-timeweb-cloud.md`) — на момент ревью документация Timeweb
  явно указывала "приватные сети для Apps — план на будущее". Уточнено 2026-07-04 при вопросе
  про отключение платного IPv4 у App Platform-приложений: документация `/docs/public-ip`
  явно перечисляет поддерживаемые сервисы («облачными серверами, базами данных,
  балансировщиками нагрузки, а также приватными сетями») — App Platform в списке нет, то есть
  публичный IP приложения сейчас нельзя ни отвязать, ни заменить приватным без риска разрыва
  связи балансировщик↔приложение.
- **Зависит от / блокируется:** зависит от Timeweb (внешний таймлайн, не отслеживаемый нами
  напрямую) — периодически проверять `https://timeweb.cloud/docs/apps` на предмет апдейта.

### Перенести CAS-хранилище медиафайлов (tsk-110/ADR-0040) на S3, а не общий диск
- **Что:** `CAS_MEDIA_ROOT` (LMS) и CB-downloader сейчас рассчитаны на общий локальный путь
  на одной машине (`ADR-0040`, `docs/specs/2026-06-04-plan-tsk110-adr0040-media-serving.md`).
  После переноса LMS на App Platform это допущение сломано: LMS-контейнер не видит локальный
  диск Виктора, где остаётся ContentBackbone.
- **Почему:** без общего хранилища эндпоинт `GET /api/v1/media/{sha}.{ext}` в проде отдаёт 404
  на все файлы — фича tsk-110 фактически не работает в production, пока не появится общее
  сетевое хранилище (например, Timeweb S3-совместимое: CB заливает туда, LMS либо проксирует,
  либо редиректит на публичный/presigned URL).
- **Плюсы/минусы:** плюс — рабочая медиа-фича в проде; минус — доработка кода в 2 репозиториях
  (CB downloader/cas.py, LMS app/api/v1/media.py), новый ADR поверх ADR-0040.
- **Контекст:** обнаружено 2026-07-04 при первом реальном заполнении переменных LMS на
  App Platform (tsk-005). На момент обнаружения решено: не блокировать деплой LMS — оставить
  `CAS_MEDIA_ROOT` неустановленным (дефолт из `app/core/config.py`), эндпоинт media деградирует
  до 404, ядро LMS (логин, задания, попытки) не затронуто.
- **Зависит от / блокируется:** блокирует полноценный re-import задач с картинками (см. G1-G3 в
  `docs/specs/2026-06-04-plan-tsk110-adr0040-media-serving.md`) — но это уже было открытым
  пробелом независимо от деплоя.

### Оценить timeweb-cloud/mcp-server для создания новой инфраструктуры
- **Что:** официальный MCP-сервер Timeweb Cloud (`github.com/timeweb-cloud/mcp-server`, npx
  `timeweb-mcp-server`) — 11 инструментов: создание App Platform-приложений с автоопределением
  параметров, подключение VCS-провайдеров, создание floating IP, VPC, БД. Требует `TIMEWEB_TOKEN`.
- **Почему:** избавит от ручных проходов оператора по панели при создании НОВЫХ ресурсов —
  Claude сможет создавать их напрямую через API вместо инструкций по скриншотам.
- **Плюсы/минусы:** плюс — быстрее и без ручных шагов оператора для create-сценариев; минус —
  сервер экспериментальный (сам себя так называет), не умеет управлять уже существующими
  ресурсами (нет балансировщиков, доменов/DNS, update/delete для apps и серверов — только
  создание с нуля), не видит `.env` проекта (переменные всё равно вписывать руками), нужен
  API-токен как секрет в конфиге MCP.
- **Контекст:** найдено 2026-07-04 во время деплоя LMS/SPW/TG_LMS на Timeweb Cloud (tsk-005).
  Не подключали сразу — на тот момент шла починка уже существующей инфраструктуры (балансировщик,
  домены), для чего этот сервер не подходит по определению.
- **Зависит от / блокируется:** ничем; имеет смысл подключить, когда понадобится создавать
  новые ресурсы Timeweb (ещё один VDS/БД/App) в этом или другом проекте.

### Восстановить корректную начальную Alembic-миграцию (нет создания базовых таблиц)
- **Что:** миграция `add_courses_triggers` (самая первая в цепочке, `down_revision=None`)
  создаёт триггеры на таблицах `user_courses`/`courses`/`course_dependencies`, которые нигде
  в истории миграций явно не создаются через `create_table`. На чистой БД `alembic upgrade
  head` падает с самого начала (`UndefinedTableError`). Похоже, исходная схема когда-то была
  создана напрямую (`Base.metadata.create_all()` в раннем деве) до того, как проект начал
  трекать миграции Alembic.
- **Почему:** любой будущий полностью чистый деплой (новая БД с нуля) упрётся в ту же ошибку.
  Сейчас обойдено вручную: снят `pg_dump --schema-only` с рабочей локальной dev-БД `Learn` и
  применён напрямую к прод-БД, затем `alembic stamp head` (без реального прогона миграций).
  Это работает, но хрупко — обходной путь, не постоянное решение.
- **Плюсы/минусы:** правильный фикс (сгенерировать настоящую baseline-миграцию через
  `alembic revision --autogenerate` от пустой БД) — трудоёмкий, требует сверки со всеми ~30
  миграциями; текущий обходной путь (dump+stamp) быстрый, но не самодокументируемый — новый
  разработчик не сможет поднять чистую БД одним `alembic upgrade head`.
- **Контекст:** обнаружено 2026-07-05 при первом деплое LMS на VPS (tsk-005) — App Platform
  никогда не доходил до реального прогона миграций из-за более ранних проблем с доменами/edge.
- **Зависит от / блокируется:** ничем не блокируется, но стоит сделать до следующего раза,
  когда понадобится поднять ещё одну чистую копию БД (staging, тесты, ещё один регион).

### Зафиксировать политику `COOKIE_DOMAIN=victor-komlev.ru` (риск shared-domain cookie) — риск уже АКТИВЕН, не только на будущее
- **Что:** сессионная cookie LMS/SPW сейчас ставится с `Domain=victor-komlev.ru` (без учёта
  поддомена), чтобы браузер отправлял её и на `api.learn.victor-komlev.ru`, и на
  `learn.victor-komlev.ru`. По правилам cookie `Domain=victor-komlev.ru` действует на **весь**
  родительский домен и все его поддомены уже сейчас — включая `www.victor-komlev.ru`
  (WordPress-сайт, живой прямо сейчас), а не только на гипотетические будущие поддомены.
- **Почему:** независимый ревью (Кодекс, tsk-159) указал на этот риск как на архитектурный;
  при повторной проверке (2026-07-06) Кодекс уточнил — `www.victor-komlev.ru` уже существует
  и уже резолвится на WordPress/root `victor-komlev.ru`, то есть widescoped cookie уже делит
  сессионную область с WordPress прямо сейчас, а не только «если завтра появится поддомен».
  WordPress — типовая цель для XSS/плагин-уязвимостей; при XSS на WordPress атакующий сможет
  прочитать/использовать `document.cookie` для `victor-komlev.ru`, включая сессию LMS.
- **Плюсы/минусы:** текущая схема (widescoped cookie) — простая, не требует отдельного
  auth-домена или прокси-схемы для шаринга сессии между `api.learn` и `learn`; минус — WordPress
  (более широкая площадь атаки: темы/плагины) уже сейчас в одной cookie-области с LMS/SPW, и
  любой новый сервис на `*.victor-komlev.ru` в будущем добавит то же самое.
- **Правило (зафиксировать и соблюдать до архитектурного фикса):** не размещать на любом
  поддомене `*.victor-komlev.ru` приложения, которым не доверяем в той же мере, что и LMS/SPW —
  WordPress **уже** под этим правилом, а не только будущие поддомены. Оценить риск WordPress
  отдельно (обновления core/плагинов, WAF) с учётом того, что он теперь косвенно защищает и
  сессию LMS.
- **Контекст:** cookie получила `Domain` атрибут 2026-07-05/06 при фиксе кросс-поддоменного
  логина (сессия не долетала с `api.learn` до `learn` без него) — см. коммит `06ddccf` в LMS.
  Риск найден постфактум Кодексом 2026-07-06 (tsk-159); уточнён (WordPress уже в scope, не
  гипотеза) при повторной проверке Кодекса в тот же день.
- **Зависит от / блокируется:** правильный фикс — выделенный auth-домен (например
  `auth.victor-komlev.ru`) со своим прокси-хендшейком сессии вместо расшаривания cookie через
  родительский домен; требует отдельного проектирования. Не блокирует текущую эксплуатацию, но
  приоритет стоит поднять с учётом того, что риск уже реализован, а не гипотетичен.

### Расширить CSP (Content-Security-Policy) на весь SPW, не только `/embed/:path*`
- **Что:** сейчас CSP-заголовок в `next.config.ts` (SPW) применяется только к пути
  `/embed/:path*` (для iframe-встраивания, см. Y-5 §6.5.5). Остальные страницы приложения
  (включая `/me`, `/auth`, дашборды) отдаются без CSP вообще.
- **Почему:** независимый ревью (Кодекс, tsk-159) отметил отсутствие site-wide CSP как
  «Significant» (не критичный, но заметный) риск — при XSS-уязвимости где-либо в приложении
  нет второго рубежа защиты в виде ограничения источников скриптов/стилей.
- **Плюсы/минусы:** плюс — дополнительный слой защиты от XSS на всём приложении; минус —
  формирование корректной site-wide CSP-политики для Next.js приложения с внешними
  интеграциями (VK ID виджет, Google Sheets и т.п.) требует аккуратного подбора allow-list —
  наспех сделанная политика рискует сломать легитимные внешние скрипты/стили без заметного
  прироста в защите.
- **Контекст:** найдено 2026-07-06 при независимом ревью Кодекса после миграции на prod VPS
  (tsk-159). Решение — не спешить с точечным патчем, а сделать отдельной задачей с явным
  проектированием allow-list.
- **Зависит от / блокируется:** ничем не блокируется; делать отдельной задачей, когда будет
  время на аккуратный аудит всех внешних источников скриптов/стилей/фреймов, которые
  использует SPW (VK ID, любые сторонние виджеты).

## FIXED

- 2026-07-06: `rollback.sh` для VPS-деплоя LMS и SPW — `deploy.sh` сохраняет текущий SHA в
  `.last-deploy-sha` перед `git reset --hard`, `rollback.sh` откатывает код+зависимости+рестарт
  к этой версии одной командой. Alembic-миграции не откатываются автоматически (осознанное
  ручное решение). Коммиты: `2416fd2` (LMS), `2dda4f1` (SPW), запушены; скрипты подтянуты и
  `.last-deploy-sha` засеян текущим HEAD на обоих серверах.
- 2026-07-05: экранирование `%` в `DATABASE_URL` для Alembic ConfigParser (падал на
  URL-encoded паролях вида `%251MVd...%3Df`) — `app/db/migrations/env.py` (eb60788)
