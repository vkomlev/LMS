tsk-297 (2026-07-20): /fastapi-api-developer + /db-check — штатный механизм правки прогресса ученика
(эндпоинты teacher/students/{id}/progress, сервис manual_progress, миграция student_material_progress.source).
Модель данных выбрана по read-only разведке прода (MCP learn_prod), решение и обоснование —
docs/specs/2026-07-20-tech-spec-tsk297-manual-progress.md. Прод-write только через протокол /db-check.

tsk-316 (F4): /db-check — прод-write подсказок (hints_video) к 27 валидированным Python SA_COM. Read через MCP read-only (learn_prod + content_backbone_prod), маппинг разбор↔задание валидирован поштучно, write — scripts/backfill_python_hints_video_tsk316.py в транзакции (dry-run → --apply при DBCHECK_OK=1).

tsk-335/336 (2026-07-21): /fastapi-api-developer — override лимита (mode=grant_same_again), батч needs_attention/help-request/pending_review на GET /progress, миграция индекса help_requests. Живой прогон на проде выявил рассинхрон → tsk-339/tsk-340.

tsk-339 (2026-07-21): /fastapi-api-developer + /db-check — автозакрытие заявок blocked_limit при переходе задания в PASSED (attempts.py, help_requests_service.py). Прод-бэкфилл 9 подтверждённых стухших заявок только через /db-check (dry-run → --apply).

tsk-364 (2026-07-22): правка напрямую по образцу tsk-344 (scripts/fix_stem_move_formula_to_hint_tsk344.py) —
аудит и правка формул/операторов решения в stem по курсам 104-110/561 (методист/assignment-rules §3.4a п.3).
Data-скрипт для прод-БД, протокол /db-check соблюдён вручную (dry-run по умолчанию, запись
только через DBCHECK_OK=1 --apply, транзакция со сверкой текущего stem, verify внутри
транзакции + независимая проверка через MCP learn_prod_db read-only после коммита).
