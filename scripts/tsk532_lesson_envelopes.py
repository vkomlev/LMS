# -*- coding: utf-8 -*-
"""tsk-532: явные цели-действия («конверт урока») в начале 25 тем курса 112.

ЗАЧЕМ
tsk-425 (экспертное ревью курса 112 «ЕГЭ по информатике») нашло тот же
дефект, что и в курсе 88 (tsk-525): ни одна тема не открывается явной
формулировкой цели («после этой темы сможешь...»).

Задача формулировалась под «28 тем», но между постановкой и работой над ней
три параллельных чипа (tsk-528/529/530/531) правили структуру и содержание
курса 112. Свежая проверка через MCP (read-only) показала: у курса 112
сейчас 26 прямых детей — 25 содержательных тем («Задание 1» … «Задание 27»,
19-21 объединены в одну тему) + 1 контейнер «Сложные задания»
(course_id=1378, requirement_level=recommended, без своих материалов —
оборачивает 25 подкурсов усложнённых вариантов тех же тем). Контейнер
исключён из охвата — обоснование в reviews/2026-08-04-tsk532-course112-
lesson-envelopes-proposal.md (утверждено оператором).

Формулировки составлены по реальному содержанию каждой темы (материалы +
банк заданий, прочитаны целиком через MCP read-only) — не шаблонной
заглушкой. Полный текст формулировок, обоснование по каждой теме и решение
двух пограничных случаев (темы 23/26) — там же (утверждено оператором
2026-08-04).

КУДА ВСТАВЛЯЕТСЯ
23 из 25 тем: первый по order_position материал темы уже является текстовым
(content.format='html', is_active=true) — формулировка вставляется первым
абзацем в его content.text (не создаём новый материал, по аналогии с
tsk-525/tsk-523).

2 темы-исключения (23 «Рекурсивный обход дерева», курс 150; 26 «Обработка
данных», курс 153): единственный текстовый материал темы стоит на
order_position=0, но является списком тренировочных ссылок и был
is_active=false. Оператор выбрал вариант A: активировать материал
(is_active=true) и вставить формулировку туда же, первым абзацем перед
списком ссылок.

ИДЕМПОТЕНТНОСТЬ
Перед вставкой проверяем, что content.text ещё не содержит "После этой темы
сможешь" — если уже есть, пропускаем (повторный запуск безопасен).
is_active выставляется в true безусловно (повторная установка true не вредит).

Запуск: dry-run по умолчанию;
  python scripts/tsk532_lesson_envelopes.py
  DBCHECK_OK=1 python scripts/tsk532_lesson_envelopes.py --apply
"""
from __future__ import annotations

import argparse
import asyncio
import json
import os
import sys
from pathlib import Path

import asyncpg

if sys.platform == "win32":
    os.system("chcp 65001 >nul 2>&1")
    if hasattr(sys.stdout, "reconfigure"):
        sys.stdout.reconfigure(encoding="utf-8")

project_root = Path(__file__).resolve().parents[1]

# course_id (тема) -> (order_number в course_parents курса 112, ожидаемый заголовок)
COURSE_112_CHILDREN = {
    140: (2, "Задание 1 ЕГЭ по информатике. Информационные модели"),
    148: (3, "Задание 2 ЕГЭ по информатике. Таблицы истинности"),
    138: (4, "ЕГЭ по информатике. Задание №3. Базы данных в Excel"),
    155: (5, "Задание 4 ЕГЭ по информатике. Неравномерное кодирование и условие Фано"),
    156: (6, "Задание 5 ЕГЭ. Анализ алгоритмов для исполнителей"),
    157: (7, "Задание 6 ЕГЭ по информатике. Исполнитель Черепаха."),
    158: (8, "Задание 7 ЕГЭ. Кодирование различных видов информации. Передача информации."),
    159: (9, "Задание 8 ЕГЭ по информатике. Комбинаторика"),
    160: (10, "Задание 9 ЕГЭ по информатике. Агрегатные функции и вычисления в электронных таблицах"),
    141: (11, "Задание 10 ЕГЭ по информатике. Поиск информации в документах"),
    162: (12, "Задание 11 ЕГЭ по информатике. Вычисление объема информации"),
    163: (13, "Задание 12 ЕГЭ по информатике. Машина Тьюринга"),
    139: (14, "Решение заданий 13 ЕГЭ. Организация компьютерных сетей и адресация."),
    142: (15, "Задание 14 ЕГЭ по информатике. Позиционные системы счисления."),
    143: (16, "Задание 15 ЕГЭ по информатике. Логические операции"),
    144: (17, "Задание 16 ЕГЭ по информатике. Рекурсивные функции"),
    145: (18, "Задание 17 ЕГЭ по информатике. Обработка числовых последовательностей"),
    146: (19, "Задание 18 ЕГЭ по информатике. Жадные алгоритмы"),
    147: (20, "Задание 19-21 ЕГЭ по информатике. Теория игр"),
    149: (21, "Задание 22 ЕГЭ по информатике. Параллельные процессы"),
    150: (22, "Задание 23 ЕГЭ по информатике. Рекурсивный обход дерева"),
    151: (23, "Задание 24 ЕГЭ по информатике. Обработка текста"),
    152: (24, "Задание 25 ЕГЭ по информатике. Обработка числовых данных"),
    153: (25, "Задание 26 ЕГЭ по информатике. Обработка данных"),
    154: (26, "Задание 27 ЕГЭ по информатике. Анализ данных"),
}
# course_id 1378 «Сложные задания» (order_number=27) — вне охвата, не в этом словаре.

# course_id -> material_id (точка вставки — первый по order_position материал темы)
TARGET_MATERIAL: dict[int, int] = {
    140: 358, 148: 393, 138: 318, 155: 415, 156: 420,
    157: 426, 158: 435, 159: 439, 160: 444, 141: 366,
    162: 369, 163: 373, 139: 353, 142: 378, 143: 382,
    144: 385, 145: 387, 146: 389, 147: 391, 149: 404,
    150: 406, 151: 407, 152: 409, 153: 411, 154: 412,
}

# Темы, где материал-точка вставки нужно ещё и активировать (был is_active=false).
ACTIVATE_MATERIALS = {150: 406, 153: 411}

# material_id -> формулировка (без обёртки <p><b>...</b> ...</p> — добавляется в коде)
ENVELOPES: dict[int, str] = {
    358: (
        "строить граф по таблице расстояний или таблице связности, вычислять "
        "степень вершины по матрице, находить кратчайший путь между пунктами "
        "через дерево маршрутов и сопоставлять вершины графа с номерами в "
        "таблице — однозначно и по вспомогательным признакам, когда "
        "соответствие неоднозначно"
    ),
    393: (
        "строить таблицы истинности для конъюнкции, дизъюнкции, отрицания, "
        "импликации и эквиваленции, применять приоритет логических операций "
        "при разборе выражений со скобками и по неполному фрагменту таблицы "
        "истинности определять, какому столбцу соответствует каждая переменная"
    ),
    318: (
        "связывать несколько таблиц базы данных через функцию <code>ВПР</code> "
        "(VLOOKUP), считать нужные показатели функциями "
        "<code>СУММЕСЛИМН</code> и сводными таблицами, а также фильтровать и "
        "сортировать данные, чтобы найти ответ на конкретный вопрос задания "
        "(выручка, остатки, начисления по товару/магазину/периоду)"
    ),
    415: (
        "проверять код на условие Фано через построение дерева кодирования "
        "(никакое кодовое слово не начало другого), подбирать кратчайшее "
        "допустимое кодовое слово для нового символа и вычислять минимальную "
        "длину кодирования слова или сообщения по известным кодам букв"
    ),
    420: (
        "переводить число в двоичную систему через <code>bin()</code> и "
        "обратно, по шагам разбирать алгоритм преобразования числа "
        "(дописывание бит чётности, инверсия разрядов, "
        "перестановка/суммирование цифр) и находить искомое N или результат "
        "R, смоделировав алгоритм в коде на Python"
    ),
    426: (
        "переводить алгоритм «Повтори k […]» для исполнителя Черепаха в цикл "
        "Python на <code>turtle</code> (<code>forward</code>/<code>right</code>/"
        "<code>goto</code>), пошагово отслеживать координаты и направление "
        "после серии команд, считать количество целочисленных точек внутри "
        "фигуры и на её границе для разных геометрических комбинаций "
        "(многоугольник, наложение фигур)"
    ),
    435: (
        "вычислять объём памяти для текста, растрового изображения и звука "
        "по формулам I = N×i и I = t×f×n×i, оценивать время передачи данных "
        "по каналу связи через Q = q×t, пересчитывать объём файла при "
        "изменении глубины цвета, частоты дискретизации, числа каналов или "
        "степени сжатия"
    ),
    439: (
        "считать число слов/кодов по формуле N=n^L и её модификациям с "
        "ограничениями (запрет повторов, запрет сочетаний букв, обязательное "
        "вхождение символа), находить слово по номеру в алфавитно "
        "упорядоченном списке (и обратную задачу), перебирать варианты через "
        "<code>itertools.product</code>/<code>permutations</code>/"
        "<code>combinations</code> и генераторы списков"
    ),
    444: (
        "применять агрегатные функции <code>МАКС()</code>/<code>МИН()</code>/"
        "<code>СРЗНАЧ()</code>/<code>СЧЁТЕСЛИ()</code> и условную "
        "<code>ЕСЛИ()</code> к строкам электронной таблицы, отбирать строки "
        "по нескольким числовым условиям одновременно (сравнение с "
        "максимумом/минимумом строки, чётность, попадание в диапазон) и "
        "считать их количество или долю, использовать "
        "<code>НАИМЕНЬШИЙ()</code>/<code>НАИБОЛЬШИЙ()</code> для порядковых "
        "статистик"
    ),
    366: (
        "настраивать точный поиск в текстовом редакторе (только слово "
        "целиком, с учётом регистра, регулярные выражения) и писать "
        "эквивалентный поиск в Python через <code>re</code>, точно считать "
        "вхождения слова или сочетания букв в тексте, различая целое слово и "
        "часть другого слова, с учётом сносок и регистра"
    ),
    369: (
        "вычислять информационный объём сообщений и паролей по формулам "
        "I = N×K и Q = 2^K, определять минимальное количество бит на символ "
        "по мощности алфавита, переводить единицы измерения (биты → байты → "
        "Кбайты → Мбайты) и находить количество информации по формуле "
        "Шеннона для вероятностных событий"
    ),
    373: (
        "читать таблицу команд машины Тьюринга и определять нужную команду "
        "по текущему состоянию и символу под головкой, вручную моделировать "
        "выполнение программы на ленте (замена символа, сдвиг, остановка) и "
        "решать задачи на подсчёт максимального и минимального числа "
        "символов до и после работы программы"
    ),
    353: (
        "разбирать URL-адрес на протокол, домен, каталог и имя файла, "
        "представлять IP-адрес в двоичном виде и вычислять адрес подсети "
        "через побитовую конъюнкцию с маской, определять порядковый номер "
        "устройства в сети"
    ),
    378: (
        "переводить числа между произвольными системами счисления вручную и "
        "через Python-функции, находить основание системы по условиям на "
        "число цифр и остаток, определять количество определённых цифр "
        "(нулей, шестёрок и т.п.) в записи больших выражений со степенями"
    ),
    382: (
        "определять области истинности импликации и эквивалентности через "
        "включение и объединение множеств, сводить логические формулы с "
        "делимостью, отрезками и побитовыми операциями к операциям над "
        "множествами и находить минимальное или максимальное значение "
        "параметра A, при котором выражение тождественно истинно"
    ),
    385: (
        "переводить рекуррентное соотношение F(n) в рекурсивную функцию на "
        "Python (базовый случай и условный переход через <code>if</code>/"
        "<code>elif</code>/<code>else</code>), применять <code>lru_cache</code> "
        "и <code>sys.setrecursionlimit</code>, чтобы вычислять значения при "
        "больших n без переполнения стека, а также вручную трассировать "
        "разворачивание рекурсии «вниз-вверх», чтобы проверять решение на "
        "малых значениях n"
    ),
    387: (
        "читать последовательность чисел из файла в список "
        "(<code>[int(i) for i in f]</code>), перебирать пары и тройки подряд "
        "идущих элементов или все возможные комбинации элементов вложенными "
        "циклами, применять условия делимости и последней цифры "
        "(<code>%3==0</code>, <code>abs(x)%10==3</code>) и находить "
        "количество таких пар/троек вместе с максимальной или минимальной "
        "суммой их элементов"
    ),
    389: (
        "строить таблицу динамического программирования по клеткам поля "
        "(<code>best[i][j] = val[i][j] + max(best[i-1][j], best[i][j-1])</code>), "
        "учитывать внутренние стены и запрещённые ходы при переходах между "
        "клетками, и находить максимальную и минимальную денежные суммы, "
        "которые робот собирает на пути из левого верхнего угла в правый "
        "нижний"
    ),
    391: (
        "определять выигрышные и проигрышные позиции игры «куча камней» "
        "методом слоёв (+1, −1, +2, −2 и другие ходы), писать рекурсивную "
        "функцию с <code>lru_cache</code> для перебора позиций в играх с "
        "одной или двумя кучами, и находить минимальное значение S, при "
        "котором у игрока есть гарантированная стратегия выигрыша первым "
        "или вторым ходом"
    ),
    404: (
        "по таблице «процесс — время выполнения — зависимости» вычислять "
        "раннее время начала и окончания каждого процесса через "
        "<code>ВПР</code> и <code>МАКС</code>, находить минимальное время "
        "завершения всей совокупности процессов при максимальном "
        "распараллеливании независимых веток, и определять максимальное "
        "число процессов, выполняющихся одновременно в заданный момент, "
        "через диаграмму Ганта или подсчёт пересечения интервалов"
    ),
    406: (
        "писать рекурсивную функцию, которая перебирает все программы "
        "исполнителя (вызывая себя для каждой применимой команды — «прибавь», "
        "«вычти», «умножь», «раздели нацело» и другие) и подсчитывает число "
        "программ, переводящих число из одного значения в другое, а также "
        "добавлять в рекурсию условия на траекторию — обязательное или "
        "запрещённое прохождение через конкретное промежуточное число"
    ),
    407: (
        "обрабатывать текст в цикле по символам для поиска максимальных "
        "цепочек (одинаковых символов, чередующихся, по шаблону вида "
        "<code>XYZXYZ…</code>) приёмом скользящего окна с двумя указателями, "
        "а также решать те же задачи через <code>re.findall()</code>/"
        "<code>re.finditer()</code> и накапливать префиксную сумму по модулю "
        "для подсчёта блоков с заданной суммой"
    ),
    409: (
        "писать функцию-генератор <code>get_div(x)</code> для перебора "
        "делителей числа за O(√n), отбирать числа по количеству и чётности "
        "делителей и сортировать результат через <code>sorted(key=lambda ...)"
        "</code> по нестандартному критерию (например, по произведению "
        "делителей), а также проверять число на соответствие маске с "
        "символами <code>?</code>/<code>*</code>"
    ),
    411: (
        "читать входные данные построчно из прилагаемого файла "
        "(<code>open()</code>, разбор чисел на строку), сортировать массив по "
        "нужному критерию и применять жадный алгоритм с двумя указателями "
        "для парного сопоставления элементов (команда↔самолёт, "
        "цилиндр↔цилиндр, пиксель↔пиксель) с ограничением на разницу "
        "значений между парой"
    ),
    412: (
        "разбивать точки на кластеры по заданным прямоугольным границам и "
        "находить медоид кластера — реальную точку с минимальной суммой "
        "расстояний до остальных точек кластера — как через формулы "
        "<code>СРЗНАЧЕСЛИ</code>/<code>СУММПРОИЗВ</code>/<code>СЧЁТЕСЛИ</code> "
        "в Excel/LibreOffice, так и программно на Python (перебор пар через "
        "<code>hypot()</code>)"
    ),
}

MARKER = "После этой темы сможешь"


def _dsn() -> str:
    """Прод-DSN learn: из окружения либо из .mcp.json (паттерн tsk-362/366/373/524/525)."""
    env = os.environ.get("LEARN_PROD_DSN") or os.environ.get("DATABASE_URL", "")
    dsn = env.replace("postgresql+asyncpg://", "postgresql://")
    if "5.42.107.253" not in dsn:
        for candidate in (project_root / ".mcp.json", Path(r"D:\Work\LMS\.mcp.json")):
            if not candidate.exists():
                continue
            cfg = json.loads(candidate.read_text(encoding="utf-8"))
            servers = cfg.get("mcpServers", cfg)
            for arg in servers["learn_prod_db"]["args"]:
                if isinstance(arg, str) and arg.startswith("postgresql://") and "5.42.107.253" in arg:
                    dsn = arg
                    break
    if "5.42.107.253" not in dsn or "/learn" not in dsn:
        raise RuntimeError("Не нашёл прод-DSN learn (5.42.107.253/learn).")
    return dsn


async def main(apply: bool) -> None:
    conn = await asyncpg.connect(_dsn())
    try:
        # ── Разведка ДО любых изменений ─────────────────────────────────
        children = await conn.fetch(
            "SELECT cp.course_id, cp.order_number, c.title "
            "FROM course_parents cp JOIN courses c ON c.id = cp.course_id "
            "WHERE cp.parent_course_id = 112 ORDER BY cp.order_number",
        )
        child_map = {r["course_id"]: (r["order_number"], r["title"]) for r in children}
        expected_ids = set(COURSE_112_CHILDREN) | {1378}
        if set(child_map) != expected_ids:
            raise RuntimeError(
                f"Граф курса 112 изменился с момента разведки: "
                f"ожидали {sorted(expected_ids)}, нашли {sorted(child_map)}"
            )
        for course_id, (expected_order, expected_title) in COURSE_112_CHILDREN.items():
            actual_order, actual_title = child_map[course_id]
            if actual_order != expected_order or actual_title != expected_title:
                raise RuntimeError(
                    f"Курс {course_id}: ожидали order_number={expected_order} "
                    f"title={expected_title!r}, нашли order_number={actual_order} "
                    f"title={actual_title!r} — план устарел."
                )

        material_ids = list(TARGET_MATERIAL.values())
        materials = await conn.fetch(
            "SELECT id, course_id, title, content, order_position, is_active "
            "FROM materials WHERE id = ANY($1::int[])",
            material_ids,
        )
        by_id = {r["id"]: r for r in materials}

        print("=" * 78)
        print(f"tsk-532 · конверты уроков курса 112 · {'ПРИМЕНЕНИЕ' if apply else 'DRY-RUN'}")
        print("=" * 78)

        plan: list[tuple[int, int, str, str, bool, bool]] = []
        # (course_id, material_id, old_text, new_text, old_is_active, new_is_active)
        for course_id, material_id in TARGET_MATERIAL.items():
            m = by_id.get(material_id)
            if m is None:
                raise RuntimeError(f"Материал {material_id} темы {course_id} не найден — план устарел.")
            if m["course_id"] != course_id:
                raise RuntimeError(
                    f"Материал {material_id} сейчас принадлежит курсу {m['course_id']}, "
                    f"а не {course_id} — план устарел."
                )
            content = json.loads(m["content"]) if isinstance(m["content"], str) else m["content"]
            old_text = content.get("text") or ""
            already_done = MARKER in old_text
            envelope_html = f"<p><b>{MARKER}:</b> {ENVELOPES[material_id]}.</p>"
            new_text = old_text if already_done else envelope_html + old_text
            new_is_active = True if material_id in ACTIVATE_MATERIALS.values() else m["is_active"]
            plan.append((course_id, material_id, old_text, new_text, m["is_active"], new_is_active))
            status_bits = []
            status_bits.append("уже применено (пропуск)" if already_done else "к вставке")
            if m["is_active"] != new_is_active:
                status_bits.append(f"is_active {m['is_active']}→{new_is_active}")
            print(f"  курс={course_id:>4}  материал={material_id:>4}  "
                  f"is_active={m['is_active']}  {', '.join(status_bits)}  «{m['title']}»")

        pending = [p for p in plan if p[2] != p[3] or p[4] != p[5]]
        print(f"\nВсего тем: {len(plan)}. К изменению: {len(pending)}. "
              f"Уже применено: {len(plan) - len(pending)}.")

        if not apply:
            print("\nDRY-RUN: ничего не записано. Повтор с --apply.")
            return

        async with conn.transaction():
            for course_id, material_id, old_text, new_text, old_active, new_active in pending:
                if old_text != new_text:
                    await conn.execute(
                        "UPDATE materials SET content = jsonb_set(content, '{text}', $1::jsonb) "
                        "WHERE id = $2",
                        json.dumps(new_text),
                        material_id,
                    )
                if old_active != new_active:
                    await conn.execute(
                        "UPDATE materials SET is_active = $1 WHERE id = $2",
                        new_active,
                        material_id,
                    )
            print(f"\nОбновлено материалов: {len(pending)}")

            # ── Верификация ДО COMMIT ────────────────────────────────────
            print("Верификация в транзакции:")
            all_ok = True
            for course_id, material_id, _old_text, expected_text, _old_active, expected_active in plan:
                row = await conn.fetchrow(
                    "SELECT content->>'text' AS text, is_active FROM materials WHERE id = $1",
                    material_id,
                )
                ok = (
                    row["text"] == expected_text
                    and MARKER in row["text"]
                    and row["is_active"] == expected_active
                )
                all_ok = all_ok and ok
                print(f"  курс={course_id:>4} материал={material_id:>4}: "
                      f"{'OK' if ok else 'МИСМАТЧ'}")
            if not all_ok:
                raise RuntimeError("Верификация не сошлась — ROLLBACK.")

        print("\nCOMMIT выполнен. Независимую проверку — через MCP learn_prod_db "
              "(отдельным read-only соединением) и живым рендером в браузере.")
    finally:
        await conn.close()


if __name__ == "__main__":
    ap = argparse.ArgumentParser(description="tsk-532: конверты уроков курса 112")
    ap.add_argument("--apply", action="store_true", help="выполнить запись (по умолчанию dry-run)")
    args = ap.parse_args()
    asyncio.run(main(args.apply))
