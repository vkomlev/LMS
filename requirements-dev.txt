-r requirements.txt

mypy==1.20.2
pytest==9.0.2
pytest-asyncio==1.3.0

# Синхронный драйвер PG — нужен разовым скриптам аудита и правки боевых данных
# (scripts/audit_stale_false_verdicts_tsk602.py и соседи). Приложение работает
# через asyncpg; здесь psycopg2 только ради скриптов (tsk-602).
psycopg2-binary==2.9.10
